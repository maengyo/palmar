__pycache__/
*.pyc
.DS_Store

# 패키징이 지어 내는 것 (#23)
/dist/
*.egg-info/

# 로고 시안 — 다른 세션이 만들고 있다. `git add -A` 에 쓸려 들어가지 않게 여기 적어 둔다.
/docs/img/brand/

# probe-agent 가 남기는 보고 (dev/probe-agent.py)
palmar-probe-*.txt

# 앱(app/)이 짓는 것. Cargo.lock 은 넣는다 — 실행파일을 내는 크레이트라 빌드가 재현돼야 한다.
/app/target/
/app/.rust/
