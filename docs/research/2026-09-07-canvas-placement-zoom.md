# 캔버스 터미널 앱 다섯 개 — 배치와 줌 조사 원문 (2026-09-07)

에이전트 둘이 cate·opencove·termcanvas·nodeterm·ccanvas 의 **소스를 직접 읽고** 정리한 것이다.
각 항목에 (verified|likely) 등급과 파일·줄 번호가 붙어 있다. verified 는 조사 에이전트가 소스에서 확인한 것이고,
palmer 저자가 재확인한 것은 아니다. 요약과 판단은 `docs/own-runtime.md` 의 "캔버스 — 남들은 어떻게 했나" 에 있다.

각도 둘: **placement**(새 창이 어디에 놓이는가) · **zoom-xterm**(CSS 변형 아래에서 xterm.js 를 어떻게 멀쩡하게 두는가).


######################################################################
#### placement: 33 findings
######################################################################

- [1] (verified) cate has TWO distinct new-panel placement paths, both in src/renderer/canvas/placement.ts: (a) `findFreePosition` — the non-interactive auto-place, and (b) `recommendPlacements` — the interactive numbered-"ghost" picker. A third, `nudgeToFree`, is the free click-anywhere escape hatch. The file header states this outright: "Powers the interactive 'ghost' placement picker (recommendPlacements) and the non-interactive auto-placement path (findFreePosition / nudgeToFree). No store or React dependencies: nodes in, candidate spots out."
    SRC: cate-src/src/renderer/canvas/placement.ts:1-13 (repo 0-AI-UG/cate @ 2c1c345adbc32fbfedd8ac9acefb203d88d6da1a, main, 2026-09-06)
    NOTE: placement.ts is 563 lines; layoutEngine.ts is 176 lines and holds only grid-snap + AABB + shared-border helpers, no placement search.

- [2] (verified) (1) AUTO-PLACE ALGORITHM = four-ray cardinal jump-past-obstacles search from a reference node, NOT a spiral, NOT a cascade, NOT grid-fill. `findFreePosition(nodes, focusedNodeId, defaultSize, preferred?)`: empty canvas → `preferred ?? {x:100,y:100}` returned VERBATIM (no snap). If `preferred` given and grid-snapped-preferred is free → return it. Else reference = focused node, else `nodeList.reduce((a,b)=> b.creationIndex > a.creationIndex ? b : a)` (most recently created). Then for each of 4 directions `[{dx:1,dy:0},{dx:-1,dy:0},{dx:0,dy:1},{dx:0,dy:-1}]`, walk a ray: start one gap past the reference's edge, and on each collision JUMP to just past the blocking obstacle (`p = {x: obstacle.origin.x + obstacle.size.width + gap, y: p.y}` etc.), capped at `for (let i = 0; i < 200; i++)`. Winner = the slot whose center is nearest the reference center by `Math.hypot`. Result grid-snapped on return.
    SRC: cate-src/src/renderer/canvas/placement.ts:30-113
    NOTE: Test at placement.test.ts:365-373 pins it: a single 640x400 node at (0,0) → new node lands at {x:0,y:440} (below, one gap); block below → {x:0,y:-440} (above), because vertical slots are nearer for a wide panel.

- [3] (verified) (1) CONSTANTS: `PLACEMENT_GAP = 40` (canvas-space gap kept by every non-interactive placement); `CANVAS_GRID_SIZE = 20` (the snap/background grid, shared by CanvasGrid, auto-placement and snap-to-grid); ray step cap `200` iterations per direction; `nudgeToFree` ring cap `25` rings at `grid*2 = 40px` per ring step; `recommendPlacements` default `max = 6` candidates; `SNAP_TOL = PLACEMENT_GAP / 2` = 20; `FIT_TOL = CANVAS_GRID_SIZE` = 20; `EPS = 1`; free-rect list capped at 80 (`if (out.length >= 80) break`).
    SRC: cate-src/src/renderer/canvas/placement.ts:117 (PLACEMENT_GAP), :126-127, :76, :519-523, :291, :248; cate-src/src/renderer/canvas/layoutEngine.ts:22 (CANVAS_GRID_SIZE)

- [4] (verified) (1) SIZING/SHAPE CONSTANTS for the ghost picker: `PLACEMENT_MIN_W = 280`, `PLACEMENT_MIN_H = 180`, `PLACEMENT_MAX_W = 1400`, `PLACEMENT_MAX_H = 900`, `USEFUL_MIN_W = 400`, `USEFUL_MIN_H = 340`, `FILL_AR_FACTOR = 1.6`. A grown-to-fill gap is skipped if its aspect ratio strays outside `[defAR/1.6, defAR*1.6]`: `if (ar < defAR / FILL_AR_FACTOR || ar > defAR * FILL_AR_FACTOR) continue`.
    SRC: cate-src/src/renderer/canvas/placement.ts:121-136, :471-475

- [5] (verified) (1) THE GHOST PICKER is a maximal-empty-rectangles packer, nearest-first. `recommendPlacements` decomposes a "place area" minus the gap-inflated existing nodes into maximal free rectangles (`freeRectangles` → `splitFree` into up to 4 slabs → `pruneFreeRects`), then loops `for (let n = 0; n < max && free.length > 0; n++)`: each iteration scores EVERY free rect, picks the one whose resulting ghost lands closest to the ranking point, carves that ghost (plus gap) out of the free space via `splitFree`, and repeats. Place area = focused node inflated by `pitchX*2 / pitchY*2` (where pitch = std size + gap) when a focused node is on screen, ranked from its centre; otherwise the whole viewport rect, ranked from the cursor anchor.
    SRC: cate-src/src/renderer/canvas/placement.ts:262-283 (doc), :386-404 (area/rankAt), :406-497 (packing loop), :223-260 (splitFree/pruneFreeRects/freeRectangles)

- [6] (verified) (2) COLLISION DETECTION = plain AABB `rectsOverlap` linear-scanned against ALL nodes. No spatial index anywhere in the repo (grep for quadtree/rtree/spatial index returns nothing). `export function rectsOverlap(a, b) { return !(a.origin.x + a.size.width <= b.origin.x || b.origin.x + b.size.width <= a.origin.x || a.origin.y + a.size.height <= b.origin.y || b.origin.y + b.size.height <= a.origin.y) }` — touching edges explicitly do NOT count as overlap. Auto-place scans with `nodeList.find(...)`, nudgeToFree with `nodeList.some(...)`.
    SRC: cate-src/src/renderer/canvas/layoutEngine.ts:39-48; placement.ts:45-50, :516-517; grep for quadtree/rtree/spatialindex in src/ → zero hits
    NOTE: The picker's clearance check inflates by gap-1: `const clear = (rect, others) => !others.some((r) => rectsOverlap(inflateRect(rect, gap - 1), r))` (placement.ts:314), i.e. 39px inflation, so a spot exactly one gap away survives.

- [7] (verified) (3) OVERLAP IS AVOIDED BY EVERY AUTOMATIC PLACEMENT AND FREELY ALLOWED BY DRAGGING. There is NO 'allow overlap' setting. The two canvas settings that touch geometry are `snapToGrid` (default `false`, "Align panels to the grid while dragging and resizing. Hold Alt to bypass.") and `placementPicker` (default `true`, "On Cmd+T or a toolbar click, show numbered spots to pick from. Off places panels automatically."). Dragging a node calls `moveNode(id, origin)` which does a bare `{...node, origin}` with zero collision check.
    SRC: cate-src/src/renderer/settings/CanvasSettings.tsx:37-54; src/shared/types.ts:1368,1373,1565-1566; src/renderer/stores/canvas/nodesSlice.ts:140-151 (moveNode)

- [8] (verified) (4) NO-FREE-SPACE FALLBACKS — three different ones, none of which 'grows the canvas' (the canvas is unbounded; coordinates go negative freely): (a) `findFreePosition` STACKS BELOW EVERYTHING: `const maxBottom = nodeList.reduce((acc,n) => Math.max(acc, n.origin.y + n.size.height), -Infinity); return { x: snap(ref.origin.x), y: snap(maxBottom + gap) }`. (b) `nudgeToFree` GIVES UP AND ALLOWS THE OVERLAP after 25 rings: `return start // give up — allow the overlap rather than refuse the placement`. (c) `recommendPlacements` guarantees ≥1 candidate — if the packer yields nothing, `finalize` falls back to `findFreePosition`: `if (out.length === 0) { const p = snapPt(findFreePosition(nodes, focusedNodeId, std)); out.push({ point: p, size: std }) }`.
    SRC: cate-src/src/renderer/canvas/placement.ts:107-112, :519-528, :343-346
    NOTE: The give-up contract is explicitly tested: placement.test.ts:402-413 blankets the whole 25-ring x 40px = 1000px radius and asserts the returned point overlaps.

- [9] (verified) (5) SnapGuides.tsx IS DEAD CODE IN CURRENT main. It renders from `useCanvasStoreContext((s) => s.snapGuides)` and is mounted in Canvas.tsx:691, but the only writer, `setSnapGuides`, has ZERO production call sites — a repo-wide grep for `setSnapGuides` finds only its type declaration (storeTypes.ts:192), its slice implementation (selectionSlice.ts:24), the action-name union, and two test mocks (`vi.mock('./SnapGuides', () => ({ default: () => null }))`). `snapGuides` is initialised `{ lines: [] }` in canvasStore.ts:66 and never set, so `if (guides.lines.length === 0) return null` always fires.
    SRC: cate-src/src/renderer/canvas/SnapGuides.tsx:1-59; src/renderer/canvas/Canvas.tsx:17,691; src/renderer/stores/canvas/selectionSlice.ts:24-30; src/renderer/stores/canvasStore.ts:66; repo-wide grep for setSnapGuides|SnapGuides
    NOTE: Practical answer to 'does it apply to placement or only dragging': neither — it applies to nothing today.

- [10] (verified) (5, real answer) cate DOES have panel-to-panel alignment snapping, but it lives in PLACEMENT, not dragging. `deriveGuides(nodes, gap)` builds "sorted, deduped alignment lines implied by the existing windows: each edge plus edge ± gap" — for each node it adds x ∈ {left, right} and each ± gap, same for y. `snapAxis(lo, size, guides, tol, grid)` then snaps the ghost's low OR high edge to the nearest guide within `SNAP_TOL = 20`, falling back to `Math.round(lo/grid)*grid`. Applied at placement.ts:479-482: `x: clamp(snapAxis(rawX, w, guides.xs, SNAP_TOL, grid), ix0, ix1 - w)`. DRAGGING, by contrast, gets only grid snapping: `const origin = snap ? snapToGrid(rawOrigin, CANVAS_GRID_SIZE) : rawOrigin` with `const snap = useSettingsStore.getState().snapToGrid && !ev.altKey`.
    SRC: cate-src/src/renderer/canvas/placement.ts:138-156 (deriveGuides), :189-202 (snapAxis), :423, :477-482; src/renderer/drag/resolve.ts:231-238; src/renderer/drag/useDragOp.ts:430-434
    NOTE: So the design is inverted from what one would guess from the filenames: guide snapping = placement-only; grid snapping = drag/resize-only; the visible guide overlay = unwired.

- [11] (verified) (6) DEFAULT SIZES ARE FIXED PER PANEL TYPE, not viewport-derived and not remembered. `PANEL_DEFINITIONS.terminal.defaultSize = { width: 640, height: 400 }`, `minimumSize = { width: 320, height: 200 }`. Others: browser 800x600, editor 600x500, agent 760x480, document 700x500, review 1000x700, canvas 800x600. `PANEL_DEFAULT_SIZES` / `PANEL_MINIMUM_SIZES` are derived via Object.fromEntries over PANEL_DEFINITIONS.
    SRC: cate-src/src/shared/panels.ts:80-194; src/shared/types.ts:1668-1674

- [12] (verified) (6) A SECOND, SMALLER SIZE TABLE exists for in-canvas drops: `PANEL_CANVAS_DROP_SIZES` with terminal 520x340, browser 640x440, editor 540x420, canvas 640x480, agent 520x440, document 640x480, review 820x560 — commented "Compact sizes used when a panel is dropped onto the canvas from a non-canvas-node source (e.g. a tab dragged out of a side/main dock window). PANEL_DEFAULT_SIZES sizes fresh windows in their own shells and is too large for an in-canvas drop." Used only at useDragOp.ts:236 for the ghost/landing size.
    SRC: cate-src/src/shared/types.ts:1676-1688; src/renderer/drag/useDragOp.ts:10,236

- [13] (verified) (6) SIZE IS 'REMEMBERED' ONLY FOR AN EXISTING NODE BEING MOVED, not for new creates. `measureCanvasNodeGrab` takes the dragged node's live `node.size` as the ghost size — except when maximized, where it deliberately uses `preMaximizeSize` so the ghost matches the post-spring-load footprint. There is no last-used-size preference for fresh panels anywhere in cate.
    SRC: cate-src/src/renderer/drag/useDragOp.ts:145-175

- [14] (verified) (6) THE EMPTY-CANVAS PICKER OFFERS SIZE, NOT POSITION. When there are no nodes (or the viewport is panned to blank space), `centred(c)` returns three spots whose meaningful difference is size: default (`std`, ranks best), `large = std * 1.4`, `compact = std * 0.65`, each clamped to [MIN,MAX] and grid-snapped at source. Comment: "On an empty/blank area, position is irrelevant — so offer the meaningful choice instead: SIZE."
    SRC: cate-src/src/renderer/canvas/placement.ts:350-370

- [15] (verified) GHOST SIZING RULE (the interesting part of cate's design): the ghost mirrors an adjacent neighbor's FULL size on BOTH axes rather than using the panel default, so recommendations tile into a uniform grid. `matchedNeighborSize(f, inflated, gap, rankAt)` finds the window touching the free rect with the LONGEST shared run along its touching edge (tie-broken by centre nearest `rankAt`), and returns its ORIGINAL size (`{ width: oR - oL - 2*gap, height: oB - oT - 2*gap }`). Three-way sizing branch: mirror-if-fits (within FIT_TOL=20) → else grow-to-fill the gap if it clears USEFUL_MIN (400x340) and the AR guard → else the panel default if it fits. Already-placed ghosts are pushed into `obstacles` so they act as neighbors for subsequent ghosts: "Spots also mirror already-placed ghosts (not just real windows), so the recommendations tile outward into a uniform grid."
    SRC: cate-src/src/renderer/canvas/placement.ts:158-187, :417-421, :438-466

- [16] (verified) WHICH PATH RUNS WHEN, in cate: `openPanel`-style creates route through appStore/helpers.ts. The picker is used ONLY when all of: focus is on, the workspace is active, no explicit position was given, an onGhostCancel rollback exists, AND the `placementPicker` setting is on — `if (focus && isActiveWorkspace && canvasPosition == null && onGhostCancel && useSettingsStore.getState().placementPicker) { const shown = ops.beginPanelTarget({...}); if (shown) return }`. Otherwise it falls through to `ops.addNodeAndFocus(...)` → `addNode` → `findFreePosition`. Explicit-position paths (drag-drop, session restore, right-click 'new here') and background creates (`focus: false`) always skip the picker.
    SRC: cate-src/src/renderer/stores/appStore/helpers.ts:335-355

- [17] (verified) SPECIAL CASE: the very first panel on an EMPTY canvas skips the picker entirely and is centred in the viewport. `beginPanelTarget` short-circuits when `request.panelId && request.availability === 'new' && Object.keys(state.nodes).length === 0`, computing `origin = { x: center.x - nodeSize.width/2, y: center.y - nodeSize.height/2 }` from `viewToCanvasCoords({x: cs.width/2, y: cs.height/2}, zoom, viewportOffset)` and calling addNode directly.
    SRC: cate-src/src/renderer/stores/canvas/panelTargetSlice.ts:186-206

- [18] (likely) A DRAG-DROP ONTO THE CANVAS CAN STILL BE RELOCATED AWAY FROM THE CURSOR. `placeNodeOnCanvas(api, panelId, panelType, origin, size)` calls `addNode(panelId, panelType, origin, size)`, and `addNode` passes `position` into `findFreePosition(state.nodes, focusedNodeId(state), defaultSize, position)` as the `preferred` argument. Since `findFreePosition` only honours a preferred point when it is collision-free, dropping a panel on top of an existing one silently moves it to a cardinal-ray slot instead of leaving it where the user dropped it.
    SRC: cate-src/src/renderer/drag/commit.ts:159-169; src/renderer/stores/canvas/nodesSlice.ts:39-100 (esp. :73)
    NOTE: Verified by reading the call chain; not verified by running the app. The interactive free-placement path (`commitFreePlacement`) instead pre-resolves with `nudgeToFree` before calling addNode, so it lands on its own grid-aligned nudge.

- [19] (verified) THE INTERACTIVE PICKER MOVES THE CAMERA. `beginPanelTarget` computes `fitCamera(...)` over the candidate rects + existing candidates + focused node, with `padding = 80`, and sets `zoomLevel`/`viewportOffset` so all ghosts are visible: `const fit = Math.min(containerSize.width/width, containerSize.height/height); const zoom = clamp(Math.min(state.zoomLevel, fit), ZOOM_MIN, ZOOM_MAX)` (ZOOM_MIN 0.3, ZOOM_MAX 3.0). The previous zoom/offset are stashed in `prevZoom`/`prevOffset` and restored on cancel or on a non-addNode commit.
    SRC: cate-src/src/renderer/stores/canvas/panelTargetSlice.ts:63-97, :124-145, :283-292; src/shared/types.ts:1694-1696

- [20] (verified) THE FREE 'CLICK ANYWHERE' ESCAPE HATCH is armed with the `f` key while the picker is open, and uses `nudgeToFree`: a Chebyshev-ring spiral over `grid*2 = 40px` steps, 25 rings max, from the grid-snapped desired top-left (cursor minus half the panel size). `for (let r = 1; r <= 25; r++) for (dy) for (dx) { if (Math.max(Math.abs(dx),Math.abs(dy)) !== r) continue // ring only; const p = { x: start.x + dx*grid*2, y: start.y + dy*grid*2 }; if (free(p)) return p }`.
    SRC: cate-src/src/renderer/canvas/placement.ts:502-529; src/renderer/canvas/PanelTargetLayer.tsx:73-77, :111-123; src/renderer/stores/canvas/panelTargetSlice.ts:254-281

- [21] (verified) BULK ARRANGE is separate from placement and does NOT respect PLACEMENT_GAP. `autoLayout()` builds a uniform viewport-sized grid with `gap = 6`, `cols = Math.max(1, Math.round(Math.sqrt(n * aspect)))`, `cellW = Math.max(240, (containerWidth - gap*(cols+1))/cols)`, `cellH = Math.min(cellW * 0.72, Math.max(160, ...))` and RESIZES every node to the cell. `tidyGridSelected(gap = 16)` uses `cols = Math.ceil(Math.sqrt(n))` and the max width/height across the selection as the cell so mixed sizes never overlap; `stackSelected(axis, gap = 16)` lays the selection out in one row/column anchored at its top-left.
    SRC: cate-src/src/renderer/stores/canvas/arrangeSlice.ts:15-125

- [22] (verified) STALE COMMENT in cate: placement.ts:128-130 says the tuning values are adjusted with "the dev viz, Cmd/Ctrl+Shift+G", but the CHANGELOG records "**Placement debugging overlay**: removed its development-only visualization, keyboard toggle, and trace collection" and no such shortcut exists in useShortcuts.ts (only `metaKey && !shiftKey && key === 'g'`).
    SRC: cate-src/src/renderer/canvas/placement.ts:128-130; CHANGELOG.md:46,80; src/renderer/hooks/useShortcuts.ts:162

- [23] (verified) COMPARISON — opencove (DeadWaveWave/opencove @ 55fcd93, @xyflow/react ^12.10.1) does NOT use xyflow's built-in layout. It hand-rolls a Chebyshev-ring nearest-free search on a 24px grid: `const GRID_STEP = WORKSPACE_ARRANGE_GRID_PX` (=24), `const MAX_SCAN_RADIUS = 80`, `findNearestFreePosition` returns `desired` if available, else expands `radius` 1..80 collecting perimeter offsets (`if (Math.max(Math.abs(x), Math.abs(y)) !== radius) continue`) and picks the euclidean-nearest free one, returning at the first radius that yields any hit. Five variants exist: plain, `WithinBounds`, `OnRight`, `AroundBounds` (directional, tries right/down/left/up layer by layer), and `findCanvasOverflowPosition` (past `maxRight + GRID_STEP`).
    SRC: pa-opencove/src/contexts/workspace/presentation/renderer/utils/collision.ts:1-344; utils/workspaceArrange.shared.ts:8

- [24] (verified) COMPARISON — opencove REFUSES to create the node when nothing fits, and kills the PTY. Unlike cate (which always places something), `createNodeForSession` does: `if (resolvedPlacement.canPlace !== true) { await window.opencoveApi.pty.kill({ sessionId }); onShowMessage?.(t('messages.noTerminalSlotNearby'), 'warning'); return null }`. The layered fallback chain before that verdict is `resolveWorkspaceNodesPlacement`: anchor-if-free → within-target-space → free anchor within space bounds → around the space bounds in preferred directions → nearest-free → canvas-overflow (right of everything) → give up.
    SRC: pa-opencove/src/.../hooks/useNodesStore.createNodes.ts:76-93; src/contexts/workspace/domain/workspaceNodePlacement.ts:23-162

- [25] (verified) COMPARISON — opencove's anchor is viewport-centre-minus-half-size (`resolveNodePlacementAnchorFromViewportCenter(center, size) => { x: center.x - size.width/2, y: center.y - size.height/2 }`), and its default sizes are a canonical design-token grid, not literals: `CANONICAL_BUCKETS = { compact: {col:108,row:72}, regular: {col:120,row:80}, large: {col:132,row:88} }` x `KIND_UNITS.terminal = {col:4,row:4}` + `WORKSPACE_CANONICAL_GUTTER_PX = 12` per internal gutter → regular terminal = 120*4+12*3 = 516 x 80*4+12*3 = 356, clamped to min 400x260 / max 720x520. The bucket is a user setting (`standardWindowSizeBucket`).
    SRC: pa-opencove/src/.../workspaceCanvas/helpers.ts:231-236; src/contexts/workspace/domain/workspaceNodeSizing.ts:20-58,185-197; workspaceCanvas/constants.ts:47-51

- [26] (verified) COMPARISON — opencove is the ONE of the five with real object snapping WITH guides, and like cate's guides it is drag-only. `workspaceSnap.ts` computes per-axis snaps of kind `'none' | 'grid' | 'object'` against edges AND centers, emitting `WorkspaceSnapGuide = {kind:'v', x, y1, y2} | {kind:'h', y, x1, x2}`, capped at `WORKSPACE_SNAP_MAX_CANDIDATES = 96` within `WORKSPACE_SNAP_SEARCH_PADDING_PX = 288`. Its only callers are `useNodeDragSession.ts:287`, `useSpaceOwnership.ts:157`, `useSpaceDrag.preview.ts:108` — all drag paths; placement never calls it.
    SRC: pa-opencove/src/.../utils/workspaceSnap.ts:1-53,207; grep for resolveWorkspaceSnap callers

- [27] (verified) COMPARISON — termcanvas (blueberrycongee/termcanvas @ fa25981, @xyflow/react ^12.10.1) inverts the whole model: the NEW tile is pinned and the EXISTING tiles get PUSHED OUT OF THE WAY. `pickPlacement` picks an anchor by priority (preferredPosition → parent terminal's right edge + gap → focused terminal in worktree → rightmost sibling → rightmost tile in another worktree of the same project → viewport centre → {0,0}), snaps to `SNAP_GRID = 10`, then inserts a `__placement_placeholder__` rect and runs `resolveCollisions(allRects, ADJACENCY_GAP /*8*/, placeholderId)` with the placeholder as the ANCHOR — so it never moves and everything else does. It then returns a `nudged: Array<{id,x,y}>` list which the caller applies via `projectStore.updateTerminalPosition` to every displaced existing terminal.
    SRC: pa-termcanvas/src/canvas/terminalPlacement.ts:31-32,201-296; src/actions/terminalSceneActions.ts:146-178

- [28] (verified) COMPARISON — termcanvas's collision resolver is an iterative minimum-translation-vector push-apart, not a search. `resolveCollisionsDetailed(rects, gap, anchorIds)` runs `maxIterations = 20` sweeps of all pairs; on overlap it computes `overlapX = (a.width+b.width)/2 + gap - Math.abs(deltaX)` and `overlapY` likewise, pushes along the smaller-overlap axis, and splits the movement 50/50 (`a.x -= direction*(move/2); b.x += direction*(move/2)`) unless one side is anchored, in which case only the unanchored one moves the full amount. Two anchored rects are left overlapping (`continue`).
    SRC: pa-termcanvas/src/canvas/collisionResolver.ts:14-108

- [29] (verified) COMPARISON — termcanvas is the only one of the five whose default terminal size is BOTH remembered AND viewport-derived. `createTerminal` reads `usePreferencesStore.getState().defaultTerminalSize` ("Sticky default: once the user has manually resized a terminal, use that size for every subsequent creation"); with no stored value it falls back to `computeTileDimensions(windowWidth, windowHeight, leftOffset, rightOffset)` which holds a constant AREA — `TARGET_AREA = 640*480`, `h = Math.sqrt(TARGET_AREA/ratio); w = TARGET_AREA/h`, clamped to `MIN_W 400 / MAX_W 900 / MIN_H 300 / MAX_H 700` — so the tile matches the visible canvas's aspect ratio, sidebar insets included.
    SRC: pa-termcanvas/src/stores/projectStore.ts:192-239; src/stores/tileDimensionsStore.ts:6-64

- [30] (verified) COMPARISON — nodeterm (eneskirca/nodeterm @ f456e88, @xyflow/react ^12.11.5) has BOTH a modulo-4 stagger grid and a ring search, used in different situations. `staggeredPosition(index) => { x: 80 + (index % 4) * 360, y: 120 + Math.floor(index/4) * 320 }` is the no-cursor default inside `placeAt`; `freeSpot(existing, preferred, size, gap = 28)` is the ring search added later for dock/palette/kanban creates whose header explains the bug it fixed: "Those all fell back to the view center, so every node created that way piled up on the same spot — switch to the canvas and you'd find a stack of overlapping nodes." Rings step by the NODE SIZE, not the grid: `const stepX = size.w + gap; const stepY = size.h + gap`, `for (let ring = 1; ring <= 60; ring++)`, give up → `return preferred` (overlap allowed).
    SRC: pa-nodeterm/src/renderer/lib/placement.ts:1-54; src/renderer/state/workspace.ts:219-257
    NOTE: Only one production caller of freeSpot: Canvas.tsx:3596 inside `emptyNodePos()`, which filters out ephemeral subagent/loop cards before building the box list.

- [31] (verified) COMPARISON — nodeterm's default node size is a USER SETTING, clamped. `defaultNodeWidth: 640, defaultNodeHeight: 440` in DEFAULT_SETTINGS, surfaced in Settings → Behavior, and read through `terminalNodeSize()` which clamps to `[280, 2400]` x `[160, 1600]` because "settings.json is hand-editable, and a 0x0 or NaN node would be unclickable/ungrabbable forever". Per-kind minimums live separately in `NODE_MIN_SIZES` (terminal 260x160). `placeNode` grid-snaps the whole rect via `snapNodeToGrid` when the `snapToGrid` setting is on, raising w/h to the grid-aligned per-kind minimum.
    SRC: pa-nodeterm/src/shared/types.ts:1328-1329,1667-1668; src/renderer/state/workspace.ts:237-274; src/renderer/lib/nodeSizing.ts:13-55

- [32] (verified) COMPARISON — ccanvas (DevoidSloth/ccanvas @ 6f91395, hand-rolled canvas, NO xyflow) is the naive baseline: viewport centre + modulo-8 cascade, ZERO collision detection. `const world = screenToWorld(center(), ws.camera); const step = 32; const off = (ws.elements.length % 8) * step - step * 4; const x = world.x + off; const y = world.y + off` — so spawns fan down-right over 8 slots then wrap and start overlapping again. Sizes are a fixed per-kind table `WIDGET_SIZE` (terminal 520x340, agent 540x380, editor 560x440, diff 680x460) and the widget is centred on the point: `x: x - w/2, y: y - h/2`.
    SRC: pa-ccanvas/src/ui/Toolbar.tsx:165-179; src/store/workspace.ts:74-92,645-677

- [33] (verified) SUMMARY OF THE FIVE APPROACHES to 'where does a new panel land', for design comparison: cate = four-ray cardinal jump-past-obstacles from the focused/newest node + an optional numbered ghost picker backed by maximal-empty-rectangle packing with neighbor-size mirroring; opencove = Chebyshev ring search on a 24px grid from viewport-centre, refuses to create if nothing fits; termcanvas = anchor at the focused/rightmost sibling's right edge and PUSH existing tiles away; nodeterm = viewport-centre, ring search stepped by node size, gives up into overlap; ccanvas = viewport-centre + 8-slot cascade, no collision check at all. None of the four @xyflow/react apps uses an xyflow built-in layout for this — xyflow contributes the pan/zoom surface and (in nodeterm) `snapGrid`, but every placement decision is hand-written.
    SRC: Synthesis of all five files cited above

  OPEN:
   - cate's `findFreePosition` searches only 4 CARDINAL directions from a single reference node — never diagonals, and never from a second reference. With many panels arranged in a cross, all four rays can walk 200 obstacle-jumps and still find space, so the stack-
   - The drag-drop relocation finding (a panel dropped on top of an existing one gets silently moved to a cardinal slot, because `placeNodeOnCanvas` → `addNode` → `findFreePosition(preferred)`) is verified by reading the call chain but not by running cate. If it ma
   - I did not measure the cost of cate's O(nodes) linear AABB scans at scale. `pruneFreeRects` is O(free^2) per obstacle and caps the free list at 80, and the ghost packer is O(max * free * nodes); `e2e/perf-stress.spec.ts` exists but I did not read or run it. If 
   - Nothing in any of the five apps offers an explicit 'allow overlap' setting, so I could not find prior art for a canvas that treats overlap as a first-class feature rather than a failure. Since your brief says overlap is allowed by design, cate's overlap-avoida
   - cate's SnapGuides.tsx being unwired could be a mid-refactor state on main rather than a deliberate removal; I checked only the current tree, not the history. `git log -- src/renderer/canvas/SnapGuides.tsx` would settle whether the writer was deleted recently.

######################################################################
#### zoom-xterm: 49 findings
######################################################################

- [1] (verified) Local read-only checkouts used for every quote below (nothing under <저장소> was touched): cate @ commit 2c1c345 = <스크래치패드>/full/0-AI-UG-cate-2c1c345 ; opencove @ 55fcd93 = .../scratchpad/x_opencove/DeadWaveWave-opencove-55fcd93 ; termcanvas @ fa25981 = .../scratchpad/x_termcanvas/blueberrycongee-termcanvas-fa25981 ; nodeterm @ f456e88 = .../scratchpad/x_nodeterm/eneskirca-nodeterm-f456e88 ; ccanvas @ 6f91395 = .../scratchpad/x_ccanvas/DevoidSloth-ccanvas-6f91395. All paths in later findings are repo-relative under these roots.
    SRC: gh api repos/<r>/tarball, extracted to scratchpad

- [2] (verified) (1) cate canvas zoom is ONE CSS transform on a single 'world' div, applied imperatively from a store subscription so React never re-renders during pan/zoom: `const transform = `scale(${zoom}) translate(${offset.x / zoom}px, ${offset.y / zoom}px)`` … `el.style.transform = transform; el.style.setProperty('--zoom', String(zoom))`. The world div is `position:absolute; top:0; left:0; width:1; height:1; transformOrigin:'0 0'` and holds all nodes.
    SRC: cate:src/renderer/canvas/Canvas.tsx:213-252 (transform), 676-693 (world div JSX)
    NOTE: Comment at :213 — 'Imperatively update the world div transform on zoom/offset changes so Canvas itself never re-renders during pan/zoom — only the world div moves.' A second identical transform is applied to a body-portalled `topOverlayWorldRef` layer (:218, :710-724). The `--zoom` custom property is set but never read anywhere in src (grep for '--zoom' returns only that one write).

- [3] (verified) (1) cate toggles `will-change: transform` on the world layer only for the duration of a gesture and de-promotes 150 ms after it settles, deliberately, because a promoted layer bitmap-scales its cached texture.
    SRC: cate:src/renderer/canvas/Canvas.tsx:226-240
    NOTE: Quote: 'Promote the world to its own GPU layer for the duration of the gesture so pan/zoom stays smooth, then de-promote once it settles. While promoted, Chromium bitmap-scales the layer's cached texture (blurs thin SVG icon strokes); removing will-change forces a crisp re-raster at the resting transform. Debounced so it only fires after the user stops interacting.' `willChangeResetRef` timeout is

- [4] (verified) (1) cate zoom range is 0.3–3.0: `export const ZOOM_MIN = 0.3` / `export const ZOOM_MAX = 3.0` / `export const ZOOM_DEFAULT = 1.0`. Every setter clamps (setZoom, setZoomAndOffset, zoomAroundCenter, animateZoomTo, zoomToFit, zoomToSelection).
    SRC: cate:src/shared/types.ts:1694-1696; cate:src/renderer/stores/canvas/viewportSlice.ts:36,49,59,87,212,251
    NOTE: zoomToSelection additionally caps a single-node target: `const maxZoom = target.length === 1 ? Math.min(ZOOM_MAX, 1.5) : ZOOM_MAX` (viewportSlice.ts:250) — 'Cap a single-node target so we don't over-zoom a small panel.'

- [5] (verified) (1) cate zoom inputs: trackpad pinch and Cmd/Ctrl+scroll are handled FIRST and identically (`if (e.metaKey || e.ctrlKey) { applyWheelZoom(e, mouse); … }`); a physical mouse-wheel notch also zooms Miro-style under the select tool; a trackpad two-finger scroll pans; the hand tool always pans.
    SRC: cate:src/renderer/hooks/useCanvasInteraction.ts:278-290 (pinch/Cmd), 332-337 (mouse-wheel zoom), 341-375 (pan)
    NOTE: Comment at :278-282: 'Handled FIRST so it zooms the canvas no matter what's under the cursor … trackpad pinch as a wheel event with ctrlKey set, so this one rule covers both the pinch gesture and the explicit Cmd/Ctrl+scroll.' Mouse-vs-trackpad discrimination is `isMouseWheel()` from src/renderer/lib/wheelIntent.ts, using Chromium's `wheelDeltaY`.

- [6] (verified) (1) cate zoom steps: a discrete mouse-wheel notch is PROPORTIONAL — `base * (1 + Math.sign(-e.deltaY) * MOUSE_WHEEL_ZOOM_FACTOR * zoomSpeed)` with `const MOUSE_WHEEL_ZOOM_FACTOR = 0.15`; a continuous pinch is delta-proportional — `base + -e.deltaY * 0.01 * zoomSpeed`. `zoomSpeed` is a user setting, default 1.0, slider 0.5→3.0 step 0.1.
    SRC: cate:src/renderer/hooks/useCanvasInteraction.ts:24-28,227-238; cate:src/shared/types.ts:1560; cate:src/renderer/settings/CanvasSettings.tsx:25-26
    NOTE: Accumulation is from the in-flight target, not the live zoom: `const base = targetZoom.current ?? zoomLevel` (:231), then `targetZoom.current = Math.min(Math.max(next, ZOOM_MIN), ZOOM_MAX)`.

- [7] (verified) (1) cate zoom is animated, not snapped: a rAF loop lerps zoomLevel toward targetZoom with a frame-rate-corrected ease, anchored at the cursor. `zoomEaseForElapsed(ms) = 1 - Math.pow(1 - 0.15, ms / (1000/60))` — 'Time-correct equivalent of applying a 0.15 lerp once per 60 Hz frame.'
    SRC: cate:src/renderer/lib/canvas/zoomAnimation.ts:1-10; cate:src/renderer/hooks/useCanvasInteraction.ts:155-197; cate:src/renderer/stores/canvas/viewportSlice.ts:82-127
    NOTE: Offset is recomputed each frame to keep the cursor point fixed: `{ x: cursorViewPoint.current.x - canvasPoint.x * newZoom, y: … }` (useCanvasInteraction.ts:192-195).

- [8] (verified) (1) cate has +/- toolbar buttons and a clickable zoom-percent readout that resets to 100%, each calling `animateZoomTo(zoom ± 0.1)` / `animateZoomTo(1.0)`; keyboard defaults are Cmd+= / Cmd+- / Cmd+0 mapped to the same ±0.1 / 1.0 actions.
    SRC: cate:src/renderer/canvas/CanvasToolbar.tsx:324-350; cate:src/shared/types.ts:784-786; cate:src/renderer/lib/runAction.ts:190-196
    NOTE: `zoomText = `${Math.round(zoom * 100)}%`` (CanvasToolbar.tsx:180). When a BROWSER panel is focused the zoom keys are let through to the webview instead of the canvas (useShortcuts.ts:294-305).

- [9] (verified) (1) cate registers the wheel listener natively with `{ capture: true, passive: false }` rather than React's onWheel, explicitly because 'React's onWheel is passive by default, which silently ignores preventDefault'.
    SRC: cate:src/renderer/canvas/Canvas.tsx:294-309
    NOTE: Mount-only effect with a ref indirection (`handleWheelRef.current = handleWheel`) so the listener is never re-bound.

- [10] (verified) (2) Zooming does NOT resize the PTY in cate. The render-scale effect explicitly refuses to fit: 'No fit() here. Fitting against a box still sized for the PREVIOUS cell is exactly what dropped columns (see cellScale). Measure the new cell, resize the box to match it, and let the ResizeObserver's plain fit run against a box that is already in proportion — at which point it computes the same column count and never resizes the PTY at all.'
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:674-678
    NOTE: Design intent stated at :586-587: 'Cols × rows stay constant because both the box and the cell grow by the same factor before fit() runs.'

- [11] (verified) (2) cate's regression history documents exactly what a naive zoom→fit does: 'the box grew 1.25× but the cell grew 1.286×, so 90 columns became 87. preserveGrid only absorbs ±1, so a 3-4 column jump went straight through to the PTY as a SIGWINCH and reflowed the shell/TUI.' Root cause: xterm CEILS cell pixels, so cell(k·fs) ≠ k·cell(fs) — measured at 13px/DPR 1, renderScale 1.25 wants an 8.75px cell and gets 9 (+2.9%), 1.5 wants 10.5 and gets 11 (+4.8%).
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:58-73
    NOTE: This is the single most transferable measurement in the repo for a project doing the same thing.

- [12] (verified) (2) cate suppresses the fit() storm a zoom gesture would otherwise cause with three gates: (a) a `canvas-interacting` body-class deferral loop, (b) a 32 ms trailing debounce (`const DEBOUNCE_MS = 32`), (c) a `RESIZE_EPSILON = 0.5` px clientWidth/clientHeight early-out — 'Pure transform changes (canvas pan/zoom) leave clientWidth alone, so this short-circuits before we touch xterm.'
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:299-390
    NOTE: Deferral rationale at :318-323: 'Fitting mid-gesture calls terminal.resize() every tick, which re-sizes the WebGL canvas and makes the panel edge appear to "jump" (terminals only — editors don't resize a GPU canvas).' After a fit it only re-pins scroll if cols/rows actually changed.

- [13] (verified) (2) When a fit does run, cate coalesces it into at most one `terminal.resize()`: `const proposed = fitAddon.proposeDimensions()` … `if (cols !== terminal.cols || rows !== terminal.rows) { terminal.resize(cols, rows) }`, plus a sub-pixel row-overflow guard using getBoundingClientRect().height.
    SRC: cate:src/renderer/lib/terminal/terminalDom.ts:208-253 (safeFit), 464-474 (fit)
    NOTE: Comment: 'Coalesce into a single terminal.resize() call so the PTY only receives one SIGWINCH per fit. Two rapid resizes confuse TUI agents (claude code, vim, htop) which redraw their full frame on each SIGWINCH…'. The fractional-height guard cites anthropics/claude-code#46981.

- [14] (likely) (2) GAP in the deferral: a pure pinch/ctrl-wheel zoom acquires the body class `canvas-zooming`, NOT `canvas-interacting`. `canvas-zooming` has no CSS rule anywhere in the repo and is not consulted by TerminalPanel's runFit guard, so during a pinch the only thing suppressing fits is the 0.5 px clientWidth check — and the render box's width IS `${100 * cellScale.w}%`, which changes when a render-scale step is crossed, waking the ResizeObserver at gesture end.
    SRC: cate:src/renderer/hooks/useCanvasInteraction.ts:242 (acquireBodyClass('canvas-zooming','canvas-wheel-zoom')), :354 (wheel-PAN acquires 'canvas-interacting'); cate:src/renderer/panels/TerminalPanel.tsx:324, 960-961; grep 'canvas-zooming' across src+e2e returns 
    NOTE: Consequence is benign by design (the fit is argued to compute identical cols), but a fit() call — and therefore a possible resize — is not structurally prevented on the zoom path the way it is on the pan/resize path.

- [15] (verified) (3) KEY ANSWER — cate does (a) re-measure + a fontSize/atlas trick + (c) pointer-events:none during interaction. It does NOT do (b) renderer swap by zoom, (d) LOD/screenshot placeholder, or (e) zoom-triggered WebGL disable. The core trick: 'we mimic VS Code's webFrame-zoom trick: when zoom settles on a higher step, we bump xterm's fontSize to the configured base size * renderScale (forcing a fresh higher-resolution atlas) and counter-scale the render box by 1/renderScale so the on-screen size — after the world div's outer scale(zoom) — is unchanged.'
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:577-591
    NOTE: Implementation: `entry.terminal.options.fontSize = terminalBaseFontSize * renderScale` (:672) — 'Mutating options.fontSize triggers xterm's internal renderer refresh, which rebuilds the WebGL glyph atlas at the new resolution.'

- [16] (verified) (3) cate snaps zoom to discrete render-scale steps `[1.0, 1.25, 1.5, 1.75, 2.0, 2.5]`, returns 1.0 for any zoom ≤ 1.0, and caps at 2.5. The 0.25 spacing is a documented fix, not arbitrary: with [1.0, 1.5, …] 'that put the blurriest point at exactly 125% (a 25% upscale) — and 125% was also a tie (|1.25-1.0| === |1.25-1.5|), so a hair past it the scale jumped a full 1.5× at once, visibly resizing the terminal.'
    SRC: cate:src/renderer/lib/terminal/renderScale.ts:7-41
    NOTE: 'Zoom-out returns 1.0: the atlas is already being downsampled, so rasterizing it smaller buys nothing back.' Cap rationale: 'beyond that, atlas memory grows without perceptible gain.' Pure module so it unit-tests without importing the WebGL addon.

- [17] (verified) (3) cate debounces the atlas rebuild to gesture end with a DOUBLE requestAnimationFrame guarded by a captured-zoom re-check: `rescaleRafRef.current = requestAnimationFrame(() => requestAnimationFrame(() => { … if (snapRenderScale(capturedZoom) === target) setRenderScale(target) }))` — 'Waits 2 idle animation frames after the last zoom change so a continuous pinch only rebuilds the atlas at gesture end (each rebuild is expensive).'
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:589-611

- [18] (verified) (3) cate does NOT trust renderScale as the layout factor — it re-MEASURES the cell and drives the box off the measured ratio, per axis. `cellW = screenEl.offsetWidth / cols`, `cellH = screenEl.offsetHeight / rows`, `next = { w: cellW / base.w, h: cellH / base.h }`, then the render box is `width: `${100 * cellScale.w}%``, `height: `${100 * cellScale.h}%``, `transform: `scale(${1 / cellScale.w}, ${1 / cellScale.h})``, `transformOrigin: '0 0'`.
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:679-694 (measure), 953-976 (render box JSX)
    NOTE: Per-axis rationale (:962-970): 'width and height ceil independently, so the cell's aspect ratio shifts slightly per scale step (7:15 → 9:19 → 11:23). A uniform width-driven counter-scale therefore left the height 1.5-2.4% short depending on the step, which read as the terminal's vertical size wobbling on zoom.' A sanity guard rejects mid-layout reads: `if (next.w > 0.5 && next.w < 3 && next.h > 0.

- [19] (verified) (3) cate captures a base-cell BASELINE at the unscaled font and re-derives it whenever it becomes invalid. If a panel mounts while the canvas is already zoomed, it drops to the base font, measures, and goes straight back up in one synchronous block: 'reading offsetWidth forces the layout that makes the middle reading real, so the browser never paints the intermediate state. Costs one extra glyph-atlas rebuild, once per panel.'
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:646-667; baseline shape `{ w, h, dpr }` at :79
    NOTE: Why a scaled cell can't be divided back down: 'the ceil already threw the remainder away (at 1.5 the cell is 11, and 11/1.5 = 7.33 for a cell that is really 7; that 4.8% error then shrank the box at every scale, including 1).'

- [20] (verified) (3) DPR is part of cate's baseline and is re-armed on every change, because 'xterm rounds the cell to DEVICE pixels and reports it back in CSS pixels (css.cell = round(char × dpr) / dpr), so the same font measures 7px on a DPR 1 display and can measure 6.5px on a DPR 2 one. A baseline captured on one screen is simply wrong on the other.' A `measureEpoch` counter re-triggers measurement on attach() and on DPR change.
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:74-78, 81-86, 94-96, 106-133, 297
    NOTE: 'A resolution media query only matches the DPR it was built with, so it has to be re-armed at the new value after each change.' The DPR listener defers its read one rAF so xterm's own MediaQueryList has refreshed first.

- [21] (verified) (3c) cate DOES set pointer-events:none on xterm during canvas gestures, via a module-level injected stylesheet: `.canvas-interacting iframe, .canvas-interacting webview, .canvas-interacting .monaco-editor, .canvas-interacting .xterm, .canvas-interacting .xterm-screen, .canvas-interacting .xterm-helper-textarea { pointer-events: none !important; }` plus `.canvas-interacting .xterm, .canvas-interacting .xterm * { cursor: grabbing !important; }`.
    SRC: cate:src/renderer/canvas/Canvas.tsx:26-86 (injectCanvasInteractingStyle)
    NOTE: A parallel `.canvas-tool-hand` ruleset makes the whole node inert (including chrome buttons and resize strips) while the hand tool is armed, even when idle. The class is applied for wheel-PAN, node drag, edge/dock resize and the drag runtime — not for a pure zoom gesture (see the canvas-zooming finding).

- [22] (verified) (3c) the shared body class is REFCOUNTED across gesture owners after a real stranding bug: 'With plain classList.add/remove a later owner's quiet-timer remove() strips the class out from under a gesture that's still running… which is how `canvas-interacting` ended up stranded on <body> with no way to clear it short of an app restart.' Each reference carries an owner label for leak attribution, and there is a gestureLockWatchdog plus an e2e spec for recovery.
    SRC: cate:src/renderer/lib/dom/bodyClassRefcount.ts:1-45; cate:e2e/gesture-lock-recovery.spec.ts; cate:e2e/wheel-pan-lock.spec.ts
    NOTE: Wheel-pan releases 150 ms after the wheel goes quiet (useCanvasInteraction.ts:356-361).

- [23] (verified) (3) cate's mouse-coordinate fix is a capture-phase rewrite of clientX/clientY on the panel container, using a PER-AXIS effective scale: `const effective = zoomLevel / cellScale.w; const effectiveY = zoomLevel / cellScale.h; … const adjustedX = rect.left + (e.clientX - rect.left) / effective; … Object.defineProperty(e, 'clientX', { value: adjustedX, configurable: true })`, bound for mousedown/mousemove/mouseup with `{ capture: true }`.
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:762-828
    NOTE: Root cause stated at :765-770: 'xterm.js measures cell dimensions via OffscreenCanvas.measureText() or offsetWidth (both unaffected by CSS transforms), but computes mouse offsets using getBoundingClientRect() (affected by transforms).' Per-axis rationale at :777-783: 'the two differ by a couple of percent, which is enough to drift a selection by a row near the bottom of a tall terminal.'

- [24] (verified) (3) The decision of WHETHER to rewrite is extracted into a pure, unit-tested predicate with three skip cases: (1) `if (interacting) return false` — a canvas gesture owns the pointer; (2) non-left button, using the BUTTONS BITMASK for moves — `if (type === 'mousemove' && (buttons & ~1) !== 0) return false`; (3) `if (Math.abs(effective - 1.0) < ZOOM_ADJUST_EPSILON) return false` with `ZOOM_ADJUST_EPSILON = 0.001`.
    SRC: cate:src/renderer/lib/terminal/terminalCoordAdjust.ts:29-59
    NOTE: Case 2 exists because 'MouseEvent.button is normally 0 on follow-up moves even for a middle drag' — without it a middle/right-button drag-pan JUMPS at the start. The file header says the interplay is not reproducible in jsdom, so the guard rather than the bug is what's locked by test.

- [25] (verified) (3) cate has a documented BLANK-TERMINAL-AFTER-ZOOM failure and a forced repaint for it: 'The world div carries `will-change: transform`, so it (and every terminal's WebGL <canvas>) lives on a GPU compositing layer. The WebGL renderer draws with preserveDrawingBuffer:false and only repaints dirty rows, so when the compositor re-rasterizes the layer at a new scale a static terminal's drawing buffer comes up blank until xterm draws another frame.' Fix: `entry.terminal.refresh(0, entry.terminal.rows - 1)` two idle rAFs after every zoomLevel change.
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:720-760
    NOTE: Asymmetry called out: 'Zoom-IN happens to recover on its own because crossing a render-scale step runs the fontSize/fit/refresh effect above. Zoom-OUT clamps renderScale to 1.0 (snapRenderScale), so that effect early-returns and nothing ever repaints — leaving the terminal blank until the next PTY output or a zoom-in.'

- [26] (verified) (3) A second-order bug cate had to fix: xterm caches its scrollbar width ONCE in the Viewport constructor, and the CSS scrollbar track is scaled by `--cell-scale`, so FitAddon kept subtracting a stale 8 px and clipped right-hand columns. cate re-measures and writes it back into the private field: `core.viewport.scrollBarWidth = measured` where `measured = viewport.offsetWidth - viewport.clientWidth`.
    SRC: cate:src/renderer/lib/terminal/terminalDom.ts:420-458 (syncScrollBarWidth); cate:src/renderer/styles/globals.css:255-283; cate:src/renderer/panels/TerminalPanel.tsx:702-718, 973-975
    NOTE: CSS: `.xterm-viewport::-webkit-scrollbar { width: calc(8px * var(--cell-scale, 1)); }` plus scaled thumb borders. 'FitAddon — the ONLY reader of this field, xterm's own core never touches it'. Guarded so a future xterm rename silently disables the scaling instead of throwing; also refuses a 0 or >200 measurement.

- [27] (verified) (3b/3e) cate's WebGL-vs-DOM renderer choice is NOT zoom-driven — it is a CONTEXT BUDGET. Per-window cap `const MAX_WEBGL_TERMINALS = 6`; process-wide cap brokered in main `export const GLOBAL_MAX_WEBGL_TERMINALS = 12`. A denied grant leaves the terminal on xterm's DOM renderer: 'Returns false when the process is at the cap — the caller stays on xterm's DOM renderer, which always paints.'
    SRC: cate:src/renderer/lib/terminal/terminalDom.ts:13-62, 125-150 (createWebglAddon, onContextLoss); cate:src/main/webglBudget.ts:1-70
    NOTE: Rationale: 'Chromium caps live WebGL contexts per GPU PROCESS (~16), shared across every BrowserWindow… Chromium then silently drops or never-paints the overflow contexts, leaving blank terminals.' Grants are reclaimed when a window closes/crashes. Confirmed by CHANGELOG 1.4.0-beta.1: 'WebGL contexts are budgeted process-wide and fall back to the DOM renderer instead of producing blank terminals.'

- [28] (verified) (3d) cate has NO level-of-detail / screenshot / placeholder rendering for terminals at low zoom. A repo-wide grep for `\blod\b|levelOfDetail|level-of-detail` in src/ returns exactly one hit, and it is the background grid: 'LOD: when zoomed out, the on-screen step would get too small. Double the…'. A grep for numeric zoom thresholds (`zoom(Level)? [<>]=? <number>`) finds only renderScale.ts's `zoom <= 1.0`, coordinates.ts's NaN floor, and comment text.
    SRC: cate:src/renderer/canvas/CanvasGrid.tsx:53; repo-wide grep over cate:src/
    NOTE: Terminals are rendered live at every zoom from 0.3 to 3.0; the only 'LOD' is the atlas resolution step, which goes up, never down.

- [29] (verified) (4) cate DOES cull, geometrically, by unmounting nodes — but with NO zoom threshold. The cull rect is the viewport expanded by exactly one screen in each direction, computed in canvas space: `const marginX = cw / z; const marginY = ch / z; const left = -viewportOffset.x / z - marginX; …` then `if (nr < left || nx > right || nb < top || ny > bottom) continue`.
    SRC: cate:src/renderer/stores/canvasStore.ts:300-352 (selectVisibleNodeIds); consumed at cate:src/renderer/panels/CanvasPanel.tsx:213
    NOTE: Doc comment: 'This is the primary lever for reducing memory/CPU when many terminals or editors are open on a canvas — off-screen nodes don't mount at all.' Exemptions that always mount: `if (n.id === focusedNodeId || n.isPinned || keepAlive.has(n.id))` — keepAlive covers webview-backed browsers (unmounting destroys the guest process) and terminals an agent is driving.

- [30] (verified) (4) Culling in cate DISPOSES NOTHING. Unmounting a node unmounts TerminalPanel, which calls `terminalRegistry.detach()`; detach only does `captureViewport(entry)` then `el.parentElement.removeChild(el)`. The Terminal, its addons and the PTY stay live in the registry; actual teardown is a separate `dispose(panelId)` path.
    SRC: cate:src/renderer/lib/terminal/terminalDom.ts:536-558 (detach); cate:src/renderer/lib/terminal/terminalLifecycle.ts:553-584 (disposeTerminalOnly), 608-634 (dispose)
    NOTE: TerminalPanel's own header states it: 'Call terminalRegistry.detach() on unmount — does NOT kill the PTY or dispose anything; the terminal stays live in the registry.'

- [31] (verified) (4) A SECOND culling layer is per-terminal IntersectionObserver with `{ threshold: 0 }`: when the render box stops intersecting, cate detaches the xterm DOM and disconnects the ResizeObserver, and notifies main via `window.electronAPI.terminalSetVisibility(entry.ptyId, isVisible)`; when it re-intersects it re-attaches.
    SRC: cate:src/renderer/panels/TerminalPanel.tsx:438-459
    NOTE: Comment: 'IntersectionObserver: detach WebGL/ResizeObserver when hidden, re-attach when visible. Also notify main so it can SIGSTOP the PTY after it has been hidden + silent for IDLE_SUSPEND_MS.'

- [32] (verified) (4) The visibility signal reaches the runtime, which SIGSTOPs an offscreen+silent pty after `const IDLE_SUSPEND_MS = 2 * 60_000` (2 minutes) and SIGCONTs on input/visibility. The feature is described in-code as OFF by default. Suspended trees are then skipped by the terminal process scanner ('a frozen process tree cannot change state until resumed').
    SRC: cate:src/runtime/capabilities/process.ts:173-174, 221, 240, 248, 257, 366-378, 469, 486-489; cate PR #201 body
    NOTE: Comment at :173-174 — 'Idle-suspend (POSIX-only): SIGSTOP a pty that's offscreen and silent past the threshold, SIGCONT on input/visibility. OFF by default — remote daemons…' (line truncated in grep).

- [33] (verified) (4) The WebGL addon is disposed and RECREATED on every DOM reparent (which includes every IntersectionObserver hide/show cycle), while the process-wide grant is deliberately kept: 'its internal canvas buffers are tied to the old container dimensions and cannot survive a DOM reparent reliably. The panel's grant is a reservation that outlives the addon, so the upgrade below reuses it (requestWebglGrant is idempotent) rather than churning the budget.'
    SRC: cate:src/renderer/lib/terminal/terminalDom.ts:340-353
    NOTE: Grants are released only on context loss, resetRendering(), and dispose() — terminalDom.ts:49-56, 111-121, 135-145; terminalLifecycle.ts:620-621. So a merely CULLED terminal appears to keep its WebGL slot against the 6/12 caps.

- [34] (verified) (4) cate memoizes the cull so a pure pan/zoom costs nothing: the creation-ordered node list is cached in `const sortedNodeCache = new WeakMap<object, CanvasNodeState[]>()` keyed by the `nodes` object identity, and the keep-alive set uses a two-level cache (nodes identity, then a dockLayout-identity membership signature).
    SRC: cate:src/renderer/stores/canvasStore.ts:203-295
    NOTE: 'The cull selector below runs on EVERY store update — including every pan/zoom frame, where only viewportOffset/zoomLevel changed and `nodes` is the same object. Without this cache that path re-allocated Object.values() and re-sorted the whole node set 60×/s during a drag.' DOM order is kept creation-ordered on purpose: 'moving a mounted Electron webview in the DOM reloads its guest.'

- [35] (verified) (5) PR #173 (merged 2026-05-29, shipped in 1.1.0) is the zoom perf entry: 'zoom: stop threading `zoomLevel` as a prop into every `CanvasNode`. It re-rendered every node on every zoom frame (measured 374 renders/s); now 3/s. Resize and `TerminalPanel` read zoom from the store directly.' Same PR added the CATE_PERF=1 profiler (FPS, long-task observer, render counts, dev HUD on Cmd+Alt+P) and `e2e/perf-stress.spec.ts` driving idle / pan / zoom / terminal flood / many-terminals.
    SRC: gh api repos/0-AI-UG/cate/pulls/173 (body); cate:CHANGELOG.md:681 under '## [1.1.0] - 2026-05-29'
    NOTE: Also in that PR: 'many-terminals / big-canvas measured cheap at ~9 nodes, left as-is.' It references `docs/perf-audit.md` for the full before/after, but that file is NOT present in the repo at main (docs/ contains only agent-integrations.md).

- [36] (verified) (5) PR #201 (merged 2026-05-30, shipped in 1.1.1) is the cull perf entry: 'Canvas (`canvasStore.ts`) — Memoize the viewport-cull node sort in a `WeakMap` keyed by the `nodes` object, so a pure pan/zoom (nodes unchanged) no longer re-sorts the full node set every frame.' Measured with 9 terminals: 'Pan/zoom cull re-sorts | 1 per frame | 0', 'Idle subprocess spawns ~18/s → ~0.5/s'. Claim: '120fps and zero long tasks held throughout, including a 46 MB/s multi-terminal flood.'
    SRC: gh api repos/0-AI-UG/cate/pulls/201 (body); cate:CHANGELOG.md:644 under '## [1.1.1] - 2026-06-01'

- [37] (verified) (5) PR #161 (merged 2026-05-29) is about sustained compositor cost, not zoom specifically, but is directly relevant to canvas smoothness: 'WindowServer was pegged with calm paint flashing, the signature of GPU layers being composited every frame rather than repainted. The cause: continuous, always-on animations, made expensive by compositing them over a live backdrop-filter.' Fix included removing the sidebar `backdrop-filter: blur`. Headline: '~45% to ~2%-3%' CPU, 'Between 94% and 96% less WindowServer (mac) utilization.'
    SRC: gh api repos/0-AI-UG/cate/pulls/161 (body)
    NOTE: Directly transferable warning for a browser canvas: a live backdrop-filter over a continuously animating layer re-samples everything behind it every frame.

- [38] (verified) (5) cate's zoom perf gate in CI is loose: the zoom stress test drives 90 rAF-cadence zoom changes over a populated canvas and asserts only `expect(m.fps).toBeGreaterThan(20)` and `expect(m.longTasks.maxMs).toBeLessThan(2000)`. Pan stress has the same thresholds.
    SRC: cate:e2e/perf-stress.spec.ts:180-208 (zoom), 161-177 (pan)
    NOTE: The zoom driver is `h.setZoom(1 + 0.4 * Math.sin(i / 7))` from inside the page — 'deterministic (no cursor/empty-space dependency) and faithful to the real smooth-zoom path'. Other scenarios run at setZoom(0.5)/(0.55) with 9 nodes. No published number exists for many terminals at HIGH zoom.

- [39] (verified) (5) cate CHANGELOG zoom/render entries, with versions: 0.2.5 — 'Crisp terminal fitting: resize work is coalesced and terminal rendering remains sharp at high canvas zoom.'; 1.4.0-beta.1 — 'Terminal renderer exhaustion: WebGL contexts are budgeted process-wide and fall back to the DOM renderer instead of producing blank terminals.'; 1.4.0-beta.3 — 'Terminal GPU rendering: glyph-atlas desynchronization and process-wide WebGL context overflow are contained.'; 1.6.0 — 'Terminal rendering and sizing: … zoom or display-density changes no longer leave unstable glyph sizing or rendering artifacts.'
    SRC: cate:CHANGELOG.md:1134, 341, 317, 172
    NOTE: The arc reads as: crispness (0.2.5) → context exhaustion (1.4.0) → glyph-size stability under zoom/DPR (1.6.0). Each maps to code quoted above.

- [40] (verified) CONTRAST — opencove solves crispness at a strictly better layer than cate: it PATCHES @xterm/addon-webgl@0.19.0 to add `setRasterScale(value)`, which multiplies the renderer's effective DPR and device cell dimensions without ever touching fontSize, so cols/rows/layout are untouched by construction. `device.cell.width = Math.max(1, Math.round(device.cell.width * rasterScale)); … device.canvas.width = this._terminal.cols * device.cell.width;` and `_setCanvasDevicePixelDimensions` is wrapped to multiply by rasterScale.
    SRC: opencove:patches/@xterm__addon-webgl@0.19.0.patch (added block, lines ~6-149 of the diff)
    NOTE: It wraps _updateDimensions, handleResize, _refreshCharAtlas, handleDevicePixelRatioChange, _setCanvasDevicePixelDimensions, the render-layer prototype, and CellColorResolver, temporarily shadowing `_coreBrowserService.dpr` with a getter (`opencoveWithEffectiveDpr`). Explicit invariant comment: 'Raster invariant: _updateDimensions, handleResize and handleDevicePixelRatioChange each recompute effect

- [41] (verified) CONTRAST — opencove's raster levels are `[1, 1.25, 1.5, 1.75, 2]` (clamped to [1,2] in the patch) with 0.05 DOWNGRADE HYSTERESIS, and it picks the first level ≥ canvasZoom (round UP, never blur) rather than nearest: `const desiredScale = terminalRasterScaleLevels.find(scale => scale >= canvasZoom) ?? …`; downgrades are held back by `if (canvasZoom > lowerBoundary - terminalRasterDowngradeHysteresis) return normalizedCurrentScale`.
    SRC: opencove:src/contexts/workspace/presentation/renderer/components/terminalNode/terminalZoomRasterPolicy.ts:1-41; controller at .../terminalRasterScaleController.ts:1-91
    NOTE: cate rounds to NEAREST step and has no hysteresis (renderScale.ts:31-39) — it prevents step-thrash with a 2-rAF debounce instead. Two different answers to the same oscillation problem.

- [42] (verified) CONTRAST — termcanvas AND opencove both fix xterm mouse hit-testing by patching xterm's PRIVATE `_mouseService` instead of rewriting DOM events like cate. termcanvas wraps `getCoords` (selection), `getMouseReportCoords` (escape sequences to mouse-aware TUIs), AND `_selectionService._getMouseEventScrollAmount` — flagging the last as 'the easy path to miss: drag-selecting near the terminal edge asks SelectionService whether to start auto-scroll. That private helper does not call MouseService, so the selection endpoint and edge threshold must both be patched here or they will disagree under React Flow zoom.'
    SRC: termcanvas:src/terminal/xtermMouseScalePatch.ts:1-155; opencove:src/contexts/workspace/presentation/renderer/components/terminalNode/patchXtermMouseService.ts
    NOTE: cate's DOM-event rewrite (TerminalPanel.tsx:773-828) covers mousedown/mousemove/mouseup only — it does NOT obviously cover the auto-scroll threshold path termcanvas calls out, nor is there a separate cate fix for it that I found. opencove additionally derives the scale from `rect.width / element.offsetWidth` rather than reading the canvas store, and subtracts computed padding-left/top.

- [43] (verified) CONTRAST — termcanvas REFUSES to cull terminals, the exact opposite of cate: `export function resolveTerminalMountMode(_input: { focused: boolean; visible: boolean }): TerminalMountMode { // Viewport visibility is too volatile for a stateful xterm surface. // Keep every terminal that is still present in the scene live… return "live" }`. It also disables React Flow's own zoom inputs (`zoomOnScroll={false} zoomOnPinch={false}`, `minZoom={0.1} maxZoom={2}`) and drives zoom itself through discrete steps `[0.1, 0.15, 0.2, 0.25, 0.33, 0.5, 0.67, 0.8, 1, 1.25, 1.5, 2]`.
    SRC: termcanvas:src/terminal/terminalRuntimePolicy.ts:10-19; termcanvas:src/canvas/XyFlowCanvas.tsx:748-751; termcanvas:src/canvas/viewportZoom.ts:8-23
    NOTE: The `_input` parameter is retained but unused — the signature admits culling was tried and abandoned. termcanvas's LOD is BEHAVIORAL not visual: `isOverviewMode` triggers at `viewportScale <= fitAllScale * 1.2`, capture-phase-swallows mousedown/mouseup/click/dblclick on tiles, and turns click→focus, dblclick→zoom-in (TerminalTile.tsx:252-260, 604-640).

- [44] (verified) CONTRAST — nodeterm does not put xterm on the canvas at all. `src/renderer/glyphgrid/` is a hand-written WebGL2 renderer that draws EVERY terminal's grid into ONE shared canvas in world space, with a camera in React Flow's exact convention (`screen = world * zoom + pan`), culling per grid via `rectsIntersect(visibleWorldRect(cam, viewW, viewH))`. xterm is reduced to the emulator behind a hand-written `TermInternals` narrowing: 'Like the rest of this directory it imports NOTHING from xterm.'
    SRC: nodeterm:src/renderer/glyphgrid/addon.ts:1-9; nodeterm:src/renderer/glyphgrid/engine.ts:1-9; nodeterm:src/renderer/glyphgrid/camera.ts:1-28
    NOTE: Rationale for the isolation: 'xterm's private surface is the part of that library most likely to move under us, and keeping it behind one injected interface means a bump breaks ONE thin shell instead of the renderer, and lets the whole render path be unit-tested without a DOM, a canvas or a terminal.' MIN_CAMERA_ZOOM = 0.01.

- [45] (verified) CONTRAST — nodeterm found and fixed two zoom artifacts a DOM/CSS-transform approach never sees. (i) Pan must be snapped to whole DEVICE pixels (zoom must NOT be): 'A pan carrying a sub-device-pixel fraction re-rolls that choice EVERY frame: during a drag the same glyph is resampled slightly differently sixty times a second, and the text ripples behind node chrome that is moving rigidly — the "our text waves while the node doesn't" report.' (ii) Atlas filtering is zoom-banded: `if (zoom >= 1) return { min: 'nearest', mag }`, `if (zoom >= NEAR_ONE_ZOOM /* 0.9 */) return { min: 'linear', mag }`, else trilinear-over-mips; `const mag = zoom > 1 ? 'linear' : 'nearest'`.
    SRC: nodeterm:src/renderer/glyphgrid/camera.ts:30-49; nodeterm:src/renderer/glyphgrid/gl-webgl2.ts:41-95; nodeterm:src/renderer/glyphgrid/atlas-filter.test.ts:1-41
    NOTE: The filter band was a measured user report: 'The 2026-08-09 report — "the terminal text isn't so crisp, which might be related to retina displays" — measured. The reporting canvas was at 0.976, and the rule jumped to trilinear mip filtering the instant zoom fell below 1… A canvas is very rarely at exactly 1 — any pinch, fitView or window resize lands on a number like that — so the near-1 band is w

- [46] (verified) CONTRAST — ccanvas implements the same supersample trick as cate but tracks zoom EXACTLY rather than snapping to steps, and its comment is a direct indictment of the stepped approach: 'The old code bucketed k to the nearest 0.5 (and capped it at 3), so k≠zoom almost always and the selection highlight drifted a line or two below the cursor on zoom-in. Track zoom exactly; floor at 1.' Implementation: `term.options.fontSize = BASE_FONT * k` with the host at `width: ${k*100}%`, `height: ${k*100}%`, `transform: scale(${1/k})`, 150 ms debounce, `Math.abs(nk - kRef.current) < 0.01` dead band.
    SRC: ccanvas:src/widgets/TerminalBody.tsx:55-62, 607-650, 698-711
    NOTE: cate reaches the same correctness by the opposite route: it KEEPS the steps and cancels the residual mismatch in the pointer handler (`effective = zoomLevel / cellScale.w`). ccanvas pays for exactness with an atlas rebuild on every settled zoom; cate pays for steps with a coordinate rewrite. These are the two real options.

- [47] (verified) CONTRAST — ccanvas DOES issue a real PTY resize on every zoom-driven refit: `fitRef.current?.fit(); if (modeRef.current === 'pty') transportRef.current?.resize(term.cols, term.rows)`, in the k-change effect, the ResizeObserver, and the visibility effect. cate deliberately avoids exactly this at the render-scale change point.
    SRC: ccanvas:src/widgets/TerminalBody.tsx:637-650, 303-317, 655-669
    NOTE: ccanvas zoom clamp is 0.05–8 (`const z = clamp(nextZoom, 0.05, 8)`, ccanvas:src/lib/geometry.ts:19) and it uses no WebGL addon at all — package.json lists only @xterm/xterm and @xterm/addon-fit, so it runs on xterm's DOM renderer.

- [48] (verified) CONTRAST — canvas zoom ranges across the five apps: cate 0.3–3.0 (custom canvas); opencove 0.1–2 (@xyflow/react); termcanvas 0.1–2 (@xyflow/react, own step ladder); ccanvas 0.05–8 (hand-rolled); nodeterm floor 0.01 (hand-rolled GL camera, React Flow convention). Only cate refuses to go below 0.3.
    SRC: cate:src/shared/types.ts:1694-1695; opencove:src/contexts/workspace/presentation/renderer/components/workspaceCanvas/constants.ts:9-10; termcanvas:src/canvas/viewportZoom.ts:8-9 and XyFlowCanvas.tsx:750-751; ccanvas:src/lib/geometry.ts:19; nodeterm:src/rendere
    NOTE: cate's 0.3 floor is consistent with having no LOD: at 0.3 a 13px font is ~4px on screen and still rendered live from a full atlas. The apps that go to 0.1 either have a behavioral overview mode (termcanvas) or mip-filter the atlas (nodeterm).

- [49] (verified) CONTRAST — opencove disabled a MutationObserver-based WebGL canvas transform cleanup for a perf reason worth knowing: the whole exported function body is `// Disabled: MutationObserver + double-rAF causes drag lag when DevTools is open. // Transform cleanup is driven by explicit terminal layout sync points instead. return () => undefined`.
    SRC: opencove:src/contexts/workspace/presentation/renderer/components/terminalNode/registerWebglCanvasTransformCleanupMutationObserver.ts:1-9
    NOTE: Relevant because cate's equivalent work (repaint, rescale) is also double-rAF-driven (TerminalPanel.tsx:599-604, 740-753) — same pattern, no reported lag, but the two repos disagree about whether it's safe.

  OPEN:
   - Does cate's pinch/ctrl-wheel zoom path leave xterm interactive mid-gesture in a way users notice? The pinch path acquires `canvas-zooming` (useCanvasInteraction.ts:242), which has NO CSS rule anywhere in the repo, so the `.canvas-interacting .xterm { pointer-e
   - Does crossing a render-scale step actually produce a fit() (and therefore a possible terminal.resize()) at gesture end? The render box width is `${100 * cellScale.w}%` (TerminalPanel.tsx:960), so a step change moves clientWidth by ~2-5% — far past RESIZE_EPSIL
   - Are cate's WebGL grants leaked by culling? A culled node unmounts TerminalPanel, which calls detach() (terminalDom.ts:542-558) — DOM removal only. releaseWebglGrant is called on context loss, resetRendering(), and dispose() but not on detach. So an off-screen 
   - No published or in-code numbers exist for many terminals at HIGH zoom. Every perf-stress scenario runs at setZoom(0.5)/(0.55) with ~9 nodes, and PR #173 says 'many-terminals / big-canvas measured cheap at ~9 nodes, left as-is.' The zoom stress test asserts onl
   - docs/perf-audit.md, cited in PR #173 as holding 'the full audit + before/after', is not present in the repo at main (docs/ contains only agent-integrations.md). The before/after table beyond the PR body's summary is not recoverable from the repo.
   - Whether cate covers xterm's auto-scroll threshold path under scale. termcanvas explicitly patches `_selectionService._getMouseEventScrollAmount` and calls it 'the easy path to miss' — cate's DOM-event rewrite intercepts mousedown/mousemove/mouseup on the conta
