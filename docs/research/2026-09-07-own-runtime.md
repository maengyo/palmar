# 우리 런타임 조사 원문 (2026-09-07)

에이전트 5개가 각자 다른 각도로 조사한 결과를 **그대로** 둔 것이다. 각 항목에 (verified|likely|unverified) 등급과 출처가 붙어 있다.
verified 는 조사 에이전트가 문서·소스·로컬 실행으로 확인한 것이고, palmer 저자가 재확인한 것은 아니다. 요약과 판단은 `docs/own-runtime.md` 에 있다.
각도: pty-options(언어별 PTY) · agent-hooks(CLI 훅/이벤트) · cate-baseline(비교 대상·경쟁 제품) · web-terminal-prior-art(xterm.js·브라우저 터미널 선례) · persistence(세션 유지·복원).


######## pty-options: 27 findings ########
- [1] (verified) Python 3.13 stdlib has everything needed to own a PTY: os.openpty, os.forkpty, os.posix_openpt (3.13+), os.login_tty (3.11+), the pty module, termios.TIOCSWINSZ/TIOCGWINSZ constants, termios.tcgetwinsize/tcsetwinsize (3.11+), fcntl.ioctl, select, asyncio.
     src: Local: `python3 -c` on Python 3.13.9 printed has os.openpty/forkpty/posix_openpt/login_tty: True; termios.TIOCSWINSZ=0x80087467; tcgetwinsize/tcsetwinsize: True. Docs: https://docs.python.org/3.13/lib
     note: Zero third-party packages for the PTY layer.
- [2] (verified) Resize works from Python stdlib on macOS: fcntl.ioctl(master, termios.TIOCSWINSZ, struct.pack('HHHH', rows, cols, 0, 0)) changed the child's `stty size` from '24 80' to '50 200' and termios.tcgetwinsize(master) returned (50, 200).
     src: Local run (openpty + fork + TIOCSCTTY + execvp sh): child output b'24 80\r\n50 200\r\ndone\r\n', exit status 0. Script in <스크래치패드>
     note: termios.tcsetwinsize((rows, cols)) is the portable 3.11+ wrapper for the same ioctl.
- [3] (verified) asyncio integration with a PTY master works on macOS with the default KqueueSelector: loop.add_reader(master, cb) fired for output and for EOF (os.read returned b'').
     src: Local run: 'default selector: KqueueSelector … asyncio add_reader on pty master works: (b\'hello-from-pty\\r\\nbye\\r\\n\', "EOF (read returned b\'\')")'.
     note: Linux (epoll) not tested here — no docker/Linux box available; widely used (tmux, pexpect) so 'likely' fine, but see EIO gotcha.
- [4] (verified) Linux vs macOS PTY EOF gotcha: on Linux, read() on the master after the child exits raises OSError EIO (errno 5); on macOS/FreeBSD it returns b''. Code must treat both as EOF.
     src: CPython PR #12049 / bpo-26228 (merged 2021-08-11, fix in 3.10+): 'On non-Linux POSIX platforms, like FreeBSD or macOS, the FD used to read a forked PTY may signal its exit not by raising an error but 
     note: Source verified; Linux behavior not reproduced locally (macOS only).
- [5] (verified) pty.fork()/os.forkpty() emit DeprecationWarning when the process has more than one thread (3.12+), and the docs warn it is unsafe on macOS when mixed with higher-level system APIs (including urllib.request).
     src: Local: with one extra thread alive, pty.fork() produced 'This process (pid=18821) is multi-threaded, use of forkpty() may lead to deadlocks in the child.' Docs text: Doc/library/os.rst lines 4671-4683
     note: Harmless in a single-threaded asyncio server; becomes a real constraint if any thread (e.g. a ThreadingHTTPServer) exists when spawning.
- [6] (verified) The fork-free alternative (os.openpty + subprocess.Popen(stdin/stdout/stderr=slave, start_new_session=True)) does NOT give the child a controlling tty, so TIOCSWINSZ on the master delivers no SIGWINCH to the child. Adding preexec_fn=lambda: fcntl.ioctl(0, termios.TIOCSCTTY, 0) restores SIGWINCH, but preexec_fn is documented 'NOT SAFE to use in the presence of threads'.
     src: Local run: (a) pty.fork → b'ready<RESIZED>GOT-WINCH end'; (b) Popen setsid only → b'ready<RESIZED>end' (no GOT-WINCH); (c) Popen setsid+TIOCSCTTY via preexec_fn → GOT-WINCH. Docs: Doc/library/subproce
     note: Agents like Claude Code (Node/ink) re-layout on SIGWINCH, so a controlling tty is required, not optional. Python offers no thread-safe stdlib path to TIOCSCTTY except being single-threaded (pty.fork or preexec_fn).
- [7] (verified) Python stdlib has no WebSocket module, but a stdlib-only RFC 6455 server (socket + hashlib.sha1 + base64 handshake, struct framing) is feasible and was verified end-to-end; if a library is preferred, `websockets` 17.1 ships a pure-Python py3-none-any wheel (no compile), requires Python >=3.11.
     src: `curl https://docs.python.org/3.13/py-modindex.html | grep -i websocket` → empty. Local ws_stdlib.py (imports only socket, threading, hashlib, base64, struct, sys) handshook and echoed a frame to the 
     note: http.server is synchronous; a stdlib-only design would either hand-roll WS over asyncio streams or run http.server in a thread (which then triggers the forkpty thread warning).
- [8] (verified) Install-footprint gotcha for a Python backend: macOS does not ship a usable Python 3 by default; /usr/bin/python3 is an Apple stub that offers to install Xcode Command Line Tools, and CLT's Python is 3.9.6 — which lacks termios.tcsetwinsize, os.login_tty and os.posix_openpt. 'Python 3.13 stdlib' therefore means a user-installed Python.
     src: Local: `/usr/bin/python3 --version` → Python 3.9.6, resolves to /Library/Developer/CommandLineTools/usr/bin/python3; hasattr checks → tcsetwinsize False, login_tty False, posix_openpt False. https://m
     note: fcntl.ioctl + struct.pack still work on 3.9, so a 3.9-compatible codebase is possible if you avoid the 3.11+ helpers.
- [9] (verified) One-line install for a Python backend without assuming Python exists: uv's standalone installer `curl -LsSf https://astral.sh/uv/install.sh | sh`, then `uvx <tool>` runs the tool in a temporary isolated env; uv auto-downloads a Python interpreter by default (python-downloads = automatic).
     src: https://docs.astral.sh/uv/getting-started/installation/ (installer commands), https://docs.astral.sh/uv/guides/tools/ ('Tools are installed into temporary, isolated environments when using uvx'), http
     note: Strictly two commands unless chained with &&; uv is itself an external binary the user must trust. No account/service involved.
- [10] (verified) Node 26.0.0 has no built-in PTY: child_process, os and tty expose nothing named pty/openpty (tty exports only isatty, ReadStream, WriteStream). A core feature request exists but has no traction: nodejs/node#64019 'Feature request: PTY support in child_process.spawn()' (opened 2026-06-20, open, 0 comments, label 'feature request'); no open PRs mention pty.
     src: Local `node -e` on v26.0.0: 'child_process keys w/ pty/tty/term: []', 'has node:tty openpty? [isatty, ReadStream, WriteStream]'. `gh api repos/nodejs/node/issues/64019` → state open, comments 0. `gh s
     note: Any Node backend needs node-pty (native addon) or another runtime.
- [11] (verified) node-pty is a native N-API addon (dependency node-addon-api). Install script is `node scripts/prebuild.js || node-gyp rebuild`: it uses the shipped prebuilt only if prebuilds/<platform>-<arch> exists, otherwise compiles with node-gyp, which requires Python, make and a C/C++ compiler (Xcode CLT on macOS).
     src: `npm view node-pty` → version 1.1.0, scripts.install 'node scripts/prebuild.js || node-gyp rebuild', dependencies {node-addon-api: ^7.1.0}. prebuild.js text: 'Rebuilding because directory ${PREBUILD_D
     note: Violates 'no native dependencies' literally; whether it also violates 'no build toolchain' depends on platform coverage of the prebuilds (next two findings).
- [12] (verified) node-pty 1.1.0 (npm `latest`) ships prebuilds ONLY for darwin-arm64, darwin-x64, win32-arm64, win32-x64 — no Linux. On Linux, `npm i node-pty` compiles from source (needs python/make/g++).
     src: Local `find node_modules/node-pty/prebuilds -type f` after `npm i node-pty` (1.1.0): only darwin-*/pty.node, darwin-*/spawn-helper and win32-* files. npm unpackedSize 64,363,742 bytes.
     note: Linux users of the stable release hit node-gyp today.
- [13] (verified) node-pty 1.1.0's macOS prebuilt is broken out of the box: spawn-helper is packaged without the execute bit, so pty.spawn() throws 'Error: posix_spawnp failed.' Reproduced with npm 11.12.1 + Node 26.0.0 on macOS arm64; `chmod +x prebuilds/darwin-arm64/spawn-helper` fixes it. Fix exists only in 1.2.0-beta.2+; issue #919 asking for a 1.1.x patch is still open.
     src: Local: `ls -la` → '-rw-r--r-- … spawn-helper'; `node -e` spawn → 'Error: posix_spawnp failed.'; after chmod → 'exit 0 output: "50 200\r\n50 200\r\ndone\r\n"'. microsoft/node-pty#850 (closed 2026-01-13
     note: Issue text says npm/yarn 'may not exhibit the issue' but it reproduced with npm 11.12.1 here.
- [14] (verified) node-pty 1.2.0-beta.15 (2026-08-03) ships prebuilds for darwin-arm64/x64, linux-x64/arm64, win32-x64/arm64 with a correctly executable spawn-helper; it installed in ~0.4 s with no compilation and spawn+resize worked on Node 26.0.0. The Linux prebuilt is glibc-only (needs GLIBC_2.28, GLIBCXX_3.4.22; musl/Alpine falls back to node-gyp).
     src: Local: `tar tzf node-pty-1.2.0-beta.15.tgz | grep prebuilds` lists linux-x64/pty.node and linux-arm64/pty.node; `ls -la` → '-rwxr-xr-x … spawn-helper'; test → 'node-pty beta on node v26.0.0 exit 0 out
     note: Pinning a beta is the only way to get toolchain-free installs on Linux+macOS with node-pty today. Package unpackedSize 26.9 MB.
- [15] (verified) Node single-executable applications (SEA) can bundle .node addons as assets and load them via process.dlopen, so a Node+node-pty single binary is possible, but SEA is Stability 1.1 (Active development) and has a known Linux arm64 Docker-build crash caveat.
     src: https://nodejs.org/api/single-executable-applications.html: 'Native addons can be bundled as assets … loading it with process.dlopen()'; 'Stability: 1.1 - Active development'.
     note: Not tested locally.
- [16] (verified) Bun has a built-in PTY API: `Bun.Terminal` and `Bun.spawn(cmd, { terminal: { cols, rows, name, data(term, bytes), exit(), drain() } })`; Terminal has write(), resize(cols, rows), setRawMode(), close(), ref()/unref(), closed, and termios flag properties. Introduced in Bun v1.3.5 (2025-12-17, POSIX only, implemented with openpty(), no external deps); Windows ConPTY support added in v1.3.14 (2026-05-13).
     src: Local `bun run` on 1.3.14: 'typeof Bun.Terminal: function'; prototype methods [close, closed, controlFlags, inputFlags, localFlags, outputFlags, ref, resize, setRawMode, unref, write]. https://bun.com
     note: The only mainstream JS/TS runtime with a first-party PTY. Requires Bun >= 1.3.5 (>= 1.3.14 for Windows).
- [17] (verified) Runnable check on Bun 1.3.14 / macOS arm64: Bun.spawn with terminal {cols:80, rows:24} → child `stty size` printed '24 80'; after proc.terminal.resize(200, 50) → '50 200'; `tty` → /dev/ttys012; proc.stdin/stdout are null (all I/O via proc.terminal). The same script compiled with `bun build --compile` into a 63 MB single binary ran identically.
     src: Local: <스크래치패드>/bunpty.ts → output "24 80\r\n50 200\r\n/dev/ttys012\r\nisatty-done\r\n", exit 0; `bun bui
     note: Resize works; no native module, no build step.
- [18] (verified) With the INLINE `terminal: {...}` form, the Bun child is a proper session leader with the PTY as controlling tty: sid == pgrp == pid, tcgetpgrp(0) == own pgrp, SIGWINCH is delivered on resize(), writing '\x03' delivers SIGINT (exit 130, signalCode SIGINT, ~1 ms) and the `exit` callback fires on macOS. Verified both with and without the parent bun process having a controlling tty.
     src: Local bunctty.ts on Bun 1.3.14: 'GOT-WINCH', 'stdin-tty /dev/ttys012', 'pgrp 35636 sid 35636 fg-pgrp-of-stdin 35636', 'exitCallbackFired true', 'TEST2 ^C: result 130 signalCode SIGINT after 1 ms'. Rep
     note: This is the path to use.
- [19] (verified) BUG (reproduced): a pre-created `new Bun.Terminal({...})` passed as `terminal: term` to Bun.spawn does NOT setsid/TIOCSCTTY the child; '\x03' is only echoed and no SIGINT is delivered. Fix PR oven-sh/bun#33240 is still open/unmerged as of 2026-09-07.
     src: Local bunpre.ts on Bun 1.3.14: 'pre-created Bun.Terminal ^C -> TIMEOUT signalCode null out "start\r\n^C"'. https://github.com/oven-sh/bun/issues/33237 (open; robobun: 'The inline terminal: { ... } for
     note: Avoid reusing a Terminal object across spawns until #33240 lands; one PTY per agent window via inline options sidesteps it.
- [20] (likely) Other open Bun.Terminal reports to watch: #40289 — on Linux (1.3.14) the `exit` callback never fires spontaneously after a voluntary child exit (fires on macOS; host close() fires it on both); #25912 — PTY allocation fails after ~17 h or sleep/wake on macOS (1.3.5); #41410 — no pause()/resume()/fd, so no read backpressure (1.4.0).
     src: `gh api repos/oven-sh/bun/issues/40289|25912|41410` (all state open as of 2026-09-07) and `gh search issues --repo oven-sh/bun "Bun.Terminal" --state open`.
     note: Sourced from issue text; none reproduced here (no Linux box; no long-running test). #40289 matters for a 'done' status light — use `await proc.exited` rather than the exit callback.
- [21] (verified) Bun install footprint: 'Bun ships as a single, dependency-free executable'; one-liner `curl -fsSL https://bun.com/install | bash` (Windows: `powershell -c "irm bun.sh/install.ps1|iex"`); needs macOS >= 13.0, Linux glibc >= 2.17 (musl build available). `bunx <pkg>` auto-installs and runs an npm package. Current Bun is 1.4.2 (2026-09-05), i.e. past the 1.3.x line named in the question.
     src: https://bun.com/docs/installation, https://bun.com/docs/cli/bunx ('Use bunx to auto-install and run packages from npm'), `gh release list --repo oven-sh/bun` → 'Bun v1.4.2 Latest 2026-09-05'.
     note: So a Bun backend's 'one line' is `curl … | bash && bunx palmer` (or a `bun build --compile` binary per platform). Bun itself becomes the one external dependency; it is not a native addon or toolchain.
- [22] (verified) Deno has no built-in PTY. Deno.CommandOptions (main branch) has only args, cwd, clearEnv, env, uid, gid, signal, stdin, stdout, stderr, windowsRawArguments, detached. Issue denoland/deno#3994 'Support a pty interface' has been open since 2020-02-14 (9 comments, last activity 2026-04-01) with a 2023 API sketch from a core member and no linked PR.
     src: `curl raw lib.deno.ns.d.ts | awk '/interface CommandOptions/…'` → the property list above. `gh api repos/denoland/deno/issues/3994` → state open, created 2020-02-14, updated 2026-04-01; kt3k comment 2
     note: Deno is not installed on this machine; no runtime test.
- [23] (verified) The only Deno route is FFI: jsr @sigma/pty-ffi (0.42.0) wraps Rust portable-pty; at runtime it uses @denosaurs/plug to download `libpty_<arch>.so/.dylib/pty.dll` from GitHub releases and Deno.dlopen()s it, requiring --allow-ffi ('native code runs outside of Deno's security sandbox') plus network access. That is a native dependency and an external download.
     src: deno.json imports '@denosaurs/plug'; src/ffi.ts lines 110-156: `Deno.dlopen(libPath, SYMBOLS)`, `https://github.com/sigmaSd/deno-pty-ffi/releases/download/${metadata.version}`, `libpty_${Deno.build.ar
     note: Violates 'no native deps' and 'no external download'; rules Deno out.
- [24] (verified) Go: github.com/creack/pty v1.1.24 (2024-10-31) is pure Go (no cgo at build time): `import "C"` appears only in `//go:build ignore` generator files (types.go, types_*bsd.go); pty_darwin.go opens /dev/ptmx via syscall. API: pty.Start(cmd)/StartWithSize, pty.Setsize(f, &pty.Winsize{Rows, Cols, X, Y}) → ioctl TIOCSWINSZ, Getsize. go.mod: 'go 1.18', no dependencies; the repo cross-compiles with plain `GOOS=$os GOARCH=$arch go build`.
     src: `gh api search/code?q=repo:creack/pty+"import \"C\""` → types.go, types_netbsd.go, types_freebsd.go, types_openbsd.go, types_dragonfly.go; types.go header '//go:build ignore'. pty_darwin.go imports er
     note: Go is not installed on this machine (`go` not found), so no local run. Windows support in creack/pty is stubbed (pty_unsupported.go) — detail unverified.
- [25] (verified) Rust: portable-pty 0.9.0 (crates.io, updated 2025-02-11, 14.1M downloads, from wezterm) provides native_pty_system().openpty(PtySize), MasterPty::resize(PtySize), try_clone_reader()/take_writer(), CommandBuilder; Unix + Windows ConPTY. Its dependencies are all Rust crates (libc, nix 0.28, filedescriptor, anyhow, downcast-rs, log, serial2, shell-words; winapi/winreg/bitflags/lazy_static/shared_library on Windows; optional serde) — no C compilation step.
     src: `curl https://crates.io/api/v1/crates/portable-pty` and `/0.9.0/dependencies` (listed above); https://docs.rs/portable-pty/latest/portable_pty/.
     note: cargo 1.97.1 is installed locally but no build was attempted.
- [26] (verified) For Go/Rust, 'install one line' means shipping prebuilt per-platform static binaries: cargo-dist generates `curl --proto '=https' --tlsv1.2 -LsSf https://github.com/<org>/<repo>/releases/download/<tag>/<name>-installer.sh | sh` (needs only curl/wget + tar/unzip); GoReleaser publishes GitHub Releases and a Homebrew cask/tap for `brew install`. The end user needs no toolchain; only the developer/CI needs Go or Rust.
     src: https://axodotdev.github.io/cargo-dist/book/installers/shell.html ('detects the current platform, fetches the best possible archive from your Artifact URL, copies the binary into your install-path'); 
     note: Satisfies no-native-deps/no-toolchain for the USER; the xterm.js frontend must be embedded in the binary (Go embed / Rust include_bytes) — inference, not verified.
- [27] (verified) Summary matrix of 'violates no native deps / no toolchain?': Python stdlib — no violation for PTY (needs user-installed Python >= 3.11 for the nice helpers; WS is hand-rolled or pure-Python `websockets`). Node — node-pty is a native addon; toolchain-free only where a prebuilt exists (1.1.0: macOS/Windows only and broken on macOS; 1.2.0-beta: adds glibc Linux). Bun — no native addon, no toolchain, but Bun itself is the dependency. Deno — FFI-only, violates. Go/Rust — clean for the user, adds a compiled language + re
     src: Aggregation of the findings above (each individually sourced).
     note: Bun is the only runtime where PTY + resize + WebSocket server (Bun.serve websocket) are all first-party; Python is the only one where everything is stdlib but the runtime is not guaranteed present on macOS.
  OPEN: Bun on Linux: does the Bun.Terminal `exit` callback really never fire after a voluntary child exit (oven-sh/bun#40289, 1.3.14)? No Linux machine here to reprodu | Does the pre-created-Terminal controlling-tty bug (#33237) persist in Bun 1.4.2? PR #33240 is unmerged as of 2026-09-07 so it likely does; only Bun 1.3.14 was t | Is depending on the Bun runtime acceptable under 'install must be one line / no external service'? It is one `curl | bash` plus `bunx`, but Bun is a 63 MB third | Python on Linux: asyncio add_reader with a PTY master under epoll was not tested here (only macOS kqueue); the EIO-on-exit path (errno 5) must be handled but wa | Will node-pty 1.2.0 ever be tagged stable (microsoft/node-pty#919 open)? A Node backend would have to pin `node-pty@1.2.0-beta.x` to get Linux prebuilds and a w | Is Windows in scope? Python's pty module is Unix-only; node-pty and Bun (>=1.3.14) and portable-pty support ConPTY; creack/pty does not. | Go/Rust would require the project to adopt a compiled language plus a release pipeline (cargo-dist/GoReleaser) and to embed the xterm.js assets in the binary —  | Which Python version floor is acceptable? Apple's CLT Python is 3.9.6 (no tcsetwinsize/login_tty); requiring 3.11+ means requiring uv/Homebrew/python.org Python

######## agent-hooks: 34 findings ########
- [1] (verified) Claude Code hook events as of Sep 2026 (installed CLI is 2.1.260): SessionStart, Setup, UserPromptSubmit, UserPromptExpansion, PreToolUse, PermissionRequest, PermissionDenied, PostToolUse, PostToolUseFailure, PostToolBatch, Notification, MessageDisplay, SubagentStart, SubagentStop, TaskCreated, TaskCompleted, Stop, StopFailure, TeammateIdle, InstructionsLoaded, ConfigChange, CwdChanged, DirectoryAdded, FileChanged, WorktreeCreate, WorktreeRemove, PreCompact, PostCompact, PreModelSwitch, PostModelSwitch, Elicitation
     src: https://code.claude.com/docs/en/hooks (lifecycle table); `claude --version` -> 2.1.260
     note: For a status light the relevant ones are UserPromptSubmit (start working), PreToolUse/PostToolUse (still working), PermissionRequest (blocked on approval), Stop (done), StopFailure (error), Notification idle_prompt/permi
- [2] (verified) Common hook stdin fields: session_id, prompt_id (v2.1.196+, absent before first prompt), transcript_path (written async, may lag), cwd, permission_mode (default|plan|acceptEdits|auto|dontAsk|bypassPermissions), effort.level, hook_event_name, agent_id/agent_type (subagents only). Tool events add tool_name, tool_input, tool_use_id.
     src: https://code.claude.com/docs/en/hooks#common-input-fields; live payloads captured in <스크래치패드>/hook-hit.js
     note: Live SessionStart payload: {session_id, transcript_path, cwd, hook_event_name:"SessionStart", source:"startup"}. Live Stop payload: {session_id, transcript_path, cwd, prompt_id, permission_mode:"default", effort:{level:"
- [3] (verified) Exit-code semantics: 0 = success, stdout parsed as JSON if it starts with { and ends with } (else plain text, only added to context on UserPromptSubmit/UserPromptExpansion/SessionStart/PostModelSwitch); 2 = blocking error on events that support blocking (stderr is the reason), and exit 2 blocks even if JSON says allow; other non-zero = non-blocking, JSON alone decides. PermissionRequest, Notification, StopFailure, PostToolUse, Elicitation do NOT honor exit 2. Default timeouts: 600 s command/http/mcp_tool, 30 s prom
     src: https://code.claude.com/docs/en/hooks (Exit code behavior + per-event exit-2 table)
     note: JSON stdout schema: {continue, stopReason, systemMessage, terminalSequence, hookSpecificOutput:{hookEventName, permissionDecision allow|deny|ask|defer, permissionDecisionReason, updatedInput, additionalContext, decision:
- [4] (verified) Five hook handler types exist: command (shell or exec form when `args` present), http (POST the event JSON to a URL, response body uses the same JSON output format; headers with $VAR interpolation limited by allowedEnvVars and httpHookAllowedEnvVars), mcp_tool, prompt, agent. Command hooks support async:true and asyncRewake.
     src: https://code.claude.com/docs/en/hooks#http-hook-fields; https://code.claude.com/docs/en/settings-reference#hooks (type list)
     note: An `http` hook lets palmer's backend receive events directly at http://127.0.0.1:PORT/... with no helper script or jq dependency, matching the no-native-deps rule.
- [5] (verified) Hook config locations: ~/.claude/settings.json (user), .claude/settings.json (project), .claude/settings.local.json (local, gitignored), managed settings, plugin hooks/hooks.json, skill and subagent frontmatter. Hooks `Scope: Any file`, and hooks MERGE across files rather than replace. Precedence highest->lowest: managed > `--settings` (command line) > project local > project > user.
     src: https://code.claude.com/docs/en/settings#settings-precedence; https://code.claude.com/docs/en/settings-reference#hooks
     note: Settings doc: "--settings: pass a key as JSON, inline or as a path to a file. Claude Code applies it above your user, project, and local files and below managed settings. It can set any key your user settings file can se
- [6] (verified) A third party CAN enable hooks for one launched Claude Code instance without touching the user's global settings: `claude --settings '{"hooks":{...}}'` (inline JSON or file path). Verified live: `claude -p --setting-sources "" --settings '{"hooks":{"SessionStart":[...],"Stop":[...]}}' --max-turns 1 "Reply with exactly: OK"` fired both hooks and wrote their stdin payloads to marker files; no settings file was modified.
     src: Command run 2026-09-07 in scratchpad; outputs at .../scratchpad/hook-hit.json and .../scratchpad/hook-stop.json; `claude --help` shows `--settings <file-or-json>` and `--setting-sources <sources>` (us
     note: Cost of the test: $0.04 (one Sonnet call). `--setting-sources` selects which of user/project/local files load; passing "" excluded all of them so only --settings hooks ran. For an interactive PTY palmer would normally om
- [7] (verified) CLAUDE_CONFIG_DIR overrides the config directory (default ~/.claude): "All settings, session history, and plugins are stored under this path... Set it in your shell, user settings, or managed settings. Ignored in project and local settings." CLAUDE_CODE_PROJECT_DIR_NAME (v2.1.234+) only works together with it.
     src: https://code.claude.com/docs/en/env-vars (rows CLAUDE_CONFIG_DIR, CLAUDE_CODE_PROJECT_DIR_NAME); https://code.claude.com/docs/en/settings ("To keep the home-directory files somewhere else, set CLAUDE_
     note: Using an isolated CLAUDE_CONFIG_DIR to inject hooks would also hide the user's settings, sessions, plugins and (per the doc's pointer to credential storage) possibly credentials, so `--settings` is the better per-instanc
- [8] (verified) The event that fires the moment Claude waits for a permission approval is PermissionRequest: "Runs when Claude Code is about to ask you for permission to use a tool." Matcher = tool name. Payload: tool_name, tool_input, permission_suggestions[] (no tool_use_id). Hook may answer via hookSpecificOutput.decision {behavior: allow|deny, updatedInput, updatedPermissions, message, interrupt}; exit 2 is ignored. It does NOT fire for a sandboxed command's network-request prompt (use Notification permission_prompt for that) 
     src: https://code.claude.com/docs/en/hooks#permissionrequest
     note: Doc: "Use this event when you need a signal the moment Claude asks for permission... Claude Code runs a Notification hook with the permission_prompt type only after the prompt has waited about six seconds." In sessions t
- [9] (verified) Notification hook types and timing: permission_prompt (permission prompt has waited ~6 s with no typing; each keystroke defers), idle_prompt (~60 s after Claude finished responding and no typing since), auth_success, elicitation_dialog, elicitation_url_dialog, elicitation_complete, elicitation_response, agent_needs_input (background session waiting on input while agent view is open, or teammate setup question; v2.1.198+), agent_completed (background session finished; only while agent view open), quota_auto_resume_f
     src: https://code.claude.com/docs/en/hooks#notification (matcher table + note on timing)
     note: Because permission_prompt and idle_prompt are delayed presence heuristics, they are a poor primary source for a status light; PermissionRequest + Stop are immediate.
- [10] (verified) There is no dedicated AskUserQuestion hook event. AskUserQuestion is a built-in tool, so PreToolUse (and PostToolUse) match on tool_name "AskUserQuestion" ("Matches on any tool name except EndConversation: built-in tools such as Bash, PowerShell, Edit, Write, Read, Glob, Grep, Agent, Workflow, WebFetch, WebSearch, AskUserQuestion, and ExitPlanMode"). tool_input = {questions:[{question, header, options:[{label}], multiSelect}]}; a hook can answer programmatically by returning permissionDecision:"allow" + updatedInpu
     src: https://code.claude.com/docs/en/hooks#pretooluse (tool list) and #askuserquestion / 'Defer a tool call for later'
     note: For a PTY status light: 'waiting on a question' = PreToolUse(AskUserQuestion) seen and no PostToolUse(AskUserQuestion) yet (inference, not stated in docs). Whether PermissionRequest also fires for AskUserQuestion/ExitPla
- [11] (verified) Stop fires once per turn "when Claude finishes responding" with stop_hook_active, last_assistant_message, background_tasks, session_crons; exit 2 / decision block keeps Claude going (capped at 8 consecutive blocks). SubagentStop adds agent_id, agent_type, agent_transcript_path. StopFailure fires when the turn ends due to an API error, matcher on error type (rate_limit, overloaded, authentication_failed, oauth_org_not_allowed...). SessionEnd matcher: clear, resume, logout, prompt_input_exit, other. SessionStart matc
     src: https://code.claude.com/docs/en/hooks#stop, #stopfailure, #sessionend; live Stop payload in scratchpad/hook-stop.json
     note: Stop = 'done' light; StopFailure = 'error' light; UserPromptSubmit = 'working' light start.
- [12] (verified) Hooks can emit terminal escape sequences through Claude Code's own terminal write path via the `terminalSequence` output field; allowlist is OSC 0/1/2 (titles), OSC 9 (iTerm2/ConEmu/Windows Terminal/WezTerm notifications incl. 9;4 progress), OSC 99 (Kitty), OSC 777 (urxvt/Ghostty/Warp), bare BEL. Written only in interactive sessions while the UI is on screen (ignored in -p/SDK).
     src: https://code.claude.com/docs/en/hooks#emit-terminal-notifications
     note: Means a palmer-injected hook could make Claude Code itself write a custom OSC (e.g. a private OSC 9;4 state or a title) into the PTY that palmer's xterm.js parser picks up, a fallback if HTTP hooks are undesired.
- [13] (verified) Claude Code natively emits OSC 9;4 taskbar/tab progress while working: setting terminalProgressBarEnabled (default true): "While Claude is working, Claude Code reports an in-progress state to the terminal and clears it when the turn ends... It does so only in terminals that support the indicator: ConEmu, Ghostty 1.2.0 or later, and iTerm2 3.6.6 or later." Issue #57366 (closed 2026-07-03) confirms the raw sequence is OSC 9;4 and was not tmux-wrapped.
     src: https://code.claude.com/docs/en/settings-reference#terminalprogressbarenabled; https://github.com/anthropics/claude-code/issues/57366
     note: Because emission is gated on terminal detection, inside a palmer PTY it will only appear if Claude Code believes the terminal is one of those (which env vars it checks, e.g. TERM_PROGRAM, is undocumented). Open question 
- [14] (verified) Claude Code continuously sets the terminal title via OSC 0/2 with a conversation-derived title (`repo-name · topic-summary · session-id` per user report), replaced by the session name after /rename or `--name` (terminalTitleFromRename, default true). CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1 disables automatic title updates. No documented encoding of working/waiting state in the title.
     src: https://code.claude.com/docs/en/settings-reference#terminaltitlefromrename; https://code.claude.com/docs/en/env-vars (CLAUDE_CODE_DISABLE_TERMINAL_TITLE); https://github.com/anthropics/claude-code/iss
     note: Title is usable for naming a window on the canvas, not for status.
- [15] (verified) Claude Code desktop/bell notifications: preferredNotifChannel auto|iterm2|iterm2_with_bell|terminal_bell|notifications_disabled. "With auto, Claude Code sends a desktop notification in iTerm2, Ghostty, and Kitty. In Terminal.app it rings the bell only when you have turned Terminal's audible bell off, and in other terminals it does nothing." terminal_bell rings BEL in any terminal. Fired "when Claude finishes a task or pauses for a permission prompt, and you appear to be away from the terminal" (same presence timing
     src: https://code.claude.com/docs/en/settings-reference#preferrednotifchannel; https://code.claude.com/docs/en/terminal-config#get-a-terminal-bell-or-notification
     note: A palmer PTY could set preferredNotifChannel=terminal_bell via --settings and treat BEL as 'needs attention', but it is delayed by the ~6 s / 60 s idle heuristics, so hooks are better.
- [16] (verified) Env vars set in hook subprocesses: CLAUDECODE=1 and CLAUDE_CODE_CHILD_SESSION=1 (v2.1.172+); CLAUDE_PROJECT_DIR / CLAUDE_PLUGIN_ROOT placeholders in hook commands; CLAUDE_EFFORT. `--include-hook-events` adds hook lifecycle events to the output stream but only with --print --output-format=stream-json (not applicable to an interactive PTY).
     src: https://code.claude.com/docs/en/env-vars; `claude --help` (--include-hook-events)
- [17] (verified) Codex CLI (installed 0.147.0) has a full lifecycle hooks system, stable and on by default (`codex features list` -> `hooks  stable  true`; `plugin_hooks removed`). Events: SessionStart, SessionEnd, PreToolUse, PermissionRequest, PostToolUse, PreCompact, PostCompact, UserPromptSubmit, SubagentStart, SubagentStop, Stop, Interrupt. No Notification event.
     src: https://developers.openai.com/codex/hooks (308 -> https://learn.chatgpt.com/docs/hooks); `codex features list`; https://learn.chatgpt.com/docs/config-file/config-reference (features.hooks: "Enable lif
     note: Stop: "When a turn ends and awaits continuation decision" with turn_id, stop_hook_active, last_assistant_message. SessionEnd also runs after a thread has been idle and not open in any client for 30 minutes. Interrupt has
- [18] (verified) Codex hook file locations and shape: ~/.codex/hooks.json, ~/.codex/config.toml [hooks] tables, <repo>/.codex/hooks.json, <repo>/.codex/config.toml (project layer only when trusted), plugin hooks/hooks.json. JSON is the Claude Code shape: {"hooks":{"EventName":[{"matcher":..., "hooks":[{"type":"command","command":...,"timeout":600,"statusMessage":...,"async":false}]}]}}. Common stdin fields: session_id (thr_*), transcript_path, cwd, hook_event_name, model; turn_id on turn-scoped events; tool events add tool_name (ca
     src: https://learn.chatgpt.com/docs/hooks (saved text in scratchpad/codex-hooks.html)
     note: Output: hookSpecificOutput.permissionDecision allow|deny (+updatedInput on PreToolUse), additionalContext, legacy {decision:"block",reason}, exit code 2 with stderr blocks. `permissionDecision: "ask"`, `continue:false`, 
- [19] (verified) Codex PermissionRequest hook: "runs when Codex is about to ask for approval, such as a shell escalation or managed-network approval. It can allow the request, deny the request, or decline to decide and let the normal approval prompt continue. It doesn't run for commands that don't need approval." Output decision {behavior: allow|deny, message}; any deny wins; updatedInput/updatedPermissions/interrupt "fail closed today".
     src: https://learn.chatgpt.com/docs/hooks (PermissionRequest section)
     note: This is Codex's 'waiting for approval' signal, replacing the missing approval-requested notify event.
- [20] (verified) Codex hooks require explicit per-hash trust before they run: "Before a non-managed hook can run, Codex requires you to review and trust the exact hook definition. Codex records trust against the hook's current hash, so new or changed hooks are marked for review and skipped until trusted. Use /hooks in the CLI to inspect... If hooks need review at startup, Codex prints a warning." Managed hooks (system, MDM, cloud, requirements.toml) are trusted by policy. `--dangerously-bypass-hook-trust`: "Run enabled hooks withou
     src: https://learn.chatgpt.com/docs/hooks (Review and trust hooks); `codex --help` line 98-100
     note: Key constraint for palmer: an injected Codex hook is skipped until the user trusts it once in /hooks (per hash, so keep the injected definition byte-stable), or palmer launches with the bypass flag. Whether hooks can be 
- [21] (verified) Codex `notify` (config.toml, array<string>): "Command invoked for notifications; receives a JSON payload from Codex." The only event delivered to it is agent-turn-complete; source enum UserNotification has a single variant AgentTurnComplete with kebab-case fields thread-id, turn-id, cwd, client, input-messages, last-assistant-message, appended as the final argv element; triggered from HookEvent::AfterAgent. `notify` is ignored in project-local .codex/config.toml.
     src: https://raw.githubusercontent.com/openai/codex/main/codex-rs/hooks/src/legacy_notify.rs; https://learn.chatgpt.com/docs/config-file/config-reference
     note: Issue openai/codex#11808 asked for approval-requested in notify; closed 2026-02-14 by a maintainer: "This capability will be included in the event hooks feature (#2109)" — i.e. use the PermissionRequest hook. notify stil
- [22] (verified) Codex TUI notifications: `tui.notifications = true | ["agent-turn-complete","approval-requested"]`; `tui.notification_condition = unfocused|always` (default unfocused); `tui.notification_method = auto|osc9|bel` (default auto). Source variants: AgentTurnComplete, ExecApprovalRequested, EditApprovalRequested, ElicitationRequested (type name approval-requested), PlanModePrompt (plan-mode-prompt). Emission is `ESC ] 9 ; <message> BEL` (tmux DCS-wrapped inside tmux) or a bare BEL; auto picks OSC 9 only when the terminal
     src: https://learn.chatgpt.com/docs/config-file/config-reference; codex-rs/tui/src/chatwidget/notifications.rs; codex-rs/tui/src/notifications/mod.rs (supports_osc9); codex-rs/tui/src/notifications/osc9.rs
     note: A hookless Codex signal: launch with `-c 'tui.notifications=["agent-turn-complete","approval-requested"]' -c 'tui.notification_condition="always"' -c 'tui.notification_method="osc9"'` and parse OSC 9 text from the PTY (m
- [23] (verified) Codex TUI encodes run state in the terminal title by default. `tui.terminal_title` defaults to ["spinner","project"]; null disables. Item identifiers (strum kebab-case): app-name, project (project-name), current-dir, spinner (alias activity), status (alias run-state), thread-name, thread (thread-title), git-branch, context-remaining, context-used, five-hour-limit, weekly-limit, codex-version, used-tokens, total-input-tokens, total-output-tokens, thread-credits, estimated-thread-cost, session-id, fast-mode, model, m
     src: https://learn.chatgpt.com/docs/config-file/config-reference (tui.terminal_title); codex-rs/tui/src/bottom_pane/title_setup.rs; codex-rs/tui/src/chatwidget/status_surfaces.rs (TERMINAL_TITLE_ACTION_REQ
     note: Hookless, trust-free Codex status: palmer can launch `codex -c 'tui.terminal_title=["status","spinner","project"]'` and parse OSC 0 titles: 'Working'/'Thinking' -> working, '[ ! ] Action Required' -> waiting for user, 'R
- [24] (likely) Codex does not appear to emit OSC 9;4 progress: a GitHub code search for "9;4" in openai/codex only hits three cursor-style test snapshot files.
     src: `gh api search/code -f q='repo:openai/codex "9;4"'` -> 3 results, all codex-rs/tui/src/custom_terminal/tests/snapshots/*
     note: Code search is token-based and may miss constructed sequences.
- [25] (verified) Codex app-server is a JSON-RPC 2.0 service (`codex app-server`, stdio default; `--listen ws://IP:PORT` marked experimental; `--listen unix://PATH`) emitting thread/started, turn/started, turn/completed, item/started, item/completed, item/agentMessage/delta, thread/status/changed (status notLoaded|idle|systemError|active with activeFlags) and issuing server->client requests item/commandExecution/requestApproval, item/fileChange/requestApproval, tool/requestUserInput ("prompt the user with 1–3 short questions"). The 
     src: https://raw.githubusercontent.com/openai/codex/main/codex-rs/app-server/README.md; `codex --help` (--remote)
     note: Architecture option mirroring OpenCode: palmer runs the app-server, spawns `codex --remote unix://...` in the PTY, and reads status from the JSON-RPC stream (approval requests arrive as server requests palmer merely obse
- [26] (verified) Codex approval/sandbox config: approval_policy = untrusted | on-request | never | {granular = {sandbox_approval, rules, mcp_elicitations, request_permissions, skill_approval}}; sandbox_mode = read-only | workspace-write | danger-full-access; `-a/--ask-for-approval`, `-s/--sandbox`, `--approve-for-me`, `--dangerously-bypass-approvals-and-sandbox`. Config overrides per launch: `-c key=value` (dotted path, TOML value) and `-p/--profile` layering `$CODEX_HOME/<name>.config.toml`.
     src: https://learn.chatgpt.com/docs/config-file/config-reference; `codex --help`, `codex exec --help`
- [27] (verified) OpenCode (installed 1.14.48) is client/server: "When you run opencode it starts a TUI and a server. Where the TUI is the client that talks to the server." `opencode serve --port N --hostname H` runs headless; `opencode attach <url>` (e.g. http://localhost:4096) attaches the TUI to a running server; `GET /event` is an SSE stream whose first event is server.connected; `GET /global/event`; OpenAPI at `GET /doc`; `--pure` runs without external plugins; OPENCODE_SERVER_PASSWORD for basic auth.
     src: https://opencode.ai/docs/server/; `opencode --help`, `opencode attach --help`, `opencode serve --help`; live: `opencode serve --port 4797 --pure` then `curl http://127.0.0.1:4797/event` -> `data: {"id
     note: Cleanest hookless design for OpenCode: palmer runs `opencode serve` on a port it chooses, spawns `opencode attach http://127.0.0.1:PORT` in the PTY, and derives status from /event.
- [28] (verified) OpenCode 1.14.48 bus events (from the live /doc OpenAPI): session.status {sessionID, status:{type:"idle"}|{type:"busy"}|{type:"retry",attempt,message,...}}, session.idle {sessionID}, permission.asked (properties = PermissionRequest {id per_*, sessionID ses_*, permission, patterns[], metadata, always[], tool?{messageID,callID}}), permission.replied {sessionID, requestID, reply: once|always|reject}, question.asked / question.replied / question.rejected, session.error, session.created/updated/deleted/compacted/diff, m
     src: scratchpad/oc-openapi.json dumped from the local server (`opencode serve --port 4797`), parsed with python3; source https://raw.githubusercontent.com/sst/opencode/dev/packages/opencode/src/session/sta
     note: Status light mapping: session.status busy -> working; permission.asked (until permission.replied) or question.asked (until replied/rejected) -> waiting for user; session.idle / status idle -> done; session.error -> error
- [29] (verified) OpenCode plugin API (@opencode-ai/plugin): `Plugin = (input: PluginInput, options?) => Promise<Hooks>`; PluginInput {client, project, directory, worktree, experimental_workspace, serverUrl, $ (Bun shell)}. Hooks: event({event}), config, tool, auth, provider, chat.message, chat.params, chat.headers, "permission.ask"(input: Permission, output: {status: "ask"|"deny"|"allow"}), "command.execute.before", "tool.execute.before"({tool,sessionID,callID},{args}), "tool.execute.after"({tool,sessionID,callID,args},{title,outpu
     src: https://raw.githubusercontent.com/sst/opencode/dev/packages/plugin/src/index.ts (Hooks interface line 224-270); https://opencode.ai/docs/plugins/
     note: A plugin is not required for status: the /event SSE stream carries the same bus events. permission.ask lets a plugin auto-decide permissions; the docs page itself does not document that hook (only the event names).
- [30] (likely) OpenCode ships a native ACP server (`opencode acp`) and appears in the ACP agents registry as a native implementation; the TUI emits no OSC 9;4 and no code-search hits for setTitle were found.
     src: `opencode acp --help`; https://agentclientprotocol.com/get-started/agents; `gh api search/code` for repo:sst/opencode "9;4" -> 0 and setTitle -> 0
     note: Negative code-search results are weak evidence; the ACP claim is verified.
- [31] (verified) OSC 9;4 progress protocol: `ESC ] 9 ; 4 ; <state> [; <progress 0-100>] (BEL | ESC \)`; states 0 remove/hide, 1 normal with percentage, 2 error (red), 3 indeterminate (pulsing/busy), 4 paused/warning (optional percentage). Originated in ConEmu; implemented by ConEmu, Windows Terminal (taskbar), Ghostty >= 1.2.0 (bar on top of each split, auto-resets after 15 s), iTerm2, WezTerm (Feb 2025), Konsole, mintty >= 3.4.2, and xterm.js via addon. Ghostty distinguishes it from the iTerm2 OSC 9 notification by the `4;` sub-ID
     src: https://rockorager.dev/misc/osc-9-4-progress-bars/; https://ghostty.org/docs/vt/osc/conemu; https://learn.microsoft.com/en-us/windows/terminal/tutorials/progress-bar-sequences
     note: Of the three agents only Claude Code emits it (and only after detecting a supporting terminal). The '3 = indeterminate' state is what Claude Code's hook docs use as their 'desktop notification' example ("]9;4;3;0\\"). 
- [32] (verified) Agent Client Protocol (ACP): JSON-RPC 2.0 (stdio); methods initialize, session/new, session/prompt (returns StopReason: end_turn, max_tokens, max_turn_requests, refusal, cancelled), notifications session/update (agent_message_chunk, agent_thought_chunk, tool_call, tool_call_update with status pending|in_progress|completed|failed, plan, usage_update), agent->client request session/request_permission (options allow_once, allow_always, reject_once, reject_always), session/cancel. Implementations: Claude Code via agent
     src: https://agentclientprotocol.com/protocol/v1/prompt-turn; https://agentclientprotocol.com/get-started/agents; https://agentclientprotocol.com/get-started/clients; `gh repo view agentclientprotocol/clau
     note: ACP gives an exact working/blocked/done state machine (session/prompt in flight = working; pending session/request_permission = waiting; StopReason = done) but the CLIENT renders the conversation — the agent's own TUI is
- [33] (likely) Cross-agent hookless signals available inside a PTY, summarized: (a) OSC 0/2 title — Codex encodes Working/Thinking/'[ ! ] Action Required'/Ready by default; Claude Code's title carries no state; (b) OSC 9 / OSC 777 / OSC 99 / BEL desktop notifications — Codex (approval-requested, agent-turn-complete; focus-gated unless notification_condition=always) and Claude Code (permission/idle after ~6 s/60 s presence heuristics, terminal-dependent); (c) OSC 9;4 progress — Claude Code only, terminal-detection gated; (d) serve
     src: Synthesis of the findings above (sources listed there)
     note: For Claude Code the only immediate, reliable per-instance signal is hooks passed via `--settings` (PermissionRequest, PreToolUse[AskUserQuestion], UserPromptSubmit, Stop, StopFailure), ideally as `http` hooks to the palm
- [34] (verified) Claude Code has a background-session mode with its own attach: `--bg/--background` starts the session in the background, `claude attach <id>` opens it in the current terminal, `claude logs <id>`, `claude stop|kill <id>`, `claude respawn`; Notification types agent_needs_input / agent_completed fire for background sessions while 'agent view' is open in a terminal.
     src: `claude --help` (lines for --bg, attach, logs, respawn, rm, stop); https://code.claude.com/docs/en/hooks#notification (agent_needs_input, agent_completed)
     note: Not investigated further; may be relevant as a Claude-side analogue of OpenCode serve/attach (open question below).
  OPEN: Does Claude Code emit OSC 9;4 progress and desktop notifications inside an xterm.js-backed PTY? Emission is gated on detecting ConEmu/Ghostty>=1.2/iTerm2>=3.6.6 | Does PermissionRequest fire for AskUserQuestion and ExitPlanMode in interactive mode, or only PreToolUse/PostToolUse? Docs confirm the PreToolUse matcher includ | Does `claude --settings '{"hooks":...}'` merge cleanly with the user's own hooks in an INTERACTIVE session (verified here only in `-p` mode with `--setting-sour | Can Codex hooks be injected per launch with `-c 'hooks.<Event>=[...]'` (dotted TOML override) and does the per-hash trust review still block them on first run?  | Exact message strings Codex writes in OSC 9 notifications for approval-requested vs agent-turn-complete (needed if palmer parses them), and whether the Codex TU | Do `opencode attach <url>` and `codex --remote unix://...` give full parity with the standalone TUIs (themes, keybindings, /commands), and is the Codex websocke | Does Claude Code's `claude --bg` + `claude attach <id>` background mode expose any local status stream palmer could read (analogous to OpenCode's /event), and w | Does @xterm/addon-progress (or equivalent) parse OSC 9;4 in xterm.js as claimed by rockorager.dev, and which xterm.js version is required? | Whether the Codex `terminal_title` item set and 'Action Required' prefix exist in the installed 0.147.0 (verified against main-branch source on 2026-09-07).

######## cate-baseline: 32 findings ########
- [1] (verified) cate (github.com/0-AI-UG/cate) repo metadata as of 2026-09-07: 2,122 stars, 135 forks, MIT license, primary language TypeScript, created 2026-03-29, latest release v1.7.0 published 2026-09-06T13:15:14Z, homepage https://cate.cero-ai.com, description 'An infinite zoomable canvas for coding. Editor, terminal, and browser panels in a spatial workspace.'
     src: gh repo view 0-AI-UG/cate --json ... (output: stargazerCount 2122, forkCount 135, licenseInfo.key mit, latestRelease v1.7.0)
- [2] (verified) cate stack (package.json @ main, version 1.7.0): Electron ^41.2.0 (lockfile resolves 41.10.3), @xterm/xterm ^5.5.0 (locked 5.5.0) with addons fit/search/serialize/web-links/webgl (webgl locked 0.18.0), node-pty ^1.0.0 (locked 1.1.0, listed under devDependencies because it ships inside the runtime tarball), monaco-editor 0.52.2, React 18.3.1, zustand 5, electron-vite 5, electron-builder 26.8, ws 8, simple-git, @parcel/watcher, pdfjs-dist, mammoth (DOCX), t3 0.0.38 (pingdotgg T3 Code agent chat), @sentry/electron. En
     src: gh api repos/0-AI-UG/cate/contents/package.json ; package-lock.json parsed with python3 (node_modules/electron 41.10.3, node_modules/node-pty 1.1.0, node_modules/@xterm/xterm 5.5.0, node_modules/@xter
- [3] (verified) cate's canvas is a hand-written React component, not a canvas library: src/renderer/canvas/Canvas.tsx header reads 'Canvas — the main infinite canvas component. Ported from CanvasView.swift.' Other files in that dir: CanvasNode.tsx, Minimap.tsx, layoutEngine.ts, placement.ts, SnapGuides.tsx, useCanvasNodeDrag.ts, useMultiNodeSelection.ts, worktree/territoryGL.ts (WebGL 'territory' shader coloring worktree regions). During pan/zoom it sets pointer-events:none on iframes/webviews/monaco/xterm ('.canvas-interacting' s
     src: gh api repos/0-AI-UG/cate/contents/src/renderer/canvas/Canvas.tsx (head) and repo tree listing via gh api git/trees/main?recursive=1
- [4] (verified) cate v1.7.0 release download sizes (bytes, exact from GitHub release assets): Cate-1.7.0-arm64.dmg 496,142,386 (~496 MB); Cate-1.7.0-arm64-mac.zip 494,450,674; Cate-1.7.0.dmg (x64) 504,537,122; Cate-1.7.0-mac.zip (x64) 502,829,698; Cate-Setup-1.7.0.exe 344,212,739 (~344 MB); Cate-1.7.0-win.zip 415,573,514; Cate-1.7.0.AppImage 392,414,279 (~392 MB); cate_1.7.0_amd64.deb 328,491,680 (~328 MB); cate-1.7.0.tar.gz 375,797,491; per-target runtime daemon tarballs cate-runtime-1.7.0-{darwin-arm64 49,712,293; darwin-x64 52,
     src: gh release view v1.7.0 --repo 0-AI-UG/cate --json assets (https://github.com/0-AI-UG/cate/releases/tag/v1.7.0)
- [5] (verified) cate installed footprint on this Mac (Homebrew cask cate 1.6.0): /Applications/Cate.app = 1.1 GB on disk; of that Electron Framework.framework = 261 MB, app.asar = 528 MB (553,214,983 bytes), plus runtime-host-arm64.tgz 51,530,047 B and runtime-host-x64.tgz 54,270,138 B both shipped in Resources (macOS ships both arches). brew info reports '/opt/homebrew/Caskroom/cate/1.6.0 (1.1GB)', 'Required: macOS >= 12', analytics 'install: 46 (30 days), 127 (90 days)'.
     src: du -sh /Applications/Cate.app ; du -sh .../Electron Framework.framework ; ls -la /Applications/Cate.app/Contents/Resources ; brew info --cask cate
     note: Measured locally, read-only; app was not launched. 1.6.0 not 1.7.0.
- [6] (verified) Why cate installers are ~330-500 MB: electron-builder.yml comments say T3 (the agent-chat server) is 'carried once inside runtime-host*.tgz' and its dependency families are excluded from app.asar 'to avoid duplicating several hundred megabytes in every installer'; the asarUnpack list includes node-pty, @parcel/watcher, koffi, ssh2, sharp, pdfjs-dist, @napi-rs, @img. Each runtime tarball bundles its own Node binary (NODE_VERSION = '22.22.2'), node-pty 1.1.0 prebuilt per target (linux compiled in an old-glibc Docker 
     src: gh api repos/0-AI-UG/cate/contents/electron-builder.yml ; scripts/build-runtime-tarball.mjs header (NODE_VERSION '22.22.2', NODE_PTY_VERSION '1.1.0', RIPGREP_VERSION '14.1.1')
- [7] (verified) cate architecture relevant to a browser+daemon design: PTYs are NOT owned by the Electron main process. A separate 'cate-runtime daemon' ('Runs as a standalone Node program — locally as a child process, on a server over SSH exec, or inside WSL — and speaks the LF-JSON runtime protocol over stdio', 'Nothing electron is imported here') owns terminals, hooks ingestion, file watching, search. Locally it is launched from the extracted runtime tarball's bundled runtime/bin/node ('so the local workspace is just another ru
     src: src/runtime/index.ts header ; src/main/runtime/transports/localTransport.ts header (gh api contents)
- [8] (likely) cate terminal persistence is scrollback-replay + resume-command, not live PTY survival across app quit: the local daemon is killed on quit (runtimeManager.ts: 'Tear down every connection (app quit)... disposeAll' → channel.kill()); PTY output is logged per-terminal to disk (terminalLogger.ts: two-file rotation {id}.log + {id}.prev.log, 1 MB rotation, 250 ms flush, sync drain on quit 'guaranteeing zero data loss'); on restore the log is replayed into xterm (sessionRestore.ts: 'terminal scrollback replay') and the ag
     src: src/main/runtime/runtimeManager.ts (grep disposeAll), src/main/ipc/terminalLogger.ts header, src/renderer/lib/workspace/sessionRestore.ts header, src/main/ipc/agentSessionStamps.ts header, README.md '
     note: Inference from file headers/comments; I did not trace the full quit path or test it.
- [9] (verified) cate DOES show agent state per terminal: README 'every terminal shows whether its agent is working, finished, or waiting on you, and Cate notifies you the moment one needs input'; 'Agent-aware terminals. Supported agent CLIs report turn start, turn end, and permission prompts, so each panel shows running / waiting / finished and pings you when it needs an answer.' Supported terminal agents in code: claude-code, codex, cursor, grok, kiro, opencode (AGENT_HOOK_SPECS map). T3 chat panels separately cover Codex, Claude
     src: gh api repos/0-AI-UG/cate/readme ; src/shared/agentHooks.ts lines 806-811 ; src/main/ipc/agentSessionStamps.ts RESUMABLE_FROM_SESSION_START keys
- [10] (verified) How cate detects agent status today (v1.7.0): hook injection, not output scraping. The daemon plants env vars CATE_HOOK_ENDPOINT, CATE_HOOK_TOKEN, CATE_TERMINAL_ID on every PTY; a 'dependency-free' bridge executable forwards each CLI's stdin-JSON hook payload to the daemon, normalized into one event stream: session-start, session-end, session-title, turn-start, turn-end, permission-wait, turn-resume, input-submit, input-interrupt. Per CLI: Claude Code → workspace file .claude/settings.local.json with events Session
     src: src/shared/agentHooks.ts (827 lines; grep output lines 3, 165-166, 332-382, 389-452, 659-720) via gh api contents
- [11] (verified) cate agent presence/'finished' detection: agentPresence.ts is a 'hook-anchored pid registry' — on each authenticated hook post it walks up the process table from the posting pid to the nearest ancestor whose comm matches the agent, registers it; a 1 Hz process-table scan then checks that pid is still alive with same comm; its disappearance is 'the falling edge' that resolves 'finished' and clears the resume stamp. 'There is deliberately NO other presence source: an agent whose hooks never speak (codex before its tr
     src: src/runtime/capabilities/agentPresence.ts header via gh api contents
- [12] (verified) cate's earlier (May 2026) status detection was a process-tree heuristic and was reported flaky: issue #140 'Agent running detection in terminal is inconsistent' (opened 2026-05-27, closed 2026-05-28) describes 1 s polling of the process tree matching names claude/codex/gemini, running = agent present AND child subprocess active, waitingForInput = no subprocess (3 s hysteresis); maintainer schroejahr2 replied the fix 'is a heuristic, not a guarantee... The only truly reliable solution would be hooks'. CHANGELOG late
     src: https://github.com/0-AI-UG/cate/issues/140 (gh issue view 140) ; https://github.com/0-AI-UG/cate/blob/main/CHANGELOG.md
- [13] (verified) cate published CPU numbers (maintainer PRs, macOS): PR #161 (merged 2026-05-29) 'reduced the CPU usage from a constant ~45% to ~2%-3%', '16x and 24x efficiency improvement', '94% and 96% less WindowServer (mac) utilization' — causes were a 1 s per-terminal lsof/process poll and always-on CSS animations over backdrop-filter blur; fix included making cursor blink opt-in. PR #201 (merged 2026-05-30, 9 terminals): idle subprocess spawns ~18/s → ~0.5/s; background spawns 4.5/s → 0.5/s; pan/zoom cull re-sorts 1 per frame
     src: https://github.com/0-AI-UG/cate/pull/161 ; https://github.com/0-AI-UG/cate/pull/201 ; https://github.com/0-AI-UG/cate/pull/173 (gh pr view bodies)
- [14] (verified) No published or user-reported absolute memory (MB/GB) numbers for cate were found: GitHub issue search for memory/RAM/CPU/leak in repo 0-AI-UG/cate returned only PRs and the CPU issue above; CHANGELOG has one qualitative entry '[0.2.14] Long-session memory usage: terminal loggers, usage watchers, and caches now release resources'; the July 2026 Menon Lab review contains 'no specific data on memory usage, CPU consumption, download size, or startup time'; cate.cero-ai.com renders only a title. docs/perf-audit.md refe
     src: gh api search/issues q='repo:0-AI-UG/cate memory OR RAM OR CPU...' ; CHANGELOG.md line 1062 ; https://themenonlab.blog/blog/cate-infinite-canvas-ide-coding-agents ; https://cate.cero-ai.com
     note: Absence of evidence, not a measurement. cate 1.6.0 is installed locally so a direct measurement is possible if wanted.
- [15] (verified) cate release cadence: 97 git tags total; CHANGELOG spans [0.2.1] 2026-04-03 to [1.7.0] 2026-09-06 (~95 changelog versions in 5 months). Stable line: 1.0.0 2026-05-24, 1.1.0 05-29, 1.2.0 06-06, 1.3.0 06-09, 1.5.0 07-15, 1.6.0 08-07, 1.7.0 09-06 (roughly one stable minor every 2-4 weeks) with beta prereleases several times per week in between (e.g. 1.6.1-beta.1..6 between 08-15 and 09-05). 1,208 commits on main; 20 contributors; 9 open issues+PRs.
     src: gh release list --repo 0-AI-UG/cate ; gh api --paginate repos/0-AI-UG/cate/tags | wc -l = 97 ; CHANGELOG.md headings ; commits Link header rel=last page=1208 ; gh api repos/0-AI-UG/cate/contributors l
- [16] (verified) cate feature list (README 'What's inside'): integrated T3 agent chat panels (Codex, Claude Code, Cursor, Grok, OpenCode, Antigravity); agent-aware terminals with running/waiting/finished + notifications; sessions survive restarts via scrollback + resume; one-click git worktrees with colored 'territory' on canvas; panels = terminals, Monaco editors, browsers (Electron webview, with history/bookmarks/tabs/password saving), PDF/image/DOCX viewers, nested canvases; float on canvas, dock into tabs/splits, or detach to o
     src: gh api repos/0-AI-UG/cate/readme ; v1.7.0 release notes
- [17] (verified) OpenCove (github.com/DeadWaveWave/opencove): 1,581 stars, 138 forks, MIT, TypeScript, created 2026-03-09, latest v0.3.2 2026-09-06. Stack: Electron ^41.5.1 + React + electron-vite, canvas engine @xyflow/react 12, @xterm/xterm 6.0.0 + addon-webgl 0.19.0, node-pty ^1.1.0, ws, zustand. Also ships a headless 'Worker' with an experimental browser Web UI (opencove-server-* tarballs, `opencove worker start --hostname 0.0.0.0 --web-ui-password ...`) and a curl|sh installer. Release 0.3.2 sizes: mac-arm64.dmg 157,735,597 B;
     src: gh repo view DeadWaveWave/opencove ; package.json via gh api ; gh release view (assets) ; README.md
- [18] (verified) OpenCove agent status: 'Claude, Codex, and Pi launches use Worker-owned loopback hook channels for authoritative working / waiting / standby observations. Other providers use session-file detection.' The Worker 'owns loopback-only POST /hooks/claude and POST /hooks/codex receivers'; Pi gets a launch-scoped `-e` extension posting to POST /hooks/pi. Providers: claude-code, codex, opencode, gemini, pi, kimi. Terminal durability: Worker owns PTY lifecycle and a PtyStreamHub with sequence numbers so Desktop/Web clients 
     src: https://github.com/DeadWaveWave/opencove/blob/main/docs/agent/README.md ; docs/terminal/MULTI_CLIENT_ARCHITECTURE.md (gh api contents)
- [19] (verified) nodeterm (github.com/eneskirca/nodeterm): 1,770 stars, 190 forks, license BUSL-1.1 (Business Source License, licensor Enes Kirca; GitHub shows 'Other'), TypeScript, created 2026-06-15, latest v0.3.4 2026-08-30, site nodeterm.dev. Stack: Electron ^42.10.1, @xyflow/react 12, @xterm/xterm ^5.5.0 + addon-webgl 0.18.0 + @xterm/headless 6, node-pty ^1.0.0, ws, zustand 4; terminals are tmux-backed ('The macOS app ships its own tmux'), sessions survive app restart and reboot ('machine reboots restore scrollback and resume 
     src: gh repo view eneskirca/nodeterm ; package.json ; LICENSE head ; README.md ; docs/SERVER.md ; gh release view assets
- [20] (verified) TermCanvas (github.com/blueberrycongee/termcanvas): 394 stars, 33 forks, MIT, TypeScript, created 2026-03-14, latest v0.39.10 2026-05-31 (no pushes since 2026-05-31). Stack: Electron ^41.0.2, @xyflow/react 12, @xterm/xterm ^6.0.0 + webgl 0.19 + serialize/image/search, node-pty ^1.1.0, zustand, ws, @aws-sdk/client-s3. Agents: Claude Code, Codex, Kimi, Gemini, OpenCode; 'a coloured status dot tells you whether the agent is thinking, awaiting input, idle, or done' — driven by lifecycle hooks it registers for Claude/Co
     src: gh repo view blueberrycongee/termcanvas ; package.json ; README.md ; https://github.com/blueberrycongee/termcanvas/blob/main/docs/user-guide.md ; gh release view assets
- [21] (verified) Collaborator (github.com/collabs-inc/collab-public, collaborator.bot): 2,942 stars, 279 forks, license FSL-1.1-ALv2 (Functional Source License; GitHub shows 'Other'), latest v0.8.4 2026-06-16 (macOS-only assets: arm64-mac.zip 152,716,646 B, x64-mac.zip 160,272,881). README 'Stack': 'Electron 40 — desktop shell with multi-webview architecture', React 19, Tailwind 4, electron-vite, 'xterm.js — terminal emulation backed by a persistent node-pty sidecar', Monaco, BlockNote/TipTap, D3, sharp. 'Terminals, context files, 
     src: gh repo view collabs-inc/collab-public ; LICENSE.md head ; README.md ; gh release view assets
- [22] (verified) Horizon (github.com/peters/horizon): 'GPU-accelerated terminal board that puts all your sessions on an infinite canvas'; 704 stars, 28 forks, MIT, Rust (rust-version 1.95, edition 2024), created 2026-03-15, latest v0.2.7 2026-08-02. Stack: egui/eframe 0.36 on wgpu 30, winit, alacritty_terminal 0.26 (in-process terminal emulation, no xterm.js/node-pty), rusqlite, git2, rmcp (MCP server for its browser panels via CDP). Agent panels: grok, claude, codex, open_code, gemini, kilo_code, pi 'with session resume where the 
     src: gh repo view peters/horizon ; Cargo.toml (workspace.dependencies) ; README.md ; gh release view assets
- [23] (verified) CodeGrid (github.com/ZipLyne-Agency/CodeGrid-Claude-Code-Terminal, codegrid.app): 25 stars, MIT, macOS Apple Silicon only, latest v0.1.76 2026-07-20. Stack: Tauri 2 + Rust backend with portable-pty 0.8, rusqlite, sysinfo; frontend React + @xterm/xterm ^5.5.0 (webgl + canvas addons), zustand. README: 'The current Apple Silicon download is about 12 MB. CodeGrid is built with Tauri, not Electron' — release asset CodeGrid_0.1.76_aarch64.dmg = 12,545,596 B. Features: free-form 2D canvas with AUTO/FOCUS/COLS/ROWS/GRID/FI
     src: gh repo view ; README.md ; src-tauri/Cargo.toml ; gh release view assets
     note: Detection mechanism (hooks vs output parsing) not stated in README.
- [24] (verified) TerminalX / tmx (github.com/txc0ld/tmx, site txc0ld.github.io/tmx): 2 stars, MIT, no GitHub releases; Tauri 2 + Rust (tokio, notify, reqwest) + React 19 + @xterm/xterm ^6.0.0. README claims 'Tauri 2 + Rust. 47 MB idle.', 'Native PTY with bounded-channel backpressure', '64-PTY cap', 'Auto-complete detection means you don't wait for process exit — chains fire on logical completion', wires between tiles (agent-chain, context-pipe, etc.), three-layer autosave (localStorage → disk → beforeunload).
     src: gh repo view txc0ld/tmx ; README.md ; src-tauri/Cargo.toml ; package.json
     note: '47 MB idle' is the author's own claim, not independently measured.
- [25] (verified) GraphCode (github.com/scgopi/GraphCode, graphcode.app): 72 stars, license FSL-1.1-MIT, Swift, macOS 15+ Apple Silicon only, latest v0.1.63 2026-09-05, dmg 19,466,863 B. Graph (node/edge) canvas of live Claude Code sessions; 'Each loop's terminal is a zmx session (zmx.sh), so it survives quitting the app and rebooting — the backend's session ID is persisted, so relaunching resumes the conversation with --resume'. Requires Claude Code on PATH; does not bundle it.
     src: gh repo view scgopi/GraphCode ; README.md ; gh release view assets
- [26] (verified) p1rallels/infinite ('infinite canvas for infinite ghostty terminals'): 27 stars, no license, Swift, created 2026-04-04, last push 2026-04-12, no releases. Experimental macOS SwiftUI canvas embedding GhosttyKit (built from ghostty source via Zig) plus WKWebView tiles. Design note relevant to any zoomable terminal canvas: 'Canvas zoom is a visual camera operation, not a real terminal resize... The terminal process itself must not be told that its size changed just because the canvas zoom changed.' Zoom is SwiftUI sca
     src: gh repo view p1rallels/infinite ; README.md
- [27] (likely) Deska (deska.dev): closed-source commercial 'agentic development environment' on an infinite canvas (terminal + Monaco editor + browser panels, worktree-per-task), desktop for macOS (arm64/Intel), Windows, Linux (AppImage/Debian), Android APK, and web app app.deska.dev; free plan ('30 voice minutes a month'), paid plans, 'Deska Cloud: $29 a month'. No GitHub repo, no stack disclosure, no download sizes or memory numbers found; gh search repos 'deska' finds nothing relevant.
     src: https://deska.dev/ ; https://deska.dev/download ; https://deska.dev/blog/warp-alternative ; gh search repos deska
     note: Stack (Electron vs Tauri) unverified.
- [28] (likely) No public project was found that embeds xterm.js terminals as a tldraw or Excalidraw custom shape. The canvas engine used by the Electron canvas apps that use one is @xyflow/react (React Flow): termcanvas, opencove, nodeterm. cate, Collaborator, CodeGrid, tmx use hand-rolled canvases; Horizon uses egui.
     src: WebSearch 'tldraw terminal xterm shape canvas github' ; WebSearch 'github tldraw "node-pty" terminal shape OR "xterm" custom shape canvas app' ; WebSearch 'excalidraw OR tldraw "xterm" embedded termin
     note: Search-based negative; a small private/unindexed repo could exist.
- [29] (likely) Only two projects in this space serve the canvas to a plain browser without Electron on the client: nodeterm Server Edition (headless Node server + WebSocket-RPC, same renderer as desktop, password auth, tmux-backed; install script needs git/curl/tar and downloads a pinned Node LTS if absent, and 'npm rebuild node-pty' needs make/gcc/python3) and OpenCove's Worker Web UI (experimental, loopback by default, server tarballs 83-111 MB). Deska also has app.deska.dev (hosted, account-based). No Python-backed spatial ter
     src: https://github.com/eneskirca/nodeterm/blob/main/docs/SERVER.md ; https://github.com/DeadWaveWave/opencove README 'Web UI (Experimental)' and release assets ; WebSearch 'browser-based "infinite canvas"
- [30] (likely) Warp / Ghostty / WezTerm have no infinite-canvas or free-placement mode; their multiplexing is tabs/splits/workspaces. Warp (2026) runs Claude Code/Codex/Gemini/OpenCode side by side in panes with an 'Agent Management Panel' and Oz cloud agents. Third-party app-size figures (Termdock blog): Ghostty 1.3 '~50 MB', WezTerm 2026.01 '~80 MB', Warp 2.0 '~300 MB' and 'Higher memory at idle'. Ghostty has pane-zoom (toggle a split to fullscreen) discussions (#3484, #8447) but not spatial placement. Warp memory issues with a
     src: https://www.termdock.com/en/blog/best-terminal-emulator-ai-cli-2026 ; https://github.com/warpdotdev/warp/issues/8569 ; WebSearch results listing warp issues #8521 #12154 #7520 ; WebSearch 'Ghostty OR 
     note: App sizes are from a blog, not measured; Warp idle RAM '200-400MB' appears only in an aiproductivity.ai guide — unverified.
- [31] (verified) Weight comparison summary (latest release, macOS arm64 primary download): cate 1.7.0 dmg ~496 MB (1.1 GB installed); nodeterm 0.3.4 dmg ~168 MB; OpenCove 0.3.2 dmg ~158 MB; TermCanvas 0.39.10 dmg ~155 MB; Collaborator 0.8.4 zip ~153 MB; Horizon 0.2.7 (Rust/egui) tar.gz ~23 MB / installer ~37 MB; GraphCode 0.1.63 (Swift) dmg ~19.5 MB; CodeGrid 0.1.76 (Tauri) dmg ~12.5 MB. cate is roughly 3x the other Electron canvases, mainly because it bundles Electron + a 528 MB app.asar (Monaco, pdfjs, sharp, ssh2, etc.) + two ru
     src: Derived from the gh release view asset byte counts cited above and the local du of /Applications/Cate.app
- [32] (verified) Local toolchain available on this machine for the project's backend decision: bun 1.3.14, node v26.0.0, python3 3.13.9, gh 2.97.0.
     src: bun --version ; node --version ; python3 --version ; gh --version
  OPEN: cate runtime memory (RSS per process, idle vs N terminals) is unpublished; cate 1.6.0 is installed at /Applications/Cate.app on this machine, so a controlled me | Does cate's local runtime daemon ever survive an Electron quit (e.g. detached mode) or is it always killed via disposeAll? The code comments strongly suggest al | Which hook events does cate use for OpenCode exactly (the 'four bus events')? I saw the plugin file path and description but did not read lines 659-760 of agent | How do CodeGrid (Tauri) and Horizon (Rust) detect running/waiting state — hooks, output parsing, or process inspection? Their READMEs advertise indicators but d | Deska's desktop stack (Electron vs Tauri) and download sizes were not discoverable from deska.dev pages fetched. | Whether any Python-backed or pip-installable spatial terminal canvas exists — searches surfaced none, but this is a search-based negative.

######## web-terminal-prior-art: 34 findings ########
- [1] (verified) xterm.js official flow-control guide: xterm.js processes input at only "5 - 35 MB/s" while producers reach "several GB/s"; without flow control "the emulator will get sluggish, might not respond to keystrokes anymore or even worse, some transport buffers might overflow"; there is a hardcoded input buffer limit ("50MB at time of writing") beyond which data is discarded.
     src: https://xtermjs.org/docs/guides/flowcontrol/
     note: Numbers are from the guide itself (undated). Rio's 2026 benchmark (below) measured much higher parse+paint throughput on modern hardware, so treat 5-35 MB/s as a conservative floor.
- [2] (verified) xterm.js WriteBuffer source constants: DISCARD_WATERMARK = 50000000 (~50 MB, 'Safety watermark to avoid memory exhaustion and browser engine crash on fast data input'), WRITE_TIMEOUT_MS = 12 ('max number of ms to spend on writes before allowing the renderer to catch up with a 0ms setTimeout'), WRITE_BUFFER_LENGTH_THRESHOLD = 50. When pending data exceeds the watermark, write() throws 'write data discarded, use flow control to avoid losing data'.
     src: https://raw.githubusercontent.com/xtermjs/xterm.js/master/src/common/input/WriteBuffer.ts
     note: Implication: xterm.js itself yields to the renderer every 12ms of parsing; you cannot make it parse faster by sending bigger chunks, only avoid overhead from too many tiny chunks.
- [3] (verified) Recommended flow-control pattern (xterm.js guide): use write(chunk, callback) accounting with high/low watermarks rather than pausing the pty on every chunk; example values HIGH = 100000 bytes, LOW = 10000 bytes; 'HIGH should not be greater than 500K' to keep Ctrl-C responsive during `yes`; optimized variant attaches a callback only every CALLBACK_BYTE_LIMIT = 100000 bytes with HIGH = 5 / LOW = 2 pending callbacks. For WebSocket transports, span the accounting from client to server: client sends custom ACK messages
     src: https://xtermjs.org/docs/guides/flowcontrol/
     note: Both ttyd and VS Code implement exactly this ACK/pause pattern (see next entries).
- [4] (verified) ttyd client flow control constants: `const flowControl = { limit: 100000, highWater: 10, lowWater: 4 }` — after every `limit` bytes written a write callback is attached; when pending callbacks > highWater the client sends a PAUSE command, when < lowWater it sends RESUME. Server side: PAUSE -> `pty_pause(pss->process)` (uv_read_stop), RESUME -> `pty_resume` (uv_read_start).
     src: https://raw.githubusercontent.com/tsl0922/ttyd/main/html/src/components/app.tsx ; https://raw.githubusercontent.com/tsl0922/ttyd/main/html/src/components/terminal/xterm/index.ts ; https://raw.githubus
     note: So ttyd tolerates up to ~1 MB (10 x 100 KB) in flight before pausing the pty.
- [5] (verified) ttyd server-side backpressure is 'one pty chunk in flight': pty.c read_cb calls `uv_read_stop(stream)` immediately after each read; protocol.c process_read_cb stores the chunk in `pss->pty_buf` and calls `lws_callback_on_writable`; in LWS_CALLBACK_SERVER_WRITEABLE it does `wsi_output(wsi, pss->pty_buf); pty_buf_free(...); pss->pty_buf = NULL; pty_resume(pss->process);`. The read buffer uses libuv's `suggested_size`.
     src: local: curl raw pty.c + grep (lines 65-77, 124-135); https://raw.githubusercontent.com/tsl0922/ttyd/main/src/protocol.c
     note: This is a simple, robust design: the server never buffers more than one pty read per client, at the cost of a stop/start syscall pair per chunk.
- [6] (verified) VS Code flow control constants (renderer <-> pty host): HighWatermarkChars = 100000 ('number of _unacknowledged_ chars to have been sent before the pty is paused'), LowWatermarkChars = 5000, CharCountAckSize = 5000 ('must be less than or equal to LowWatermarkChars or the terminal max never unpause'). Comment notes: 'heavy commands will likely pause as latency grows, not flooding the connection is the important thing'.
     src: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/common/terminal.ts
     note: Design origin: microsoft/vscode#74620 and #113827 ('send an ack every x bytes from the renderer and nodePtyHostProcess will pause if it hasn't received the yth last ack').
- [7] (verified) VS Code batches pty output before sending to the renderer: TerminalDataBufferer coalesces data events over a 5 ms window (`throttleBy` default 5) and flushes `buffer.data.join('')`. Motivation from #74620: 'Batch data events inside nodePtyHostProcess, reducing the amount of total events in favor of larger ones'; also 'Data events from node-pty seems to be blocked by the event loop'.
     src: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/common/terminalDataBuffering.ts ; gh issue view 74620 --repo microsoft/vscode
- [8] (verified) Hyper's batching experiment: BATCH_MAX_SIZE = 200 * 1024 ('this value can be exceeded by ~4k (chunk sizes seem to be 4k at the most)'); '100k can lower the throughput by ~25%'. `find ~` went from ~35 s to ~15 s and 3->17 FPS to 40+ FPS with batching + WebGL; but with `yes` batching reduced FPS from ~50 to ~30.
     src: https://github.com/vercel/hyper/pull/3336
     note: Only published chunk-size tuning numbers found. Trade-off: big batches help bursty file output, hurt steady streams. A time-based (5 ms) flush like VS Code's is the middle ground.
- [9] (verified) Local measurement of the pty layer on this Mac (Darwin 24.6.0, Python 3.13): reading a pty fed by `dd | base64` returns chunks of exactly 1024 bytes (27306 of 27307 reads), raw pty->Python throughput ~197 MB/s with no WS/xterm. With `yes` as producer, chunks were ~114 bytes (3 lines) because `yes` writes line by line.
     src: local: python3 ptychunk2.py and ptychunk.py in scratchpad (pty.fork + os.read(fd, 1<<20))
     note: macOS pty hands the master ~1 KB per read; so server-side coalescing (time- or size-based) before WS framing is necessary or you send ~200k WS frames/sec at full rate. Linux not measured.
- [10] (verified) What saturates first (maintainer analysis): xterm.js is '100% main thread bound'; parsing:rendering main-thread ratio is '<60:40' with DOM renderer, '~80:20' with canvas, '>90:10' with WebGL, so the VT parser (synchronous JS) is the bottleneck once WebGL is on; theoretical N-terminal scaling: '2 instances --> 30 fps, 4 --> 15 fps' from a 60 fps baseline; worker isolation would need '>30% rewrite' and is not planned.
     src: https://github.com/xtermjs/xterm.js/issues/3368
     note: The fps scaling figure is the maintainer's approximation, not a measurement.
- [11] (likely) Published xterm.js throughput (2026-08-10, Chrome, Apple Silicon, 120x40 grid, medians of 3): xterm.js WebGL plain text parse+paint 178 MB/s, ANSI 108 MB/s; headless parse plain 167 MB/s, ANSI 103 MB/s; cold init 33.7 ms; full-screen TUI redraw 120 fps (capped), frame time p95 10.0 ms. (Rio's own engine claimed 576 MB/s plain.)
     src: https://rioterm.com/blog/2026/08/10/riotermjs-rio-terminal-for-the-web
     note: Vendor benchmark by a competitor; methodology described and code public, but not independently reproduced here.
- [12] (verified) VS Code 2017 renderer post: DOM renderer dropped 'below 10 FPS' under heavy output; 'composing elements and doing a layout could take longer than a frame (16.6ms)'; canvas renderer was 'approximately 5 to 45 times faster'; GC pressure and frame skipping to give the parser CPU were the identified bottlenecks.
     src: https://code.visualstudio.com/blogs/2017/10/03/terminal-renderer
     note: Historical (canvas renderer era) but the DOM-renderer characterization still applies.
- [13] (verified) Current xterm.js: @xterm/xterm 6.0.0 (published 2025-12-22), @xterm/addon-webgl 0.19.0, @xterm/addon-fit 0.11.0, @xterm/addon-serialize 0.14.0, @xterm/headless 6.0.0. 6.0.0 release notes: '#5105 Remove the canvas renderer — This is a breaking change, this addon no longer exists and we recommend using either the DOM renderer or WebGL'. The addons/ directory on master has no addon-canvas; @xterm/addon-canvas 0.7.0 on npm is frozen (last modified 2024-07-14, peerDependency @xterm/xterm ^5.0.0).
     src: local: npm view ...; gh release view 6.0.0 --repo xtermjs/xterm.js; gh api repos/xtermjs/xterm.js/contents/addons
     note: So the real choice today is DOM vs WebGL only.
- [14] (verified) WebGL addon usage/pitfalls per README: load it AFTER terminal.open(element); the browser 'may drop WebGL contexts for various reasons like OOM or after the system has been suspended'; the addon exposes onContextLoss and the suggested minimal handling is `addon.onContextLoss(e => { addon.dispose(); })` which falls back to the DOM renderer.
     src: https://raw.githubusercontent.com/xtermjs/xterm.js/master/addons/addon-webgl/README.md
- [15] (likely) Chromium limits active WebGL contexts per renderer process and forcibly loses the oldest when exceeded. Current source: `ActivateContext` loops `while (ActiveContexts().size() >= max_gl_contexts ...) ForciblyLoseOldestContext("WARNING: Too many active WebGL contexts. Oldest context will be lost.")`; the limit is a runtime variable (max_active_webgl_contexts_, separate value for workers), settable via `--max-active-webgl-contexts`. The original 2013 CL set 'an arbitrary count (currently 16) or a cumulative pixel cou
     src: local: chromium.googlesource.com webgl_rendering_context_base.cc?format=TEXT (lines 444-512); https://codereview.chromium.org/14217005 ; https://issues.chromium.org/issues/40939743 (search summary)
     note: The '16 desktop / 8 Android' default is from the 2013 CL plus multiple secondary sources (Chromium issue tracker, nodeterm's docs); I could not fetch the exact current default constant (it lives in another file). Firefox
- [16] (verified) xterm.js issue #4379 'Support dozens of terminals on a single page' (Terminal7 author, opened 2023-01-23) is still OPEN with no implementation; maintainer (Tyriar) said 'the tricky part in trying to build this into xterm.js is coming up with a nice method of creating and sharing the context'; proposed approach is one hidden shared WebGL context with gl.scissor/gl.viewport. Terminal7's pane.ts loads `new WebglAddon()` per pane with no try/catch, no onContextLoss handler, and no cap.
     src: gh issue view 4379 --repo xtermjs/xterm.js ; https://raw.githubusercontent.com/tuzig/terminal7/master/src/pane.ts
- [17] (verified) nodeterm (agent terminals as draggable nodes on a pan/zoom canvas; tmux-backed; Electron + browser Server Edition) documents a WebGL context BUDGET: 'browsers cap ~16 live contexts, and a canvas holds far more terminals'; `WEBGL_BUDGET` 12 default in the browser edition, desktop raises Chromium's cap via `--max-active-webgl-contexts` = 32 and budget to 24 (`WEBGL_BUDGET_DESKTOP_MAC` 16); a per-terminal IntersectionObserver reports visibility to a module-level coordinator that grants contexts only after an acquire d
     src: https://raw.githubusercontent.com/eneskirca/nodeterm/main/CLAUDE.md (lines 478-570, saved to scratchpad/nodeterm_CLAUDE.md)
     note: Closest prior art to the palmer design (many agent terminals on a spatial canvas). Additional pitfall they hit: disposing WebglAddon while the element is unmeasurable (display:none / detached) makes the fallback DOM rend
- [18] (verified) mulmoterminal (browser grid of Claude Code/Codex PTYs, Node/TS + node-pty + tmux, `npx mulmoterminal@latest`) notes: '22 concurrent cells' seen in the field; they still use @xterm/addon-canvas ^0.7.0 on @xterm/xterm ^6.0.0 (an xterm-5 addon) to fix CJK glyph drift in the DOM renderer, with guidance to move to addon-webgl 0.19.0; canvas/WebGL text is 'not in the DOM — headless inspection sees nothing'; xterm's renderer is paused while a cell is offscreen (IntersectionObserver-gated fit). Status is hook-driven (Claud
     src: https://raw.githubusercontent.com/receptron/mulmoterminal/main/docs/terminal-notes.md ; https://raw.githubusercontent.com/receptron/mulmoterminal/main/src/composables/useTerminalConnections.ts ; local
- [19] (verified) mulmoterminal reconnect/replay design: server keeps a bounded output tail `OUTPUT_BUFFER_LIMIT = 1024 * 1024` per session and on reattach replays `boundedTail(entry.buffer, OUTPUT_BUFFER_LIMIT)` plus cols/rows; the cut is made escape-sequence-safe (`splitSequenceLength()` searches back for ESC and checks whether the CSI/OSC terminator falls before/after the cut); replay is passed through `stripTerminalQueries` removing DA1/DA2/DA3, DSR/CPR, kitty keyboard query, XTVERSION, OSC 10/11/12 color queries because 'xterm 
     src: https://raw.githubusercontent.com/receptron/mulmoterminal/main/server/session/terminal-replay.ts ; https://raw.githubusercontent.com/receptron/mulmoterminal/main/server/session/pty-connection.ts ; loc
     note: Raw-byte-tail replay is the cheap approach; its known hazards (device-query replies, mid-escape cuts, stale cursor/mode state) are exactly what this list documents.
- [20] (verified) VS Code's alternative replay approach: the pty host runs a headless xterm (`import pkg from '@xterm/headless'`) per persistent process and on reattach serializes it with `@xterm/addon-serialize` (`SerializeAddon`) into VT sequences, emitting a replay event; ACKs are suppressed during replay (`if (this._inReplay) return;`). TerminalRecorder caps recorded data at `MaxRecorderDataSize = 10 * 1024 * 1024` and records resize events between data runs so replay can re-apply cols/rows. Restored scrollback is limited by `te
     src: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/node/ptyService.ts ; https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/common/terminalRe
     note: Server-side headless xterm + serialize avoids the mid-escape and device-query problems of raw tails, at the cost of running a VT parser per PTY on the server (Node only — @xterm/headless is JS).
- [21] (verified) VS Code/code-server process lifetime across disconnects: terminals live in a 'pty host' process outside the window; on detach a grace timer runs (`LocalReconnectConstants.GraceTime = 60000`, `ShortGraceTime = 6000`) after which the process is shut down; reattach cancels the timers; an orphan check waits on a 4000 ms barrier. Docs distinguish 'process reconnection' (reload window: reconnect to the previous process and restore its content) from 'process revive' (restart: content restored, process relaunched).
     src: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/common/terminal.ts ; https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/node/ptyService.t
     note: code-server inherits this since it is VS Code server; I did not find code-server-specific documentation beyond a pty-host bug report (coder/code-server#6901).
- [22] (verified) ttyd does NOT keep the process alive across client disconnects: a new child is spawned per WebSocket client on the JSON_DATA handshake (`spawn_process(pss, columns, rows)` -> `pty_spawn`), and on connection close it sets `ws_closed = true`, calls `pty_pause` then `pty_kill(pss->process, server->sig_code)`. Options: `-o, --once` 'Accept only one client and exit on disconnection', `-q, --exit-no-conn` 'Exit on all clients disconnection', `-m, --max-clients`, `-P, --ping-interval` default 5 s. Issue #1528 'Don't kill 
     src: https://raw.githubusercontent.com/tsl0922/ttyd/main/src/protocol.c ; https://raw.githubusercontent.com/tsl0922/ttyd/main/README.md ; gh issue view 1528 --repo tsl0922/ttyd
- [23] (verified) ttyd client reconnect: on abnormal close (code != 1000) with `doReconnect` it shows 'Reconnecting...' and calls `refreshToken().then(connect)`; otherwise shows 'Press ⏎ to Reconnect'. Protocol is single-byte-prefixed text frames: '0' OUTPUT/INPUT, '1' RESIZE_TERMINAL/SET_WINDOW_TITLE, '2' SET_PREFERENCES; resize is `'1' + JSON.stringify({columns, rows})`; server applies it via `struct winsize size = {rows, columns, 0, 0}; ioctl(process->pty, TIOCSWINSZ, &size)`; child spawned with `forkpty(&master, NULL, NULL, &siz
     src: https://raw.githubusercontent.com/tsl0922/ttyd/main/html/src/components/terminal/xterm/index.ts ; https://raw.githubusercontent.com/tsl0922/ttyd/main/src/pty.c
     note: Because reconnect spawns a fresh process, ttyd 'reconnect' is not session resumption.
- [24] (verified) gotty likewise spawns a process per WebSocket connection (`slave, err = server.factory.New(params, headers)`), closes it when the client goes away (`defer slave.Close()`; `if err == webtty.ErrMasterClosed { _ = slave.Close() }`), reads the pty with a 1024-byte buffer (`make([]byte, wt.bufferSize)`, default 1024), has no flow control, and `--reconnect`/`--reconnect-time` (default 10 s) only tell the client to reconnect (SetReconnect message) to a NEW process. README: 'GoTTY starts a new process with the given comman
     src: https://raw.githubusercontent.com/sorenisanerd/gotty/master/server/handlers.go ; https://raw.githubusercontent.com/sorenisanerd/gotty/master/webtty/webtty.go ; https://raw.githubusercontent.com/soreni
- [25] (verified) wetty: node-pty (`pty.spawn('/usr/bin/env', cmd, xterm)`) wrapping ssh, transport socket.io; on disconnect `term.kill()`; resize via `term.resize(cols, rows)`; it does implement flow control (a 'tinybuffer' batching layer plus `fcServer.account(data.length)` pausing the pty until the client 'commit's consumed bytes). Install `npm -g i wetty`.
     src: https://raw.githubusercontent.com/butlerx/wetty/main/src/server/spawn.ts ; https://github.com/butlerx/wetty
     note: Another confirmation that every mainstream web terminal ties PTY lifetime to the socket; owning the PTY independently of the socket (as VS Code, mulmoterminal, nodeterm do) is the differentiator palmer needs.
- [26] (verified) Resize round-trip: xterm.js API doc says 'It's best practice to debounce calls to resize, this will help ensure that the pty can respond to the resize event before another one occurs'; FitAddon: `fit()` after `terminal.open()`, `proposeDimensions()` returns undefined when cell width/height is 0 (hidden/zero-size container) and historically NaN/'This API only accepts integers' errors (issues #3029, #4338, #3881). Wave Terminal debounces resize at 50 ms. Server side TIOCSWINSZ verified locally in pure Python stdlib: 
     src: https://xtermjs.org/docs/api/terminal/classes/terminal/ ; https://raw.githubusercontent.com/xtermjs/xterm.js/master/addons/addon-fit/README.md ; https://github.com/xtermjs/xterm.js/issues/3029 ; https
     note: Pitfall from mulmoterminal: 'tmux learns a client's size from SIGWINCH, and the kernel raises SIGWINCH only when the size actually CHANGES' — resending an unchanged size does nothing; they force a bump (e.g. 120x39 then 
- [27] (likely) Pan/zoom canvas pitfall: xterm.js character-size measurement and selection were affected by CSS transforms (issues #2488 'Character size computation is affected by CSS transforms', closed 2023-08-01; #3242 'Character selection is affected by CSS scaling transforms', closed 2021-10-19; #2584 'Selection doesn't respect the zoom CSS rule'). Both were closed, but they indicate that scaling a terminal via CSS transform (as a spatial canvas zoom would) is a historically fragile path.
     src: gh issue view 2488/3242 --repo xtermjs/xterm.js ; https://github.com/xtermjs/xterm.js/issues/2584
     note: Closed state verified; whether the fixes fully cover transform: scale() with the WebGL renderer in 6.0 is unverified. nodeterm sidesteps this by measuring/fitting in canvas space and disposing offscreen terminals.
- [28] (verified) Wave Terminal (xterm.js, many blocks per tab) pattern: detect WebGL2 (`canvas.getContext('webgl2')`), use `WebGLSupported && waveOptions.useWebGl ? 'webgl' : 'dom'`, `addon.onContextLoss(() => { this.setTermRenderer('dom'); })`, 50 ms resize debounce, restore buffer on reconnect by fetching a cached output file and writing it back while tracking a `ptyoffset`. VS Code's xtermTerminal.ts: gpuAcceleration 'auto' loads WebGL unless a previous context loss set `_suggestedRendererType = 'dom'` (then DOM for the rest of 
     src: https://raw.githubusercontent.com/wavetermdev/waveterm/main/frontend/app/view/term/termwrap.ts ; https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/workbench/contrib/terminal/browser/xterm
- [29] (verified) Input latency reference points: Typometer measurements (T14 Gen1, Arch/Xorg) — xterm 5.3 ms avg vs Hyper (xterm.js/Electron) 39.8 ms avg; author considers >20 ms 'a noticeable delay'. VS Code mitigates remote latency with local echo (typeahead) enabled when measured latency exceeds `terminal.integrated.localEchoLatencyThreshold` (default 30 ms), excluded for programs ['vim','vi','nano','tmux'] in the title. xterm.js maintainers declined to build local echo into core (#887: 'I don't think we can really action this, 
     src: https://beuke.org/terminal-latency/ ; https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/workbench/contrib/terminalContrib/typeAhead/common/terminalTypeAheadConfiguration.ts ; gh issue vie
     note: No published end-to-end PTY->WS->xterm.js keystroke-latency numbers for ttyd/gotty were found; for a localhost server the WS hop is sub-millisecond, so xterm.js render latency (~1 frame) dominates. Unverified for this ex
- [30] (verified) Server-side WebSocket backpressure primitives available without native deps: Bun `ws.send()` returns -1 ('enqueued but there is backpressure'), 0 (dropped), or bytes sent; `drain(ws)` fires when writable again; `backpressureLimit` default 16 MB; `closeOnBackpressureLimit` default false; `idleTimeout` default 120 s. Node `ws` 8.21.3 is current on npm. Python: the `websockets` package is NOT installed in this environment (ModuleNotFoundError) and the stdlib has no WebSocket server.
     src: https://bun.com/docs/api/websockets ; local: npm view ws version; python3 -c 'import websockets'
- [31] (verified) PTY without native deps: Bun 1.3.14 is installed locally and `Bun.spawn(cmd, { terminal: { cols, rows, data(term, chunk) } })` works (verified: `proc.terminal` exposes close, resize, setRawMode, write, ref/unref, flags); introduced in Bun v1.3.5 (`Bun.Terminal`), 'only available on POSIX systems (Linux, macOS)' at launch; issue #25565 'Bun.Terminal support for Windows' closed 2026-04-21. By contrast node-pty 1.1.0 has `scripts.install = 'node scripts/prebuild.js || node-gyp rebuild'` (native addon, may need a toolc
     src: local: bun run buntest.ts; bun --version; npm view node-pty@1.1.0 scripts.install; npm view @lydell/node-pty optionalDependencies; https://bun.com/blog/bun-v1.3.5 ; gh issue view 25565 --repo oven-sh/
     note: Relevant to the 'no native dependencies, one-line install' rule: Bun (single binary) or Python stdlib are the two PTY paths that meet it; Node needs a prebuilt native addon.
- [32] (verified) Known xterm.js 6.0.0 buffer bug encountered by a many-terminal app: issue #6063 '`Buffer.resize` can leave viewport rows with no live `BufferLine` when growing rows with `ybase > 0` (6.0.0) — uncaught TypeErrors in `lineFeed` and `_eraseInBufferLine`' (opened 2026-07-23, OPEN). mulmoterminal works around it with `guardBufferHealth()` + `rebuildTerminal()` (REBUILD_COOLDOWN_MS = 10_000) and replaying the server tail into a fresh xterm.
     src: gh issue view 6063 --repo xtermjs/xterm.js ; https://raw.githubusercontent.com/receptron/mulmoterminal/main/src/composables/useTerminalConnections.ts
     note: Triggered by resizing (growing rows) a terminal with scrollback — common when windows on a canvas are resized.
- [33] (verified) xterm.js defaults relevant to many instances: scrollback default 1000 lines; allowProposedApi default false (needed for some addons, ttyd sets it true); allowTransparency 'can negatively impact performance'; VS Code's WebGL context-lost bug report (#263159, 2025-08) was closed as a driver/GPU issue with advice to change `terminal.integrated.gpuAcceleration`.
     src: https://xtermjs.org/docs/api/terminal/interfaces/iterminaloptions/ ; gh issue view 263159 --repo microsoft/vscode
- [34] (likely) Other 2026 prior art with several agent terminals on one page: ccanvas (Tauri 2/Rust `portable-pty` per widget, xterm.js, 'Per-agent activity dot (idle/working/waiting)' derived from the agent's session transcript; web mode uses node-pty); wmux (Electron, 'xterm.js (WebGL)', daemon-owned sessions surviving quits/reboots). None publish N-terminal performance numbers.
     src: https://github.com/DevoidSloth/ccanvas ; WebSearch results for wmux (https://github.com/openwong2kim/wmux, https://github.com/amirlehmam/wmux)
     note: wmux details are from search snippets only, not fetched.
  OPEN: Exact current Chromium default for max active WebGL contexts: the 2013 CL and secondary sources say 16 (8 on Android) and the current source confirms the evicti | No published end-to-end latency or CPU numbers exist for PTY->WebSocket->xterm.js specifically (ttyd/gotty have none); the only measured components are xterm.js | Whether xterm.js 6.0 + WebGL renders and selects correctly inside a CSS transform: scale() container (spatial canvas zoom) — the relevant issues (#2488, #3242)  | Linux pty read-chunk size and kernel buffer behaviour were not measured (only macOS: 1024-byte reads); this affects how aggressively the server must coalesce be | Replay strategy trade-off not benchmarked: raw byte tail (mulmoterminal, 1 MiB, needs escape-safe cut + query stripping) vs server-side headless xterm + seriali | How the 'superseded' / multi-client policy should work when the same PTY is viewed from two browser tabs (mulmoterminal kicks the old one; VS Code allows one ow | Bun.Terminal maturity: verified to work on Bun 1.3.14/macOS with resize()/write(), but no data on data-loss or flow-control semantics under heavy output (the 'd

######## persistence: 44 findings ########
- [1] (verified) A1 PATTERN (industry consensus): do NOT replay a raw-byte ring buffer into a fresh xterm.js; keep a server-side headless terminal emulator fed with every PTY byte and, on (re)connect, serialize its screen+scrollback state into one ANSI string. VS Code's pty host does exactly this: PersistentTerminalProcess holds `_serializer: ITerminalSerializer` (class XtermSerializer wrapping `@xterm/headless` Terminal), feeds it via `this.onProcessData(e => this._serializer.handleData(e))`, and on reconnect calls `triggerReplay(
     src: https://raw.githubusercontent.com/microsoft/vscode/main/src/vs/platform/terminal/node/ptyService.ts (fetched 2026-09-07; names quoted from file)
     note: Same pattern independently chosen by shpool (shpool_vt100 in-memory render), zmx (libghostty-vt snapshot on attach), exe.dev exe-scroll (ghostty-vt: 'scrollback is more complicated than just replaying all the received by
- [2] (verified) VS Code 1.60 release notes: buffer restore moved from replaying recorded events to 'a headless terminal frontend on the pty host'; default restored scrollback is 100 lines ('a good balance between speed and the amount of data'); reported ~5-10x speedup for full buffer restoration vs the old replay approach.
     src: https://code.visualstudio.com/updates/v1_60
     note: Speedup number is VS Code's own claim, hardware dependent.
- [3] (verified) VS Code setting `terminal.integrated.persistentSessionScrollback` default = 100 lines; description: 'Controls the maximum amount of lines that will be restored when reconnecting to a persistent terminal session. Increasing this will restore more lines of scrollback at the cost of more memory and increase the time it takes to connect to terminals on start up... should be set to a value less than or equal to terminal.integrated.scrollback'. `terminal.integrated.scrollback` default = 1000. `enablePersistentSessions` d
     src: gh api repos/microsoft/vscode/contents/src/vs/platform/terminal/common/terminalPlatformConfiguration.ts (lines 401-406) and .../workbench/contrib/terminal/common/terminalConfiguration.ts
     note: Typical serialized-replay size reference point: 100 lines default, capped by 1000-line scrollback.
- [4] (verified) xterm.js default scrollback is 1000 rows ('Defaults to 1000.' in ITerminalOptions.scrollback doc comment).
     src: gh api repos/xtermjs/xterm.js/contents/typings/xterm.d.ts (line ~265-269)
- [5] (verified) `@xterm/headless` (npm 6.0.0 today) is 'a headless terminal that can be run in node.js. This is useful in combination with the frontend xterm for example to keep track of a terminal's state on a remote server where the process is hosted.' Package README marks it experimental. `@xterm/addon-serialize` is at 0.14.0.
     src: gh api repos/xtermjs/xterm.js/contents/headless/README.md; `npm view @xterm/headless version` -> 6.0.0; `npm view @xterm/addon-serialize version` -> 0.14.0
     note: Both are pure JS (no native deps) — usable from Node or Bun backends.
- [6] (verified) SerializeAddon API (typings): serialize() 'Serializes terminal rows into a string that can be written back to the terminal to restore the state. The cursor will also be positioned to the correct cell.' Options: `scrollback` (rows from bottom of scrollback), `excludeModes` (default false), `excludeAltBuffer` (default false), `range`.
     src: https://raw.githubusercontent.com/xtermjs/xterm.js/master/addons/addon-serialize/typings/addon-serialize.d.ts
- [7] (verified) SerializeAddon source: when the active buffer is the alternate screen it emits `\x1b[?1049h\x1b[H` + alt-screen content after the normal buffer; `_serializeModes` re-emits DECSET ?1 (app cursor keys), ?66 (app keypad), ?2004 (bracketed paste), 4 (insert), ?6 (origin), ?45 (reverse wraparound), ?1004 (focus), ?7l if wraparound off, mouse tracking mode (1000/1002/1003 family), and cursor hide; explicitly does NOT serialize synchronized-output mode ('temporary').
     src: gh api repos/xtermjs/xterm.js/contents/addons/addon-serialize/src/SerializeAddon.ts (lines 558-615)
     note: Means restore into a fresh xterm.js re-enables mouse tracking / bracketed paste / alt screen the agent TUI had turned on — important because all three agent TUIs turn these on (see A1 TUI findings).
- [8] (verified) CAVEAT of any serialize-based restore (Zed RFC on persistent terminals): their headless-alacritty snapshot 'doesn't capture: charsets, tab stops, window title, keyboard mode stack, colour overrides, cursor style, selection, vi-mode cursor'. Zed rejected tmux ('nested keybindings', 'double terminal emulation', 'configuration burden') and characterises VS Code's approach as requiring replaying buffered output through xterm.js causing visible delay with large scrollback. Zed design: one Rust `pty-host` daemon per term
     src: https://github.com/zed-industries/zed/discussions/50584
     note: Window title (OSC 0/2) and kitty keyboard protocol stack are notable omissions for agent TUIs (OpenCode emits kitty keyboard `CSI > ... u` at startup, verified locally).
- [9] (verified) CAVEAT of raw-ANSI replay (xterm.js maintainers): replaying stored history that contains terminal query sequences (DA/DSR etc.) into a terminal already connected to the PTY produces garbage like `2R0;276;0c` because 'a complex app like vi would always request some sort of reports, and those requests are contained in the data blob. The terminal will answer that with onData events'. Recommendation: 'replay first, then set up the connection to the PTY', and for v4+ use SerializeAddon instead of raw replay.
     src: https://github.com/xtermjs/xterm.js/discussions/4869
     note: If palmer ever does raw replay, it must (a) discard onData during replay and (b) only connect the WebSocket input path after replay completes.
- [10] (verified) Example raw ring-buffer size in a shipping browser terminal: sshx server keeps per-shell raw output with `const SHELL_STORED_BYTES: u64 = 1 << 21; // 2 MiB`, prunes oldest chunks beyond that, and clients resume by seqnum/byte offset.
     src: gh api repos/ekzhang/sshx/contents/crates/sshx-server/src/session.rs (line 26: SHELL_STORED_BYTES; lines 262-270 prune logic)
     note: sshx renders in the browser, so it does replay raw bytes; it is a 'shared shell' tool, not restoring alt-screen TUIs.
- [11] (verified) tmux default `history-limit` is 2000 lines per pane ('Set the maximum number of lines held in pane history').
     src: `tmux -L palmer-probe -f /dev/null start-server \; show-options -gv history-limit \; kill-server` -> 2000; `man tmux` line 2415
- [12] (verified) herdr live handoff also carries 'optional ANSI history replay from libghostty-vt' and states: 'Herdr does not fake replay for alternate-screen panes like nvim, btop, or htop. Their processes survive the handoff, but the visual state is best effort until the app redraws.' Client connections are dropped during handoff.
     src: https://herdr.dev/blog/live-updates-without-killing-your-terminal-processes/
     note: Practical implication: for alt-screen TUIs (all three agents), a resize/SIGWINCH nudge after reattach to force a full redraw is the fallback everyone relies on; abduco README similarly warns of 'incomplete redraws' and s
- [13] (verified) exe.dev exe-scroll: a per-login-shell session manager on the VM with 'a ghostty-vt emulator so it can replay screen and scrollback on attach'; clients reconnect via Unix domain socket and 'survive network roaming by retrying when the websocket disconnects'.
     src: https://blog.exe.dev/exe-scroll
     note: Earlier WebSearch summary cited '10,000 lines xterm buffer / 512 KB replay cap' for this tool but the fetched article did not contain those numbers — treat those figures as unverified.
- [14] (verified) shpool: 'continually maintains an in-memory render of the terminal state via the shpool_vt100 crate. On reattach, shpool will use this in-memory render to re-draw the screen'. Runs as a single systemd user unit (or brew services on mac, or autodaemonize-on-demand); 'runs on linux and mac. Linux is the primary supported platform and mac currently still has a few tests which don't pass'. Single daemon => 'if a single terminal session causes a crash all terminal sessions will crash'; one client per session.
     src: https://github.com/shell-pool/shpool/blob/master/README.md
- [15] (verified) zmx: one daemon per session, each with its own Unix socket; 'daemon sends pty output to client and ghostty-vt -> ghostty-vt holds terminal state and scrollback -> user re-attaches -> ghostty-vt sends terminal snapshot to client stdout'. Works on mac and linux; binaries + brew tap; no windows/tabs/splits by design.
     src: https://github.com/neurosnap/zmx
- [16] (verified) A1 TUI FACT — Claude Code: fullscreen renderer 'draws the interface on the terminal's alternate screen buffer, like vim or htop'. Whether a fresh session starts fullscreen depends on a table (e.g. users who first used Claude Code on/after 2026-05-06 with feature-flag fetching start in Fullscreen; `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` forces Classic; `CLAUDE_CODE_NO_FLICKER=1` forces Fullscreen; saved `tui` setting wins). 'Background sessions opened from agent view or claude attach always use fullscreen rendering
     src: https://code.claude.com/docs/en/fullscreen
     note: Local Claude Code is 2.1.260. Palmer can pin the renderer per pane via env var — useful for making restore deterministic.
- [17] (verified) A1 TUI FACT — Codex: `tui.alternate_screen` = auto|always|never (default auto: alt screen except inside Zellij), plus `--no-alt-screen` CLI flag. Codex uses CSI ?1049 alt screen by default.
     src: https://github.com/openai/codex/pull/8555 ; https://learn.chatgpt.com/docs/config-file/config-advanced ('tui.alternate_screen controls whether the TUI uses alternate screen mode. Setting it to never p
     note: Local codex-cli 0.147.0.
- [18] (verified) A1 TUI FACT — OpenCode 1.14.48 TUI, spawned in a PTY for 6 s, emitted: `ESC[?1049h` (alt screen) True, `ESC[?1000h` mouse True, `ESC[?1006h` SGR mouse True, `ESC[?2004h` bracketed paste True, kitty keyboard `ESC[>...u` True. Its renderer (@opentui/core renderer.ts) has an explicit `screenMode === "alternate-screen"` path calling `lib.setupTerminal(rendererPtr, useAlternateScreen)`.
     src: local: python3 pty.fork + os.execvp('opencode',['opencode','--pure']) in scratchpad, scanned 5996 bytes of output; gh api repos/anomalyco/opentui/contents/packages/core/src/renderer.ts lines 3029-3185
     note: So on reconnect palmer must restore alt-screen + mouse + bracketed-paste + kitty-kbd state; SerializeAddon covers the first three, not the kitty keyboard stack.
- [19] (verified) A2 PTY SEMANTICS (macOS, measured): when the LAST master fd of a PTY is closed, the child on the slave is killed by SIGHUP (waitpid -> WIFSIGNALED, termsig 1). A child that ignores SIGHUP keeps running but its terminal is gone. Therefore 'surviving a server restart' == 'some surviving process keeps the master fd open' (or the agent re-spawns from its own saved session).
     src: local python3 test: pty.fork(); child `sh -c 'sleep 30'`; os.close(master); waitpid -> 'signaled: True termsig: 1 (SIGHUP=1)'; second run with SIGHUP ignored -> 'still running: True'
     note: Linux: final master close runs pty_close -> tty_vhangup -> SIGHUP to the slave's session leader/foreground group (LKML/kernel tty docs via search; not re-verified on a Linux box) — confidence 'likely' for Linux. https://
- [20] (verified) A2 OPTION (a) daemon + thin bridge: the pattern used by every no-tmux tool surveyed — shpool (one daemon, systemd user unit or autodaemonize), zmx/abduco/Zed pty-host (one daemon per session, one Unix socket each), Claude Code's own per-user 'supervisor', Codex `app-server daemon`, OpenCode `serve`. Cost: an extra long-lived process the installer must start/keep alive; on macOS a separately-launched process gets its OWN TCC/privacy grants.
     src: shpool README; zmx README; Zed discussion #50584; https://code.claude.com/docs/en/agent-view; `codex app-server daemon --help`; `opencode serve --help`
     note: Claude docs spell out the macOS cost: 'Background sessions can't read Desktop, Documents, or Downloads on macOS: On macOS, the background session host runs as its own process and requests access to protected folders sepa
- [21] (verified) A2 OPTION (b) hand PTY master fds across processes with SCM_RIGHTS: herdr's `--handoff` does it — 'manifest = app snapshot + pane metadata + optional replay bytes; FDs = duplicated PTY master file descriptors; transport = private Unix socket + SCM_RIGHTS'; 'Herdr does not move the child processes. It moves ownership of the terminals those processes are already attached to.' Limits: 'This is not checkpoint/restore. It does not survive a reboot. It does not migrate processes across machines.' Same-machine, experiment
     src: https://herdr.dev/blog/live-updates-without-killing-your-terminal-processes/
     note: Only covers planned restarts (old server alive to hand off). A crash still needs a separate fd holder (daemon) or agent-native resume.
- [22] (verified) A2 LANGUAGE COST for fd passing — Python: stdlib `socket.send_fds`/`socket.recv_fds` (SCM_RIGHTS, 'Added in version 3.9', Unix) and `os.openpty`/`pty.fork` need no native deps. Verified locally: opened a PTY, sent master fd over an AF_UNIX socketpair, received it as a new fd with identical st_rdev.
     src: https://docs.python.org/3/library/socket.html (send_fds/recv_fds); local python3 3.13.9 run: 'sent master fd 3 received as fd 7 same device: True'
- [23] (verified) A2 LANGUAGE COST for fd passing — Node: `subprocess.send(message, sendHandle)` accepts only `net.Socket`, `net.Server`, or `dgram.Socket`; arbitrary fds (a PTY master) cannot be passed; the feature request to expose SCM_RIGHTS in `net` (nodejs/node#53391, opened 2024-06-08, notes libuv already supports it) is CLOSED as not planned. Third-party native addons exist (e.g. node-unix-socket, Rust/Neon).
     src: https://nodejs.org/api/child_process.html ; https://github.com/nodejs/node/issues/53391 ; https://github.com/xushengfeng/node-unix-socket
     note: Node 26.0.0 locally. `node -e` check: net module exposes no SCM_RIGHTS.
- [24] (verified) A2 LANGUAGE COST for fd passing — Bun: no SCM_RIGHTS support; `process.binding('pipe_wrap')` 'is not implemented in Bun'; issue oven-sh/bun#17247 (SCM_RIGHTS hand-off proxy use case) is still open with no maintainer response in the thread.
     src: https://github.com/oven-sh/bun/issues/17247
     note: Conclusion for a TS backend with zero native deps: PTY masters cannot be moved between processes => the process that spawned the PTY must be the long-lived one (daemon-per-pane or single daemon); a planned-restart handof
- [25] (verified) A2 PTY CREATION COST — Node: node-pty 1.1.0 (released 2025-12-22) is a native addon but its npm tarball ships `prebuilds/` (observed darwin-arm64, darwin-x64, win32-arm64 in `npm pack --dry-run`; output truncated so linux entries not confirmed) and its install script is `node scripts/prebuild.js || node-gyp rebuild`, i.e. compiles only if no matching prebuild dir exists. README still lists Xcode / `make python build-essential` as requirements. It is not installed here (`require('node-pty')` -> MODULE_NOT_FOUND).
     src: gh api repos/microsoft/node-pty/releases/latest -> v1.1.0; gh api .../contents/package.json ('install': 'node scripts/prebuild.js || node-gyp rebuild', files include 'prebuilds/'); gh api .../scripts/
     note: Whether this counts as 'no native dependencies' under palmer's rules is a judgement call: no compile in the common case, but a .node binary and a compile fallback on unusual platforms (e.g. Alpine/musl, new Node ABI).
- [26] (verified) A2 PTY CREATION COST — Bun: built-in `Bun.Terminal` and `Bun.spawn({ terminal })` since Bun v1.3.5 ('Terminal support is only available on POSIX systems (Linux, macOS)'). Verified locally on Bun 1.3.14: `new Bun.Terminal({cols:80,rows:24,data(){...}})` + `Bun.spawn(['sh','-c','tty; stty size'],{terminal:t})` printed `/dev/ttys012` and `24 80`; Terminal prototype has close, closed, controlFlags/inputFlags/localFlags/outputFlags, ref, unref, resize, setRawMode, write.
     src: https://bun.com/blog/bun-v1.3.5 ; https://bun.com/reference/bun/Terminal ; local `bun -e` runs (Bun 1.3.14+0d9b296af)
     note: Zero native deps if the backend is Bun. Bun itself is a single binary; whether its installer satisfies 'one-line install / no external service' is for the project to judge.
- [27] (verified) A2 PTY CREATION COST — Python: stdlib `pty`/`os.openpty`/`pty.fork` available (verified `import pty` on 3.13.9); no native deps. Daemonising: `subprocess.Popen(..., start_new_session=True)` 'the setsid() system call will be made in the child process' (POSIX, since 3.2); `process_group=` (3.11).
     src: local python3 -c 'import pty'; https://docs.python.org/3/library/subprocess.html
- [28] (verified) A2 DAEMONISING from Node/Bun: Node `spawn(..., { detached: true, stdio: 'ignore' }); subprocess.unref()` — 'the child process will be made the leader of a new process group and session' (setsid); parent must not share stdio or the child stays attached to the controlling terminal. Bun.spawn also honours `detached: true` (verified locally: child pgid == its pid, differs from parent's pgid) and `proc.unref()`.
     src: https://nodejs.org/api/child_process.html#optionsdetached ; local `bun -e` run: 'child pid/pgid/sess: 37657 37657 0 | parent pgid: 37653'
     note: This is how a thin bridge can auto-start a daemon-per-pane without launchd/systemd at all (the 'autodaemonize' mode shpool also offers). Costs: daemon dies with reboot/logout-kill; no restart-on-crash supervision.
- [29] (verified) A2 SUPERVISION — macOS launchd: `KeepAlive` 'default is false and therefore only demand will start the job. The value may be set to true to unconditionally keep the job alive... Jobs that exit quickly and frequently when configured to be kept alive will be throttled... The use of this key implicitly implies RunAtLoad'. Install via `launchctl bootstrap gui/<uid> <plist>` ('Bootstraps or removes domains and services'). Socket activation exists via the `Sockets` key but 'The job must check-in to get a copy of the file
     src: `man launchd.plist` (KeepAlive, Sockets sections), `man launchctl` (bootstrap, gui/<uid>)
     note: Calling launch_activate_socket needs FFI: Python has stdlib ctypes ('allows calling functions in DLLs or shared libraries'); Bun has `bun:ffi` but it is 'experimental, with known bugs and limitations. Do not rely on it i
- [30] (verified) A2 SUPERVISION — Linux systemd user units: to keep a user service alive after logout / start it at boot you need lingering: `loginctl enable-linger` — 'a user manager is spawned for the user at boot and kept around after logouts. This allows users who are not logged in to run long-running services.' Socket activation: `.socket` units pass listening fds via sd_listen_fds (LISTEN_FDS env); `Accept=no` default passes the listening socket to a single service. systemd also offers an fd STORE: `FileDescriptorStoreMax=` (
     src: https://raw.githubusercontent.com/systemd/systemd/main/man/loginctl.xml ; .../man/systemd.socket.xml ; .../man/systemd.service.xml
     note: fd store is the only supervisor-level way to survive a CRASH without a second daemon, but it is Linux-only and requires sd_notify over a datagram socket with SCM_RIGHTS (Python stdlib can; Node/Bun cannot without native 
- [31] (verified) A2 RE-PARENTING: Linux `prctl(PR_SET_CHILD_SUBREAPER)` (since 3.4): 'A subreaper fulfills the role of init(1) for its descendant processes... when a process becomes orphaned... it will be reparented to the nearest still living ancestor subreaper.' Re-parenting only affects who reaps/gets exit status; it does not keep a PTY master open, so it does not by itself make agents survive a server restart.
     src: https://man7.org/linux/man-pages/man2/PR_SET_CHILD_SUBREAPER.2const.html
     note: macOS has no subreaper equivalent that I could cite — unverified.
- [32] (verified) A2 OTHER TOOLS: reptyr re-attaches a running process to a new terminal via ptrace; Linux and FreeBSD only (no macOS), needs `kernel.yama.ptrace_scope=0` on Ubuntu-style hardening, and cannot fix job control in the old shell. nginx-style 'binary upgrade' (USR2 -> new master re-execs while old and new masters coexist) is a planned-restart pattern where the new process inherits fds; it does not help after a crash.
     src: https://github.com/nelhage/reptyr ; https://nginx.org/en/docs/control.html
     note: nginx doc confirms coexistence of old/new masters; the exact fd-inheritance mechanism (NGINX env var listing fds) is not stated on that page — unverified detail.
- [33] (verified) A2/A3 AGENT-NATIVE DAEMON — Claude Code has its own supervisor: `claude --bg` 'Start the session in the background and return immediately. Prints the id that claude attach, logs, stop and rm take'. Docs: 'The supervisor is a background service that runs your background sessions so they keep working after you close agent view or your terminal'; state at `~/.claude/jobs/<id>/`; `claude attach <id>` opens 'the full interactive session' in the current terminal and you can detach again ('Press ← on an empty prompt, or r
     src: `claude --help` (2.1.260) lines for --bg/attach/logs/respawn/stop; https://code.claude.com/docs/en/agent-view
     note: A palmer pane could run `claude attach <id>` in its PTY; then palmer's server restarting never kills the agent — only the attach client. Attached sessions always render fullscreen (alt screen).
- [34] (verified) A2/A3 AGENT-NATIVE DAEMON — Codex: `codex app-server daemon {bootstrap|start|restart|stop|enable-remote-control|...}` ('bootstrap: Install durable local app-server management for SSH-driven use'), `codex remote-control` (experimental), and the TUI can attach to a running app server with `codex --remote ws://host:port | wss:// | unix://PATH` ('Connect the TUI to a remote app server endpoint'). All marked [experimental].
     src: `codex --help`, `codex app-server --help`, `codex app-server daemon --help`, `codex remote-control --help` (codex-cli 0.147.0)
     note: Not tested end-to-end here; whether a `--remote` TUI keeps the thread running when the TUI is killed is unverified.
- [35] (verified) A2/A3 AGENT-NATIVE DAEMON — OpenCode: 'When you run opencode it starts a TUI and a server. Where the TUI is the client that talks to the server'; `opencode serve` runs the headless HTTP server (CLI help: --port default 0, --hostname 127.0.0.1; docs mention 4096); `opencode attach <url> [--dir] [-c|--continue] [-s|--session <id>] [--fork] [-p password]` attaches a TUI to a running server; server exposes `/session` CRUD, `/event` SSE and OpenAPI at `/doc`; 'This architecture lets opencode support multiple clients'.
     src: `opencode --help`, `opencode attach --help`, `opencode serve --help` (1.14.48); https://opencode.ai/docs/server/ ; https://opencode.ai/docs/cli/
     note: Whether an in-flight agent turn keeps running in `opencode serve` after the attached TUI dies is not documented on the fetched pages — unverified.
- [36] (verified) A3 CLAUDE CODE RESUME: flags `-c/--continue` (most recent in cwd), `-r/--resume [id|name]`, `--session-id <uuid>` ('Use a specific session ID for the conversation (must be a valid UUID)') to PRE-ASSIGN an id at launch, `--fork-session`, `-n/--name`. Since v2.1.223 `claude --resume <session-id>` works from any directory. Restored: full history incl. tool calls, model, agent, permission mode (terminal path; bypassPermissions and plan are NOT restored), active goal, unexpired scheduled tasks. NOT restored: 'Background
     src: `claude --help` (2.1.260); https://code.claude.com/docs/en/sessions ; local `ls ~/.claude/projects/...` shows `<uuid>.jsonl` files
     note: Pre-assigning with --session-id is the simplest palmer-side id tracking; but /clear, /branch and --fork-session create NEW ids (SessionStart hook fires with source clear/fork), so hooks are more robust.
- [37] (verified) A3 CLAUDE CODE SESSION-ID DISCOVERY: hooks receive JSON with `session_id`, `cwd`, `transcript_path`, `permission_mode`, `hook_event_name`; `SessionStart` matcher values `startup|resume|clear|compact|fork`; `SessionEnd` reasons `clear|resume|logout|prompt_input_exit|other`; `Notification` matchers include `permission_prompt`, `idle_prompt`, `agent_needs_input`, `agent_completed`; `Stop` has no matcher. Hooks live in `~/.claude/settings.json`, `.claude/settings.json`, `.claude/settings.local.json`, plugins, or can be
     src: https://code.claude.com/docs/en/hooks ; `claude --help` (--settings)
     note: This is exactly how herdr's Claude integration gets its 'session ref'. Same hook stream doubles as the working/waiting/done signal for palmer's status light.
- [38] (verified) A3 CODEX RESUME: `codex resume [SESSION_ID] [PROMPT]` — 'Session id (UUID) or session name... If omitted, use --last to pick the most recent recorded session'; `--all` disables cwd filtering; `--include-non-interactive`; `codex fork` and `codex exec resume [--last|<id>]` exist. No flag to pre-assign a session id (grep of `codex --help`/`codex exec --help` for session-id/thread-id found none; `--ephemeral` = 'Run without persisting session files to disk'). Rollouts at `~/.codex/sessions/YYYY/MM/DD/rollout-<timestamp
     src: `codex resume --help`, `codex --help`, `codex exec --help` (0.147.0); local `find ~/.codex/sessions -name '*.jsonl'` + `head -c 1200` of one rollout; https://learn.chatgpt.com/docs/developer-commands?
     note: Automation hazard: docs say 'If the current working directory differs from the session's saved directory, Codex asks which directory to use' (interactive prompt); `tui.resume_cwd` = current|session exists to control it (
- [39] (verified) A3 CODEX SESSION-ID DISCOVERY: (1) Hooks — events SessionStart, SessionEnd, PreToolUse, PermissionRequest, PostToolUse, UserPromptSubmit, Stop, Interrupt, PreCompact, PostCompact, SubagentStart/Stop; every command hook gets `session_id`, `cwd`, `transcript_path`, `hook_event_name`, `model` (turn hooks add `turn_id`); configured in `~/.codex/hooks.json`, `~/.codex/config.toml` `[hooks]`, or repo `.codex/hooks.json`; enabled by default (`[features] hooks = false` disables; `codex_hooks` is a deprecated alias). (2) `n
     src: https://learn.chatgpt.com/docs/hooks ; https://learn.chatgpt.com/docs/config-file/config-advanced ; https://learn.chatgpt.com/docs/non-interactive-mode ; local ~/.codex/config.toml already has a `noti
     note: Only one `notify` command is allowed in config.toml, so palmer must not clobber a user's existing notify; hooks are additive and preferable.
- [40] (verified) A3 OPENCODE RESUME: `-s/--session <id>` ('session id to continue') and `-c/--continue` ('continue the last session'), plus `--fork`, accepted by the TUI (`opencode`), `opencode run`, and `opencode attach`. Session ids look like `ses_1dddaa69dffeybwnj8xzpmvX3X`. Storage is SQLite at `~/.local/share/opencode/opencode.db` (tables include session, message, part, session_share, todo); `opencode session list [--format json]`, `opencode export <sessionID>`, `opencode import <file|url>`.
     src: `opencode --help`, `opencode run --help`, `opencode attach --help`, `opencode session --help` (1.14.48); local `sqlite3 ~/.local/share/opencode/opencode.db '.tables'` and `select count(*),min(id),max(
     note: No flag to pre-assign an id. `opencode session list --format json` printed nothing from the palmer dir — likely cwd-scoped; unverified whether `--session` refuses ids from another project directory.
- [41] (verified) A3 OPENCODE SESSION-ID DISCOVERY: plugins (JS/TS modules in `.opencode/plugins/` or `~/.config/opencode/plugins/`, or npm packages in config) get an `event` hook; SDK type `EventSessionCreated = { type: "session.created", properties: { info: Session } }` (also session.updated/idle/status/compacted/deleted/error, permission.asked/replied, message.updated/...). The plugin context includes `project` and `directory`. Alternatively use the server: POST/GET `/session`, SSE `/event`.
     src: https://opencode.ai/docs/plugins/ ; gh api repos/anomalyco/opencode/contents/packages/sdk/js/src/gen/types.gen.ts (lines 562-567)
     note: A GitHub issue reports 'Plugin event listener for session.created not firing' (anomalyco/opencode#14808) — current status unverified. herdr uses a plugin for OpenCode for the same purpose.
- [42] (verified) A3 HERDR 'NATIVE AGENT SESSION RESTORE' (the model palmer would copy): 'Herdr resumes an agent only if it has a session ref for that pane. Session refs come from official integrations — hooks/plugins that the agent runs and that report its session ID back to Herdr over a socket.' Hooks for Claude Code/Codex (SessionStart -> socket), JS plugin for OpenCode; resume commands: Claude Code & Codex `--resume <id>`, OpenCode `--session <id>`; disabled via `[session] resume_agents_on_restore = false`; requires minimum inte
     src: https://herdr.dev/docs/integrations/ ; https://github.com/herdrdev/herdr/issues/126
     note: So even the Rust daemon's answer to a COLD restart is 'relaunch the agent with its own resume flag' — process survival is only for warm restarts (handoff).
- [43] (verified) A3 WHAT 'RELAUNCH WITH RESUME' DOES AND DOES NOT RESTORE (all three): restores the conversation/thread from the agent's own on-disk store; does not restore the terminal screen (new TUI redraws from scratch), in-flight tool runs (Claude: 'Background Bash and monitor tasks aren't' restored), or per-launch flags (Claude lists --mcp-config/--settings/--plugin-dir/--add-dir). Claude's own supervisor after a reboot follows the same path: 'Attach or reply to it and it restarts from where it left off'. Long-idle Claude ses
     src: https://code.claude.com/docs/en/sessions ; https://code.claude.com/docs/en/agent-view
- [44] (verified) CONTEXT: palmer README currently shows an architecture with `herdr terminal session control` child processes and states 'pane 안 프로세스는 보통 재시작에 살아남지 않는다... --handoff (문서 기준, 미측정)'. Nothing under <저장소> was modified by this research.
     src: cat <저장소>/README.md; git status was clean at start; only scratchpad files (octest/) were created
  OPEN: Does Claude Code keep the palmer-preassigned `--session-id` across `/clear` and `/branch`, or does it mint a new id (SessionStart source `clear`/`fork` suggests | Can Codex's interactive 'cwd differs from the session's saved directory' prompt on `codex resume <id>` be suppressed non-interactively (e.g. `-c tui.resume_cwd= | Does `opencode --session <id>` work when launched from a different project directory than the session's, and is `opencode session list` cwd-scoped? Untested (th | Do agent turns keep running when the attached client dies for `codex --remote` / `opencode attach` (i.e. can palmer treat those as durable daemons the way `clau | node-pty 1.1.0 prebuilds: `npm pack --dry-run` output was truncated to darwin/win32 entries — confirm linux-x64/arm64 (glibc and musl?) prebuilds exist before c | Linux SIGHUP-on-master-close was only measured on macOS here; the Linux claim rests on kernel docs/LKML. Re-run the python3 pty test on a Linux box. | If palmer replays SerializeAddon output into a fresh xterm.js for an alt-screen agent TUI, does a follow-up SIGWINCH/resize reliably make Claude Code / Codex /  | What exact scrollback/replay caps do herdr (libghostty-vt replay bytes) and zmx use? Not found; VS Code 100 lines, sshx 2 MiB, tmux 2000 lines are the documente | Is a macOS LaunchAgent-started palmer daemon subject to separate TCC prompts (Desktop/Documents/Downloads, Local Network) like Claude's background host? Inferre | Whether the project's 'one-line install / no native deps' rule tolerates (a) node-pty's prebuilt .node addon, (b) requiring Bun as the runtime for Bun.Terminal, | Memory/CPU cost of one headless xterm (`@xterm/headless`) per pane on the server, and of daemon-per-pane (one Node/Bun/Python process each) vs a single daemon —
