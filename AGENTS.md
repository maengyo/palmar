# palmar — 작업 지침

돌고 있는 코딩 에이전트들을 위한 **공간형 캔버스 클라이언트**. 브라우저에서 돌고, 터미널을
원하는 자리에 원하는 크기로 놓고, 각각이 일하는 중인지 나를 기다리는지 끝났는지 보여준다.
**런타임은 우리 것이다** — 남의 멀티플렉서 위에 얹지 않는다(2026-09-07, 사용자 결정).

## 절대 원칙

1. **런타임은 우리 것이고, 좁다.** PTY 를 띄우고, 바이트를 옮기고, 훅을 받고, 자리를 기억한다.
   그게 전부다. **터미널 에뮬레이션은 브라우저(xterm.js)가 한다.** 서버에서 화면을 해석하는 것은
   기본값이 아니다 — polycanv 가 pyte 로 그걸 하다가 부하에서 tmux 의 8배 CPU 를 냈다(polycanv 실측).
2. **세션은 UI 보다 오래 산다.** 브라우저 탭을 닫아도 pane 안 프로세스는 죽지 않는다.
   데몬이 PTY 를 소유하고 UI 는 붙고 뗀다. **데몬은 재시작하지 않는다**(⑦=b, 2026-09-07 저녁) —
   업데이트 때만 다시 뜨고, 그때 에이전트는 `--resume`, 셸 pane 은 죽는다. 핸드오프는 만들지 않는다.
   **재접속은 에이전트가 SIGWINCH 흔들기, 셸이 링버퍼(256KB) 재생이다**(⑨, 스파이크 F).
   "탭을 닫으면 다 죽는다" 만이 요구 위반이다.
3. **상태는 CLI 가 주는 이벤트로 받는다. 화면을 읽지 않는다.** claude·qwen 은 훅,
   opencode 는 SSE, codex 는 훅이나 터미널 타이틀 — **어느 쪽인지는 아직 미정이다**(⑧).
   **Claude Code 2.1.259 에서는** `PermissionRequest` 가 대화상자 종류(승인·질문)와
   무관하게 "나를 기다린다" 의 신호였다(실측). 다른 CLI 는 같은 자리의 이벤트를 쓰되 아직 그렇게까지
   재지 않았다. 화면 정규식은 herdr 에서 실제로 틀렸다(codex 업데이트 대화상자 → idle).
   **훅은 palmar 가 PATH shim 으로 붙인다** — 자기 셸의 PATH 앞에 껍데기를 두어 `--settings` 를 끼운다.
   사용자 설정 파일은 안 건드리고, 프로젝트마다 파일을 심지 않는다(2026-09-07 저녁; 오전의 "사용자가
   한 번 설치" 를 뒤집음. `--settings` 가 병합임은 스파이크 E 로 실측). **훅은 무슨 일이 있어도 0 으로 끝난다.**
   훅이 안 깔린 것은 **palmar 가 알아채고 알려 준다** — 조용히 회색으로 두지 마라.
4. **palmar 는 셸만 연다.** 폴더를 고르면 그 경로의 셸이 뜨고, 무엇을 돌릴지는 사용자가 정한다.
   **도구 목록을 갖지 않는다** — claude 를 어떻게 실행하는지 몰라도 된다.
5. **프런트엔드는 하나다.** 브라우저로 만들고, 같은 웹 코드를 나중에 **Tauri** 로 감싸
   macOS·Windows·Linux(AppImage·deb) 앱을 낸다. Electron 은 검토하지 않는다.
   **CLI/TUI 는 만들지 않는다** — polycanv 가 TUI 였고, pane 마다 다른 배율·연속 줌·겹침이
   거기서는 안 됐다.
6. **가벼움이 기능보다 우선이고, 가볍다는 건 "버벅이지 않는다" 는 뜻이다.** 크기도 작아야 하지만
   먼저 재는 것은 **쓰는 동안의 프레임 시간과 입력 지연**이다 — pane 8개를 띄우고 하나는 출력 중인
   채로 끌고, 줌하고, 타이핑해 본다(`docs/decisions.md` ⑤). 기능을 넣을 때마다 **무엇을 안 넣을지**를
   함께 정한다. 숫자는 비교 대상·조건과 함께 잰다(tmux · herdr 서버 35MB+6MB/pane, 1회 측정 ·
   polycanv 55MB/1개·75MB/8개 · cate). 유휴 비용도 본다 — 폴링과 감시가 유휴를 먹는다.
7. **설치가 한 줄이어야 한다.** 네이티브 의존성·빌드 툴체인·외부 서비스·계정 — 넷 다 마찰이다.
   이 제약이 PTY 라이브러리와 언어 선택을 좁힌다(`docs/decisions.md` ②).
   **훅은 이 원칙을 다시 지킨다** — PATH shim 이 붙이므로 사용자가 설치할 것이 없다(2026-09-07 저녁).
   **언어도 정해졌다: 파이썬, 바닥 3.9** — `/usr/bin/python3` 이 3.9.6 이고 그 심은 `/usr/bin/git` 과
   같은 파일이라, git 이 도는 기계엔 이미 있다(스파이크 H). 3.10+ 문법을 쓰면 `uv` 가 필요해져 깨진다.
   **남은 것은 앱 배포뿐이다** — 앱을 내면 "받아서 실행" 이 된다. ④ 에서 정한다.

## 구조

```
web/       캔버스 UI. xterm.js 로 pane 을 그린다. 양옆에 레일 둘
server/    데몬. PTY 소유 · 웹소켓 · 훅/SSE 수신 · 폴더 읽기. 얇게 유지한다
docs/      조사 기록, 날짜별 스파이크 스크립트
```

**터미널과 상태의 진실은 데몬에 있다.** 브라우저는 사본을 들고 있지 말고 붙을 때마다 받아라.
좌표를 **어디에 두는지는 아직 미정이다**(③) — 데몬 쪽 파일일 수도, 브라우저 저장소일 수도 있다.

**화면은 셋이다** — 왼쪽 터미널 목록(기다리는 것이 위로), 가운데 캔버스,
오른쪽 디렉터리 레일(폴더만, 게으르게 읽고, 새로고침 버튼). 자세한 것은 `docs/decisions.md` 의 "화면".

## 언어

- **커밋 메시지는 영어로 쓴다.** 예외 없다.
- **README 는 영어가 정본이다**(`README.md`). 한글판 `README.ko.md` 를 같이 고친다.
  한쪽만 고쳐야 한다면 영어를 고치고, 한글판이 뒤처졌다는 것을 한글판에 적어라.
- **`docs/` 나머지는 한국어다.** 읽는 사람이 하나인 작업 문서다. 여기까지 영어로 바꾸지 마라.
- 코드 주석은 그 파일이 이미 쓰는 언어를 따른다.

## 검증

- **근거 없이 "됐다"고 하지 않는다.** 에이전트는 **진짜 PTY 에서** 띄워 훅이 실제로 발화하는
  것으로 확인한다. 헤드리스(`claude -p`)는 승인 흐름을 띄우지 않아 검증이 안 된다.
- **텍스트와 Enter 는 따로 보내라.** 한 번에 쓰면 Claude Code 가 제출하지 않는다(실측).
- **기억이 아니라 실물을 본다.** 훅 페이로드 키는 문서와 다를 수 있다 — 받은 JSON 을 기준으로 짠다.
- **표본 수와 측정 창을 숫자 옆에 적어라.** 폴링으로 잰 시각은 그 주기보다 정밀하게 말하지 마라.
- 자동으로 확인되는 것(훅 발화·fd 전달·CPU)과 **사람이 봐야 하는 것**(색·배치·읽히는가·깜박임)을
  보고할 때 구분해라. 후자를 "확인했다"고 쓰지 마라.
- **우리가 실행한 것과 조사로 읽은 것을 구분해라.** 조사는 "조사" 라고 적는다.
- **결정이나 측정이 문서에 들어가면 codex 로 교차 검증한다.** 같은 모델이 자기 글을 검토하면
  같은 자리에서 같이 틀린다. polycanv 에서 "Codex 교차 검증이 잡은 것" 이 그날의 가장 큰 교훈이었다.
  묶어서 하는 시점: 사용자 결정이 한 덩어리 들어왔을 때 · 스파이크 결과를 적었을 때 · 커밋 전.
  찾은 것은 심각도와 함께 남기고, 고칠 때는 **근거를 확인하고 고친다** — 지적이라고 다 맞지 않는다.
- 테스트로 띄운 에이전트는 끝나고 죽였는지 `pgrep` 으로 확인한다.
- **데몬을 고쳤으면 새 파이썬에서도 한 번 돌려라.** 이 기계의 `/usr/bin/python3` 은 3.9 인데
  리눅스·WSL 사용자는 3.10~3.13 을 쓴다. 표준 라이브러리의 동작이 그 사이에 바뀐다 — `Ctrl-C` 가
  안 먹던 #42 가 그것이었다(`Server.wait_closed()` 가 3.12.1 부터 열린 연결을 기다린다).
  `/opt/anaconda3/bin/python3` (3.13) 로 같은 것을 한 번 더 돌리면 된다. 3.9 는 여전히 바닥이다(②).

## 라이선스

공개 저장소다. 라이선스는 한 번 넣고 잊는 것이 아니라 **의존성이 들어올 때마다 같이 보는 것**이다.

- **palmar 는 MIT 다** — `LICENSE`. 저작권자는 `maengyo` 다(개인정보를 새로 늘리지 않으려고
  GitHub 계정을 쓴다).
- **남의 코드나 자산을 저장소에 넣을 때, 같은 커밋에 고지를 함께 넣는다.** 줄인(minified) 빌드는
  머리말이 없는 경우가 많다 — xterm.js 셋이 그랬고, 그동안 MIT 고지 없이 실려 있었다(2026-09-08 발견).
  고지는 파일 **옆에** 둔다(`palmar/web/vendor/LICENSE-xterm`), 링크가 아니라 사본으로.
- **라이선스 전문은 기억으로 쓰지 않는다.** 원본에서 받아 그대로 넣는다.
- **넣기 전에 양립하는지 본다.** MIT·BSD·ISC·Apache-2.0 은 MIT 배포에 실을 수 있다. GPL 계열은
  못 싣는다 — 실으려면 palmar 전체가 따라가야 한다. 애매하면 넣지 말고 물어본다.
- **버전을 올릴 때 라이선스가 그대로인지 확인한다.** 바뀌는 일이 있다(`palmar/web/vendor/VERSIONS` 에 적는다).
- 새 의존성이 생기면 `README.md` 와 `README.ko.md` 의 라이선스 절도 같이 고친다.

## 하지 말아야 할 것

- **브라우저를 `127.0.0.1` 밖으로 열지 마라.** 이걸 HTTP 로 여는 것은 셸을 여는 것이고
  인증이 없다. 원격이 필요하면 `ssh -L` 로 넘긴다. host 를 바꾸는 옵션을 두지 마라.
- **신호등 색을 다른 데 쓰지 마라.** 매핑은 사람이 정하지만(⑥), **초록·호박·빨강·회색을 미리
  비워 둔다** — polycanv 도 넷을 썼다.
- **이모지로 신호등을 그리지 마라.** 색과 모양을 우리가 못 정하고 폰트마다 다르게 그려진다
  (polycanv 는 TUI 에서 칸 폭이 밀려 뺐다 — 브라우저에서는 이유가 다르지만 결론은 같다).
- **기다리는 상태를 보는 것만으로 끄지 마라.** 승인 프롬프트는 쳐다본다고 사라지지 않는다.
- **상태만 보고 자동으로 키를 보내지 마라.** herdr 스파이크에서 그 Enter 가 codex 업데이트를 실행했다.
- **미니맵은 스토어에서 읽고 `transform` 으로만 움직여라.** 스크롤마다 DOM 레이아웃을 읽지 마라 —
  지금은 안 아프지만(스파이크 J) cate 가 374/s 를 낸 것이 그 종류다. 캔버스는 무한히 자란다(⑩).
- **카메라 줌을 만들지 마라.** 첫 버전에서 뺐다(2026-09-07). 브라우저 `Ctrl+-` 로 충분하고,
  그 단축키를 **화면에 보이게 적는 것**이 우리가 할 일이다. 나중에 넣게 되면 cate 가 겪은 여덟 가지를
  먼저 읽어라 — 줌으로 PTY 를 리사이즈하면 90열이 87열이 된다(`own-runtime.md`).
- **줌 값을 창마다 prop 으로 내리지 마라.** cate 가 그것 때문에 초당 374번 다시 그렸다.
  창은 스토어에서 직접 읽는다.
- **파이썬 3.9 를 바닥으로 지켜라.** `/usr/bin/python3` 가 3.9.6 이고, 그게 "설치 0" 의 근거다.
  3.10+ 문법을 쓰면 사용자가 `uv` 를 먼저 깔아야 한다(② , 스파이크 H).
- **나중에 Bun 으로 옮길 수 있게 짜라.** ② 는 되돌릴 수 있게 정했다 — 프로토콜을 문서로 고정하고,
  터미널 상태를 데몬 밖에 두고, 표준 라이브러리로 되는 것은 표준으로 한다.
- **데몬 핸드오프를 만들지 마라.** ⑦ 은 (b) 로 정해졌다(2026-09-07) — 데몬은 업데이트 때만
  재시작하고 그때 에이전트는 `--resume`, 셸 pane 은 죽는다. fd 전달을 전제로 구조를 짜지 마라.
  되긴 된다(스파이크 G) — 안 하기로 한 것이다.
- **데몬이 오래 도는 것을 전제로 짜라.** 재시작으로 새는 것을 씻어 내지 않는다. 메모리·fd 누수는
  (b) 에서 더 아프다.
- **클라이언트가 준 명령을 실행하지 마라.** 데몬이 `$SHELL` 을 박아 쓴다. `cmd` 파라미터를 두는 순간
  방문한 웹사이트가 명령을 고를 수 있다 — 브라우저는 웹소켓에 CORS 를 적용하지 않으므로
  `127.0.0.1` 에 묶는 것으로 막히지 않는다(#29).
- **웹소켓 업그레이드에서 `Origin` 을 검사하고 토큰을 요구하라.** 토큰은 `~/.palmar/run/` 0600.
- **`~/.palmar/` 는 0700 이다.** 거기 shim 이 있고 PATH 맨 앞에 온다 — 남이 쓸 수 있으면
  `claude` 를 칠 때 무엇이 뜰지 남이 정한다. 소켓·토큰·settings 파일을 `/tmp` 에 두지 마라.
- **경로 포함 검사에 `startswith` 를 쓰지 마라.** 형제 디렉터리가 통과한다(`/x/web` 에 `/x/web-evil`).
  `is_relative_to` 를 쓴다.
- **사용자의 `~/.claude/settings.json` 을 쓰지 마라.** 훅은 shim 이 `--settings` 로 끼운다. 사용자 파일을
  고치는 코드는 어떤 이유로도 넣지 않는다.
- **겹치기 설정을 만들지 마라.** 2026-09-07 에 없앴다. 창은 겹치지 않고, 새 창이 옆을 민다
  (termcanvas 식 최소 이동). 겹칠 일이 없으니 켜고 끌 것도 없다.
- **밀어내기를 타일링으로 바꾸지 마라.** 크기는 사용자가 정한다. palmar 가 푸는 것은 겹침뿐이다.
- **확대 보기를 카메라로 만들지 마라.** 하나를 캔버스 크기로 펼 때 **행·열이 진짜로 늘어난다.**
  그리고 그때도 레일은 남는다 — 크게 보는 동안 누가 기다리는지가 안 보이면 안 된다.
- **홈 전체를 훑거나 트리를 지켜보지 마라.** 펼친 폴더 하나만 그때 읽는다. 다시 읽는 것은
  사용자가 새로고침으로 시킨다. `git status` 도 돌리지 마라 — 브랜치 이름만 파일에서 읽는다.
- **파일을 만들거나 지우거나 이름을 바꾸지 마라.** 폴더 만들기까지가 런처의 일이다.
  나머지는 터미널이 한다 — 그 옆에 터미널이 있다.
- **창에 macOS 식 빨강·노랑·초록 버튼을 달지 마라.** 그 세 색은 신호등의 것이다.
  모서리는 둥글게 하되 그 자리에는 신호등을 앉힌다.
- **서버에서 터미널 화면을 해석하는 것을 기본값으로 삼지 마라.** 재접속 복원은 먼저 다시 그리게
  하는 길(SIGWINCH)부터 잰다. 헤드리스 에뮬레이터는 마지막 수단이고, 쓰자고 하려면 ⑨ 의 측정을
  들고 와야 한다.
- **테스트 서버·에이전트는 깨끗한 환경에서 띄워라.** Claude Code 세션 안에서 띄우면 자식이
  `CLAUDE_CODE_*` 를 물려받아 auto mode 로 뜬다(실측).

## 이 기계의 제약

**디스크가 거의 항상 가득 차 있다.** 큰 빌드 전에 `df -h` 로 확인하고 임시 파일을 남기지
마라. 디스크가 차면 빌드가 엉뚱한 오류로 실패하고, 심하면 명령 자체가 안 돈다.

## 아직 정하지 않은 것 — 미리 정하지 마라

`docs/decisions.md` ①~⑩ 참고. 웹 스택, 데몬의 언어, 좌표 저장 위치, **앱을 언제 낼지**,
가벼움의 기준, 신호등 색 매핑, 데몬 재시작 전략, codex 상태(훅 vs 타이틀), 재접속 복원,
**자동 배치 규칙**.
**결정은 사람이 한다 — 에이전트가 대신 정하지 마라. 후보를 줄이는 것도 정하는 것이다.**
언어는 특히 되돌리기 비싸다.
