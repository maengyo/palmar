# palmer — 작업 지침

herdr 위에 얹는 **공간형 캔버스 클라이언트**. 브라우저에서 돌고, 터미널을 원하는 자리에
원하는 크기로 놓고, 각각이 일하는 중인지 나를 기다리는지 끝났는지 보여준다.

## 절대 원칙

1. **런타임을 다시 만들지 않는다.** PTY·세션 영속성·에이전트 상태 감지는 herdr 가 한다.
   직접 구현하려는 충동이 들면 그건 herdr API 를 덜 읽었다는 신호다.
2. **프런트엔드는 하나다.** 브라우저로 만든다 — 줌이 공짜로 오고, 어느 OS 에서도 같다.
   창처럼 쓰고 싶으면 `--app=` 이고, 나중에 진짜 창이 필요하면 **같은 웹 코드를 Tauri 에
   넣는다**(Electron 이 아니다 — 무겁다는 게 우리가 피하려는 바로 그것이다).
   CLI 는 만들지 않는다. herdr 에 이미 TUI 가 있고, 두 번째는 더 못한 복제가 된다.
3. **가벼움이 기능보다 우선이다.** 이 프로젝트가 존재하는 이유가 "Orca·Paseo 는 무겁다"
   이므로, 기능을 넣을 때마다 **무엇을 안 넣을지**를 함께 정한다.
4. **설치가 한 줄이어야 한다.** herdr 가 `curl | sh` 로 끝나는 것처럼. 네이티브 의존성,
   빌드 툴체인, 외부 서비스, 계정 — 넷 다 설치 마찰이고 이 프로젝트의 반대편이다.

## 구조

```
web/       캔버스 UI. xterm.js 로 pane 을 그린다
server/    herdr 유닉스 소켓 ↔ 브라우저 웹소켓 다리. 얇게 유지한다
docs/      조사 기록, 날짜별 작업 기록
```

**server 는 다리일 뿐이다.** 상태를 들고 있으려 하지 마라 — 진실은 herdr 에 있고,
여기에 사본을 두면 반드시 어긋난다.

## herdr API (실측)

소켓: `~/.config/herdr/herdr.sock` · 뉴라인 구분 JSON · 프로토콜 20 · 메서드 91개

```jsonc
// 요청. params 는 비어 있어도 **반드시 보내야 한다** (없으면 invalid_request)
{"id":"1","method":"ping","params":{}}
```

자주 쓸 것:

| 메서드 | 쓰임 | 주의 |
|---|---|---|
| `session.snapshot` | 전체 상태 | pane 마다 `agent_status` 가 들어 있다 |
| `pane.read` | 화면 읽기 | **`source` 필수** (`visible`/`recent`/…), `format:"ansi"` 여야 이스케이프가 산다 |
| `pane.send_text` / `send_keys` | 입력 | 식별자는 `pane_id` (`"w1:p1"` 꼴) |
| `pane.resize` / `pane.zoom` | 크기 | |
| `events.subscribe` | 상태 변화 | 스트리밍 가능 여부는 아직 미검증 |
| `layout.export` / `apply` | 배치 | 자유 배치를 여기에 실을 수 있는지 확인할 것 |
| `workspace.create` | 새 작업공간 | `{"path": "..."}` |

스키마 전문: `herdr api schema --json` (255KB). **기억하지 말고 매번 확인해라** —
프로토콜 버전이 올라간다.

## 검증

- **근거 없이 "됐다"고 하지 않는다.** herdr 는 실제로 띄워서 소켓을 두드려 확인한다.
- 브라우저 화면은 사람이 봐야 한다. 자동으로 확인할 수 있는 것(소켓 응답, 상태 전이)과
  사람이 봐야 하는 것(색·배치·읽히는가)을 **보고할 때 구분해라.**
- herdr 를 띄웠으면 **끝나고 `herdr server stop`** 으로 정리한다.

## 이 기계의 제약

**디스크가 거의 항상 가득 차 있다.** 큰 빌드 전에 `df -h` 로 확인하고, 임시 파일을
남기지 마라. 디스크가 차면 빌드가 엉뚱한 오류로 실패한다.

## 아직 정하지 않은 것

- **웹 스택** — 프레임워크를 쓸지 말지. "가볍게"가 목표이므로 기본값은 **안 쓰는 것**이고,
  쓰자고 하려면 이유를 대야 한다.
- **다리(server)의 언어** — TS 로 통일할지, 파이썬으로 할지.
- 자유 배치 좌표를 herdr `layout` 에 실을지, palmer 가 따로 보관할지.

이 셋은 첫 스파이크 결과를 보고 정한다. 미리 정하지 마라.
