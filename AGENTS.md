# palmer — 작업 지침

[herdr](https://github.com/herdrdev/herdr) 위에 얹는 **공간형 캔버스 클라이언트**.
브라우저에서 돌고, 터미널을 원하는 자리에 원하는 크기로 놓고, 각각이 일하는 중인지
나를 기다리는지 끝났는지 보여준다.

## 절대 원칙

1. **런타임을 다시 만들지 않는다.** PTY·세션 영속성·에이전트 상태 감지는 herdr 가 한다.
   직접 구현하고 싶어지면 그건 herdr API 를 덜 읽었다는 신호다.
2. **프런트엔드는 하나다.** 브라우저로 만든다 — 줌이 공짜로 오고 어느 OS 에서나 같다.
   창처럼 쓰려면 `--app=`, 나중에 진짜 창이 필요하면 **같은 웹 코드를 Tauri 에 넣는다**.
   Electron 은 검토하지 않는다 — 무겁다는 게 우리가 피하려는 바로 그것이다.
   **CLI 는 만들지 않는다.** herdr 에 이미 TUI 가 있고 두 번째는 더 못한 복제가 된다.
3. **가벼움이 기능보다 우선이다.** 이 프로젝트의 출발점이 "Orca·Paseo 는 무겁다" 이므로,
   기능을 넣을 때마다 **무엇을 안 넣을지**를 함께 정한다.
4. **설치가 한 줄이어야 한다.** herdr 가 `curl | sh` 로 끝나는 것처럼.
   네이티브 의존성·빌드 툴체인·외부 서비스·계정 — 넷 다 설치 마찰이다.

## 구조

```
web/       캔버스 UI. xterm.js 로 pane 을 그린다
server/    herdr 유닉스 소켓 ↔ 브라우저 웹소켓 다리. 얇게 유지한다
docs/      조사 기록, 날짜별 작업 기록
```

**server 는 다리일 뿐이다.** 상태를 들고 있으려 하지 마라 — 진실은 herdr 에 있고,
사본을 두면 반드시 어긋난다.

## 검증

- **근거 없이 "됐다"고 하지 않는다.** herdr 는 실제로 띄워서 소켓을 두드려 확인한다.
- **기억이 아니라 실물을 본다.** 프로토콜 버전이 오른다. `herdr api schema --json` 이 원본이다.
- 자동으로 확인되는 것(소켓 응답·상태 전이)과 **사람이 봐야 하는 것**(색·배치·읽히는가)을
  보고할 때 구분해라. 후자를 "확인했다"고 쓰지 마라.
- herdr 를 띄웠으면 끝나고 `herdr server stop` 으로 정리한다.

## 하지 말아야 할 것

- **브라우저를 `127.0.0.1` 밖으로 열지 마라.** 이걸 HTTP 로 여는 것은 셸을 여는 것이고
  인증이 없다. 원격이 필요하면 `ssh -L` 로 넘긴다. host 를 바꾸는 옵션을 두지 마라.
- **신호등 색을 다른 데 쓰지 마라.** 초록·호박·회색은 상태의 것이다.
- **이모지로 신호등을 그리지 마라.** 폭이 터미널·폰트마다 달라 배치가 밀린다.
- **`blocked` 를 보는 것만으로 끄지 마라.** 승인 프롬프트는 쳐다본다고 사라지지 않는다.

## 이 기계의 제약

**디스크가 거의 항상 가득 차 있다.** 큰 빌드 전에 `df -h` 로 확인하고 임시 파일을 남기지
마라. 디스크가 차면 빌드가 엉뚱한 오류로 실패하고, 심하면 명령 자체가 안 돈다.

## 아직 정하지 않은 것 — 미리 정하지 마라

`docs/decisions.md` 참고. 웹 스택, 다리의 언어, 좌표 저장 위치, 창으로 낼지 여부.
**첫 스파이크 결과를 보고 정한다.** 넷 다 되돌리기 비싸다.
